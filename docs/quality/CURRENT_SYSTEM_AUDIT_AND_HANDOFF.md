# Current System Audit and Handoff

Status: **Current contract / living document**
Last updated: **2026-09-24 13:55 Asia/Singapore**
Current objective: **Sync the published `staging-deploy` Render Blueprint,
supply its Aiven/Cloudinary secrets without committing them, and prove the green
local release candidate against disposable production-shaped staging before
deployment; keep the local
worktree storage lean without deleting
development data, uploaded media, recovery backups, secrets, source files, or
installed environments; keep Admin Analytics backed by one permission-scoped,
read-only live-data endpoint with six selected-only operational workspaces,
explicit Asia/Manila periods, validated workspace filters, honest comparison
ranges, qualification-aware technician rankings, confirmed-sales accounting,
inventory coverage and shortage risk, after-sales/maintenance visibility, and
validated monthly seasonal-trend demand forecasts, holdout error evidence,
historical service-location allocation, and projected-item-demand evidence
in a modern glance-first hierarchy across desktop
and mobile, with the shared Analytics Assistant consuming the same readiness
contract, never presenting historical evidence as a validated prediction, and
withholding future quantities unless freshness, history, per-service volume,
WAPE, and baseline-comparison publication gates all pass,
with complete wrapping assistant messages and a local fallback for provider
responses stopped before completion,
and with complete labels instead of clipped chart axes, ellipses, or hidden
mobile workspace tabs;
keep record-detail dialogs truthful across admin, client,
and technician workspaces, including turnover completion semantics, readable
timeline identities, calendar request notes, display names, and image/video proof;
make notification-derived sidebar badges acknowledgeable at their destination
while preserving unrelated unread alerts; make technician job details operationally complete with
schedule, scope, client phone, checklist progress, active-versus-cancelled
equipment clarity, and a state-aware next action; keep technician job completion clear and evidence-backed
with one consistently named Finish Job action and server-enforced completion
photos for regular service work, keep job cards action-focused without duplicate
navigation controls, make Dispatch Board team availability immediately legible
with qualified Online, Busy, and Offline technician states, keep selection
consistent with the documented derived availability rule, preserve the completed
local reconciliation evidence, and advance the audited system toward production readiness for
a small-to-medium service business while preserving the dirty worktree and
requiring evidence at each release gate. Phase 1 release reliability is
complete, and the Phase 2 security gate now capability-scopes administrator
staff messaging, client support, support notifications, ticket WebSockets, and
operational settings across backend and frontend. Phase 3 now isolates the full
operations dashboard, coverage feeds, completed-job evidence, technician field
inventory, dashboard stock alerts, and direct client ticket replies behind their
specific capabilities; spoofed support-image content is directly rejected.
Django was patched from 6.0.6
to 6.0.8 after a dependency audit identified four fixed advisories. The fresh
2026-09-06 remediation also upgraded Django REST Framework to 3.17.2 and the
frontend build-only transitive packages to `browserslist==4.28.9` and
`postcss-selector-parser==6.1.4`; `pip-audit` and both npm audit scopes now
report no known vulnerabilities, and the complete release and browser gates
pass. The exhaustive Chen ERD is now generated directly from the physical DBML;
all 102 diamonds connect to the actual entity boxes through structured gutters
and dedicated relationship rails on the same editable page, using the supplied
classic green-entity/yellow-attribute/orange-diamond Chen reference style.
Stable `R001`-`R102` identifiers now appear on both legs and their matching
diamonds, which also state both endpoint entities, fields, and cardinalities so
long routes can be read without guesswork. Hub
degree and module affinity now drive placement instead of database table order.
The generated `ERD_CURRENT.md` is the complete end-to-end text edition with all
51 entities, 676 typed and constrained attributes, and 102 relationships.
`ERD_CURRENT_CONNECTIONS.md` remains the shorter connection-only companion;
both write relationships as source-cardinality-verb-cardinality-target lines
with exact field mappings.
Root-invoked Django discovery executes
all current tests, Windows
Playwright owns and cleanly stops isolated E2E servers, CI jobs are time-bounded,
and `npm run quality` provides one repeatable checks/migrations/tests/build/smoke
gate (`npm run quality:full` selects all browser tests). Phase 4 guarantees
nonblank normalized evidence at the central ticket
transition boundary, requires operator reasons for manual status changes,
reschedules, and inspection exceptions, and separates public-site publish,
asset-upload, and asset-delete authority while preserving the legacy manage
umbrella. Phase 5 now provides a guarded `npm run quality:staging` gate for live
PostgreSQL, Redis, durable-media, row-lock concurrency, HTTPS/header/CORS, and
anonymous-access evidence. Its safety interlocks and local behavior are tested;
the gate still must be run against separately provisioned disposable staging
services before production readiness can be claimed. Keep the audited full-stack system
deployable, preserve the
dirty worktree, and continue improvements without committing until explicitly
authorized. The after-sales ticketing phase now has controlled lifecycle
transitions, required resolution/reopen reasons, owner assignment, server-side
pagination, response deadlines separated from warranty expiry, and an
append-only case activity timeline. Inventory now supports explicit category
management, customizable product details/types/units, normalized unique SKUs,
opening-balance ledger history, retirement safeguards, and a non-overlapping
action column. Its admin workspace now prioritizes physical, reserved, and
available stock; provides attention and condition filters, compact desktop and
mobile layouts, explicit receive/correction actions, item-level ticket
reservation context, and a reachable guarded stock-movement dialog without
duplicating movement controls inside item editing. The existing scheduled
landing-page promotion flow was verified. The real public landing page now uses
defined, accessible hero/footer actions rather than absent theme classes, gives
every service a truthful next step, supports usable mobile navigation, and
resolves Home/About cross-page section links to the actual services, calculator,
and contact regions while retaining editor-managed copy, promotions, and
calculator settings.
Authenticated clients now create solar calculations directly inside the Solar
Estimates workspace instead of being sent to the public landing page. The same
calculator and published settings remain shared with the public experience,
while the client shell stays visible and newly saved estimates appear in the
account list immediately.
Admin walk-in intake now opens as a focused, responsive dialog instead of an
oversized inline queue form. It separates client/service, schedule, and
location work, makes review-versus-immediate-approval explicit, protects dirty
drafts before closing or clearing, and closes with visible confirmation after a
successful save while preserving the existing request API and approval rules.
Activity Logs now operates as a usable audit workspace rather than a page-local
timeline: filtered summary totals come from the complete server queryset,
expanded events expose stable IDs, exact zoned timestamps, actor IDs, IP/user
agent evidence, and metadata, and authorized administrators can refresh or
stream a formula-safe CSV export using the same active filters. Known target
types also provide permission-preserving navigation to their related admin
workspace without pretending the current pages support record-level deep links.
The latest whole-system functional audit passed the unified quality gate and
then exercised all 193 maintained browser scenarios. The first complete browser
run exposed two order-sensitive test assertions rather than product failures:
Activity Logs assumed the newest event was service-related, and My Jobs searched
for obsolete generic copy instead of the current visible job controls. Both
assertions now target the actual service event and current accessible UI; their
focused rerun passed 2/2 and the final uninterrupted full suite passed 193/193.
Every authenticated role now has a direct Notifications sidebar destination with
an API-backed total unread badge capped visually at `99+`. One authenticated
notification context now owns shell/dashboard loading, 45-second recovery
polling, and read/delete actions instead of each consumer issuing duplicate
request pairs. Read/delete actions still announce immediate refreshes, transient
failures retain the last known state, and contextual unread badges on related
work pages remain available.
The admin Service Calendar is now an operations workspace rather than a direct
reschedule launcher. It provides monthly operational counts, work/service/team/
status filters, a mobile date-led agenda, synchronized month/day navigation,
legacy-overlap warnings, and a details-first modal with client, location,
assignment, schedule, and work context plus explicit ticket/dispatch/reschedule
actions. New manual assignments, crew assignments, reschedules, and automatic
assignments reject exact-time overlaps for the same technician while retaining
the existing duration-based daily capacity limit. Calendar events now include
the bounded staff-only fields required to explain those decisions.
A professor-format final diagram pack now provides exactly one manually laid
out draw.io Context Diagram, one unified draw.io DFD, and one generated
single-page exhaustive Chen draw.io ERD matching the approved pre-oral notation
and presentation. At the user's direction, the Chen figure includes the same 51
entities, all 676 local attributes, and all 102 in-scope relationships as the
physical contract. It is generated directly from the DBML: wide domain lanes
contain every canonical entity and attribute, and all relationship legs connect
those actual entities to diamonds through clear exits, shared domain gutters,
dedicated rails, and visible bridge arcs. The physical
database-schema DBML is generated from and matches all 51 active application
model tables, every local column, and all 102 in-scope model relationships;
it also removes retired Firebase/SMS/template-era concepts. The sources retain the
paper's actor/process/store and entity/diamond/attribute visual conventions;
the unified figures follow the professors' one-diagram-per-type requirement.
The admin/superadmin first screen now operates as an actionable operations
dashboard instead of a passive summary. Capability-safe KPI cards, nonzero
attention signals, approval/schedule counts, SLA rows, and active-job summaries
lead to their related workspaces; inventory, maintenance, after-sales, and
unassigned-visit risks use existing server evidence. Manual refresh exposes
progress and stale-data recovery, active technician wording no longer implies
real-time availability, and workflow percentages are labeled as estimates.
Request details use dialog/focus/Escape semantics, and dashboard rejection now
requires meaningful client-visible audit evidence rather than a generic reason.
Dashboard navigation now preserves the meaning of each signal: Pending
Approvals focuses the actual in-dashboard request-review queue, while active,
unassigned, missed-dispatch, SLA-warning, and SLA-overdue ticket links open
distinct URL-backed Service Tickets views. Signals without a truthful connected
destination remain informational instead of linking to a misleading generic
queue.
The same first screen now uses a restrained visual hierarchy: a compact live
status toolbar, lightly differentiated actionable KPI cards, a single compact
operations-attention strip, and clearer approval/schedule headers. Service
Tickets exposes its URL-backed work queues as a responsive labeled selector so
operators can change focus without losing the page context. The operations
attention strip uses a neutral outer border while preserving color only inside
the individual urgency indicators.
Cross-workspace ticket totals now disclose their scope: Service Tickets labels
the complete active set as assigned plus unassigned work, while Dispatch Board
labels only its eligible unassigned subset as Ready to Assign and states that
subset as `X of Y` active tickets.
Dispatch Board now presents assignment as a guarded three-step flow: select an
eligible ticket, choose its lead/crew, then review equipment and confirm.
Technician actions stay disabled until a ticket is selected, unavailable staff
cannot be newly selected, current assignments remain editable, and the summary
row prioritizes four active-work metrics instead of mixing operational and
historical counts.
Smart assignment now has one backend ranking contract for both unattended
automatic dispatch and the Dispatch Board's manual Find Best Match action. Both
paths enforce service skill, daily duration capacity, concrete-time overlap,
availability, location fitness, and a score above 30; they revalidate the
selected technician after row locking. The manual action no longer forces a
different technician when the current assignee remains the best match, and a
scheduled-but-not-started job no longer makes a technician globally busy while
daily capacity remains.
The dispatch review now has one authoritative Equipment Reservation Plan rather
than separate requirement, reservation, and editable blocks. It distinguishes
pending reservations from already issued equipment, prevents duplicate item
selection, labels item/quantity controls, includes current reservations in the
available-to-plan figure, warns about partial-stock shortages, and blocks
confirmation only for structurally invalid quantities while preserving the
backend's supported partial-reservation behavior.
Completed Job History now closes the remaining issued-equipment loop through a
two-party control. The assigned technician declares quantities, condition, and
notes; this creates a pending request without changing physical or available
stock. Dispatch-authorized receiving staff separately record the observed
condition and decision. Only sealed/usable verified returns create append-only
inventory movements and restore stock; rejection leaves stock unchanged, and
damaged/incomplete units cannot be verified into availability. Pending
quantities are excluded from duplicate submissions, every request preserves
both actors and timestamps, and both role dialogs keep the underlying completed
job record open.
A restrained UI-consistency pass now keeps one compact shell context header,
removes the unsafe/redundant global hard-refresh action, reduces double workspace
padding on profile/settings/editor surfaces, uses valid single-main landmark
structure, shortens the clipped after-sales navigation label, and reserves mobile
space for the assistant without redesigning role workflows. Logout confirmation
now uses the shared centered dialog instead of cramped controls inside the
sidebar, with a clear consequence message, safe Cancel action, pending state,
Escape/focus-trap behavior from the shared dialog, and focus restoration to the
Logout trigger after cancellation.
A route-complete manual QA playbook now provides a safe isolated setup, test
accounts, evidence and defect templates, public/account and access-control
passes, one ordered client-to-admin-to-technician golden lifecycle, all visible
admin/technician/client workspaces, failure/concurrency/file testing, responsive
and accessibility checks, reconciliation, and release sign-off.
The playbook now begins with a strict 27-step role-by-role execution order so a
tester can create each prerequisite record before checking its downstream
schedule, dispatch, field, document, inventory, Sales/Purchase Record, support,
reporting, security, and sign-off behavior.
It is now formatted as the clean canonical manual E2E checklist and includes a
separate 20-item operational/backend pass for health, email, notifications,
WebSockets, automations, SLA deduplication, location retention, external
providers, media, exports, settings, audit history, idempotency, constraints,
capabilities, staging perimeter, and recovery behavior.
Admin, technician, and client profiles now discard cancelled drafts and selected
photo previews, update authenticated shell identity without a page reload, state
the existing image and pending-email verification rules, and share one labeled
password/security panel. The duplicate profile-page sign-out card was removed
because logout remains available in the shell. Client read mode now exposes the
middle-name-complete display name, landline, and company information that its
edit form already stores; role-specific technician performance and skills remain.
Their shared visual treatment now uses a restrained solid brand-blue identity card,
larger avatar, role/status badges, canonical account ID, account/email status,
member-since summary, and bordered contact fields. Contact and security sections
have clearer hierarchy and the redundant local profile description was removed
in favor of the canonical shell description.
The shell title is now the single canonical page title across authenticated
workspaces; repeated content-area titles were removed while descriptions,
actions, dynamic record identities, and true report/document headings remain.
Filter-heavy list and report pages now use one search-first progressive pattern:
optional choices stay behind a labeled Filters control, applied choices remain
visible as removable plain-language chips, and one Clear all action restores the
page defaults. Required form choices and immediate operational controls remain
visible instead of being incorrectly hidden as optional filters.
Coverage Heatmap keeps its two immediate map-layer controls in that same compact
toolbar, eliminating the otherwise empty Filters card and separate Map layers
card while preserving independent service-density and technician-coverage
toggles.
Technician Tracking now represents the canonical active ticket lifecycle rather
than omitting ready-for-service and inspection work, distinguishes technician GPS
health from job-site coordinate gaps, exposes connected active-job context, and
retains last-known data through transient refresh failures. Administrators can
search and filter the field view, select a technician for contact, current-job,
GPS-accuracy, and recent-trail detail, and identify direct-line route fallbacks
without mistaking them for calculated road routes.
Tracking bounds, label, minimum zoom, history retention, and recent-trail window
are now validated backend environment settings returned to authenticated map
workspaces. Technician navigation displays the collection purpose, authorized
audience, and retention period before collection; it starts GPS, routing, and
navigation state only after an explicit Start location sharing action and stops
the watcher on request or page exit. The technician dashboard now follows the
same consent boundary: it makes no geolocation call on entry, presents one
session-scoped Start location sharing action inside the tracking card, and starts
the initial fix plus watcher only after that action. The daily operational automation deletes
expired history and clears expired current-location profiles.
Coverage Heatmap now shares the keyless OpenStreetMap tile source used by
Technician Tracking after CARTO introduced a separate basemap-key requirement.
Admin and technician Job History now share a genuine completed-work boundary:
`Completed` and `Turned Over / Accepted` records are included while the active
`Inspection Completed` stage is excluded. Both feeds are server-paginated;
technician search/date filtering is server-backed and recoverable on failure.
History details now connect actual start/end/duration, team, inventory,
checklist/proof, installed equipment, full field-report readings, warranty,
maintenance, after-sales, official-document metadata, and status timeline where
the role is authorized. Admin CSV export streams every filtered page and admin
document actions use the existing controlled document generator; the technician
download is explicitly labeled as a work summary rather than an official form.
Both history routes reset inherited document scroll on entry so their canonical
shell heading is not clipped after navigation from a longer workspace.
Their View details dialogs now use a restrained completed-record presentation:
clear status and ticket identity, stronger section hierarchy, a neutral
scrolling record body, and a conventional persistent footer. This was a visual
and usability refinement only; the connected data, permissions, document
actions, and completed-work workflows are unchanged.
Client accounts now have a separate support assistant with portal guidance,
client-scoped ticket/support summaries, safe credential refusal, and direct
links to the appropriate client workflows; it does not expose admin analytics.
Customer-facing support terminology treats the system as a record of delivered
products/services rather than a billing or payment-processing platform.
The connected Sales Records flow now provides draft/confirm/void-and-replace
lifecycle control, snapshots accepted quotations or actual inventory issues,
retains installed-equipment/warranty evidence, and gives clients an owned
read-only Purchase Records view without introducing payment processing.
Frontend/API identity labels now use one canonical visual scheme: `REQ`, `TKT`,
`CSC`, `ASC`, `SAL`, `QUO`, `ITX`, `RSV`, `DOC`, `EST`, and role-specific user
codes. Ledger codes use six digits and standard identities use at least four;
legacy/raw values are normalized for display without changing database IDs.
Operational automation now has one deployment entry point with `frequent` and
`daily` groups, 24-hour per-record SLA alert deduplication, correct urgent-first
maintenance catch-up, repository-relative Windows scheduling, and non-zero
failure propagation for analytics jobs.
Admin Analytics now uses one live, read-only `/api/admin/analytics/` endpoint
with a validated `workspace` selector. Overview retains exactly five primary
KPIs and adds a lower-emphasis cross-domain attention row. Technicians, Sales,
Inventory, After-Sales & Maintenance, and Forecasting load only their selected
aggregates. Comparison options cover a previous equal period, prior month,
prior quarter, prior year, or none; count/money changes use percentages while
rates use percentage points and zero prior values never produce infinity.
Technician rankings enforce category-specific observation thresholds and keep
lead versus crew credit separate. Sales uses confirmed Sales Records, separates
currencies, and never treats quotation text or estimates as revenue. Inventory
shows ticket-link coverage and deterministic shortage risk. Revisits are not
called rework, maintenance is not assigned to technicians without a model
relationship, and predictions remain hidden until both genuine-history and
validated-model gates pass. The frontend issues one cancellable request for the
selected workspace, rejects reversed dates locally, hides stale tab content,
and provides loading, empty, error, retry, refresh, and responsive states.
Reports remain the home for detailed/exportable records.
Admin Documents ticket prefill now imports the shared numeric-ID normalizer, so
installed-equipment and quotation lookups run instead of being skipped by a
caught frontend `ReferenceError`.
Technician completion checklists now show all service procedures on one
touch-friendly work-step screen instead of one wizard page per procedure, retain
per-step photo requirements, allow proof removal, and recover non-file drafts on
the same device. Site inspections now start with unanswered tri-state checks,
require explanations for negative findings and an explicit recommendation, show
readiness progress, retain device-local text/answer drafts, and use a mobile
sticky completion action. The API rejects implicit inspection answers and can
resume an incomplete one-to-one inspection after a transient submission error.
Both forms explicitly warn that device-local drafts preserve non-file fields
but selected photos/videos must be reattached after a refresh.
Completion checklists are now composed per requested service: a multi-service
ticket receives one ordered section for every service, each section uses that
service's own admin-configured procedures and equipment, and submitted step
records preserve their originating service ID/name and source-step position.
Schema cleanup retired three confirmed zero-row/disconnected models:
`history.ServiceHistory`, `notifications.NotificationTemplate`, and
`notifications.NotificationLog`. The active `ServiceStatusHistory`,
`Notification`, and secured append-only `TicketProgress` contracts remain.
Deletion migrations fail closed if unexpected rows exist and are not applied to
production pending explicit deployment authorization. They were applied to the
local SQLite database only after confirming all three target tables were empty
and creating an ignored local backup.
Before file-structure cleanup, an ignored SQLite backup and a secret-excluding
source-worktree ZIP were created and SHA-256 verified. The first cleanup step
removed only four dead history API/admin modules; the history app/migration
shell remains until production applies `history.0005`.
The former 176 KB `services/tests.py` monolith is now a discoverable
`services/tests/` package with 15 domain-focused test modules and one shared
import module. All 15 test classes and 118 methods were preserved, and complete
backend discovery remains at 372 tests.
The former 73 KB `services/serializers.py` monolith is now a compatibility
package with seven domain modules and explicit public re-exports. All 30
serializer class ASTs match the verified pre-cleanup source, so existing
`services.serializers` imports and API behavior are preserved.
Technician job progress is now a canonical status-evidence contract shared by
the admin dashboard and client ticket API. It has distinct Inspection visit and
Service job tracks, so the same operational status receives the correct
type-specific label/checkpoint and the record switches tracks when an approved
inspection becomes installation work. It never advances from elapsed time:
navigation and arrival remain active/visible, holds preserve the last recorded
checkpoint and identify it as paused, and only completed/accepted work reaches
100%. The UI calls these status checkpoints rather than measured task
completion and exposes an accessible stage label/value.
Clicking an admin dashboard progress card now opens a live ticket-details modal
instead of navigating immediately. The modal shows track/status progress,
technician and crew, client/service, schedule/start time, location, priority and
SLA, equipment reservations, and the recorded status timeline. It has explicit
ticket-queue and dispatch-board actions, Escape/backdrop close, focus return,
loading/retry states, and a mobile-safe scrolling body with persistent actions.
Its live detail is grouped into Client details, Service request, Assignment,
Visit details, Operational status, Equipment plan, and Status timeline. Client
ID/source/phone/email/account address, service description/items, scheduling and
ticket notes, admin ownership/assignment evidence, route estimate, and
reschedule reason are shown when available; truthful labeled empty values remain
visible when the source record has no data.
The same modal now uses progressive disclosure for lower scanning cost: the
checkpoint, priority/SLA, client/direct contact, lead/crew, schedule, and
location stay immediately reachable; Service details and Equipment plan start
collapsed; Status timeline opens with only its newest three events and can
expand to the complete record. Empty optional route/contact/ownership rows are
omitted, while active hold, reschedule, and SLA risks consolidate into one
Needs attention block.
The new return-control migration `inventory.0007` is applied to the user's local
development database and the disposable test/E2E databases. It has not been
applied to production/Aiven; remote application remains pending explicit
deployment authorization. Public client registration now provides persistent
field labels, field-associated API errors, backend-aligned password guidance,
a bounded request timeout, provider-neutral verification instructions, and a
throttled enumeration-safe verification resend. Its optional account address is
constrained to dependent CALABARZON province, city/municipality, and barangay
dropdowns while retaining the existing single text address API/database contract.
All actual paginated list workspaces now default to 10 records per page across
the API and admin, client, technician, and after-sales interfaces; compact
dashboard previews, lookup limits, and full export batches retain their separate
task-specific limits. Purchase Records now labels the amount as Recorded contract
value: accepted-quotation and signed-contract amounts are visibly source-controlled
and read-only, while the manual fallback exists only when neither source provides
an amount and requires an explanation before confirmation.**

This is the first document a new agent should read. It records the current
verified state so a routine handoff does not require another whole-repository
scan. Read the linked specialist contract only when changing that area. If code
or configuration disagrees with this file, treat that as documentation drift:
verify the code, then update this file in the same change.

### Latest pass-on checkpoint (2026-09-08)

- **Most recent completed work:** Removed the ambiguous editable Agreed Sale Value
  behavior from source-backed Sales Record drafts. The UI now says Recorded
  contract value, identifies its source, and keeps accepted-quotation or signed-
  contract amounts read-only. Manual value entry appears only without either
  source and requires an explanation; confirmation refreshes authoritative values.
- **Latest affected validation:** 7 focused Sales Record backend tests passed,
  including authoritative-source locking, confirmation refresh, signed-contract
  fallback, and reasoned manual fallback. The Vite production build passed with
  2,518 modules, Django system check and migration drift passed, focused isolated
  Chromium passed 1/1, and its 1440x900 screenshot was visually inspected.
- **Latest broad evidence:** `npm run quality` passed Django system check,
  dependency integrity, migration drift, all 394 discovered backend tests with
  3 expected PostgreSQL-only skips, the 2,517-module production build, and all
  15 browser smoke tests. The subsequent complete Playwright run passed 197/197
  scenarios in 17.5 minutes, including all-role functional journeys and 108
  desktop/mobile quality checks across all 54 canonical routes.
- **Deployment status:** this is still a verified development/release candidate,
  not an approved production deployment. The production-shaped staging gate,
  remote migration review/backups, scheduler configuration, external security
  review, and production secret/provider checks remain open release gates.
- **Next-agent boundary:** preserve the dirty worktree, do not rerun migrations
  or deploy without explicit approval, and start from the relevant specialist
  contract plus the dated evidence below rather than rescanning the whole repo.

## 1. Non-negotiable handoff rules

1. Treat every existing modified or untracked file as intentional user/prior-agent work.
2. Do not reset, revert, stash, clean, delete, migrate, commit, or push without current user authorization.
3. Update this document with every repository change, following `AGENTS.md`.
4. Record only validations actually executed. Keep older evidence dated if it has not been rerun.
5. Never put `.env` values, tokens, passwords, database URLs, SMTP credentials, Cloudinary secrets, or API keys here.
6. Development uses SQLite by default. Production is required to use PostgreSQL, with Aiven supported through `DATABASE_URL` and CA/SSL settings.
7. Uploaded production media is expected to use Cloudinary unless durable local media is explicitly configured and allowed.

GitHub Actions also enforces rule 3: a commit or pull request that changes any
other repository path must include this living handoff file.

## 2. Repository and Git state

| Item | Current state |
| --- | --- |
| Repository root | `C:\Users\Iman\Desktop\current\finals-revision\capstone-1` |
| Branch | `main` |
| HEAD | `69326d1 security: enforce append-only ticket progress access` |
| Remote | `origin -> https://github.com/JH1Npop/capstone-current.git` |
| Remote comparison | `main` is ahead of `origin/main` by one pre-existing local commit |
| Worktree | Dirty by design, including this handoff and `AGENTS.md` |
| Tracked deletions | None |
| Commit/push status | No commit or push performed for the current audit/fix phases |

Generated build output, Playwright output, the E2E SQLite database, local logs,
media, dependency directories, and real secrets are intended to remain ignored.
The real `.env` exists locally but must not be inspected merely to populate docs.

## 3. System architecture

| Layer | Implementation | Primary locations |
| --- | --- | --- |
| Frontend | React 18, React Router 7, Vite 8, Tailwind-style utilities, Recharts, Leaflet | `frontend/src`, `frontend/vite.config.js` |
| Backend | Django 6, Django REST Framework, Channels/Daphne | `backend/afn_service_management`, backend apps |
| Authentication | DRF token authentication with custom user roles/capability grants | `backend/users`, `frontend/src/context`, `frontend/src/rbac.js` |
| Realtime | Django Channels; in-memory locally, Redis required for production | `backend/*/consumers.py`, `backend/afn_service_management/settings.py` |
| Development database | SQLite by default | `backend/db.sqlite3` through `DATABASE_ENGINE=sqlite3` |
| Browser-test database | Isolated SQLite, flushed and reseeded by Playwright global setup | `backend/db.e2e.sqlite3`, `settings_e2e.py`, `e2e/global-setup.js` |
| Production database | PostgreSQL; Aiven supported through `DATABASE_URL` and SSL/CA variables | `settings.py`, `docs/deployment/AIVEN_DEPLOYMENT.md` |
| Production media | Cloudinary when credentials are configured | `settings.py`, upload validation helpers |
| Static files | WhiteNoise/Django static root; Vite production assets | backend settings and root/frontend build scripts |
| Deployment forms | Docker Compose, backend Dockerfile/Procfile, PythonAnywhere build, production checklist | root, `backend`, `docs/deployment` |
| CI/release gate | Time-bounded GitHub Actions living-handoff, backend, frontend, and browser smoke jobs; local smoke/full release commands | `.github/workflows/quality.yml`, `scripts/release-gate.mjs`, root `package.json` |

Backend domain apps are `users`, `services`, `inventory`, `messages_app`,
`notifications`, `progress`, `history`, and `api`. Document templates live in
`backend/document_templates`; generated/user media is not source code.

## 4. Environment and deployment contract

Use `.env.example` for local/development shape and `backend/.env.example` for a
production-oriented backend example. Required production categories are:

- Django: `DJANGO_ENV=production`, strong `SECRET_KEY`, `DEBUG=False`,
  `ALLOWED_HOSTS`, `CORS_ALLOWED_ORIGINS`, `CSRF_TRUSTED_ORIGINS`, HTTPS settings.
- PostgreSQL/Aiven: `DATABASE_URL` (preferred), SSL mode, optional Aiven CA
  certificate/path, connection timeout, and connection lifetime.
- Media: `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, and
  `CLOUDINARY_API_SECRET`, unless durable local media is deliberately allowed.
- Realtime: `USE_REDIS=True` and `REDIS_URL` in production.
- Email: SMTP backend, host/port/TLS-or-SSL, user, app password, and default sender.
- Optional integrations: route-service and analytics/AI API keys only when those
  features are enabled.
- Technician location: `TRACKING_REGION_NAME`, validated south/west/north/east
  bounds, minimum zoom, `TECHNICIAN_LOCATION_RETENTION_DAYS`, and
  `TECHNICIAN_LOCATION_TRAIL_MINUTES`. Daily operational automation enforces
  the configured retention period.

Production settings fail fast when secrets, PostgreSQL, trusted hosts/origins,
durable media, Redis, or SMTP requirements are inconsistent. Never deploy the
development SQLite database as the production source of truth. Follow
`docs/deployment/PRODUCTION_DEPLOYMENT_CHECKLIST.md` and
`docs/deployment/AIVEN_DEPLOYMENT.md`.

## 5. Routes and page coverage

The canonical page audit covers 54 destinations at desktop 1440x900 and mobile
390x844, producing 108 checks. Redirect-only aliases are inspected in
`frontend/src/App.jsx` and resolve to canonical destinations.

### Public/auth pages (8)

`/`, `/about-us`, `/login`, `/register`, `/forgot-password`,
`/reset-password`, `/verify-email`, `/solar-calculator`

The public navbar uses root-qualified section links, so Services, Solar
Calculator, and Contact Us continue to work from `/about-us`; the landing route
handles hashes after client-side navigation and exposes the same destinations in
an accessible mobile menu. Hero, service, and footer actions connect to the
calculator, registration, or email as labeled. The optional Our Work section is
fed only by published project entries that have complete details, a project
photo, and confirmed client permission; drafts and unapproved entries remain
private.

### Admin pages (26)

`/admin/dashboard`, `/admin/calendar`, `/admin/service-tickets`,
`/admin/dispatch-board`, `/admin/technician-tracking`, `/admin/technicians`,
`/admin/clients`, `/admin/services`, `/admin/inventory`, `/admin/documents`,
`/admin/sales-records`,
`/admin/analytics`, `/admin/reports`, `/admin/operations-report`,
`/admin/after-sales-cases`, `/admin/user-management`, `/admin/settings`,
`/admin/landing-page`, `/admin/landing-page/preview`, `/admin/activity-logs`,
`/admin/profile`, `/admin/notifications`, `/admin/messages`,
`/admin/client-support`, `/admin/coverage-heatmap`, `/admin/job-history`

### Technician pages (10)

`/technician/dashboard`, `/technician/my-jobs`, `/technician/schedule`,
`/technician/map-navigation`, `/technician/checklist`,
`/technician/inspection-checklist`, `/technician/notifications`,
`/technician/messages`, `/technician/job-history`, `/technician/profile`

### Client pages (10)

`/client/dashboard`, `/client/service-requests`, `/client/solar-estimates`,
`/client/requests`, `/client/requests/:requestId`, `/client/service-history`,
`/client/purchase-records`, `/client/support`, `/client/notifications`, `/client/profile`

The route audit checks expected navigation, uncaught browser errors, HTTP 5xx,
whole-page horizontal overflow, clipped buttons, button collisions, and unnamed
main actions. Functional suites separately cover authentication and primary/side
workflows. Documents has a real E2E save/reload path for quotation drafts.

## 6. Completed implementation themes

- Request idempotency and locked lifecycle decisions.
- Scheduling/dispatch eligibility and immutable progress/history behavior.
- Evidence-backed completion with a clear Finish Job action and a server-enforced
  completion photo for non-inspection work, plus inspection review, cancellation
  reasons, and SLA handling. Inspection completion remains exempt from the extra
  post-service photo because it follows its dedicated inspection evidence flow.
- Document JSON contracts, retry-safe quotations, final-record immutability,
  commissioning/turnover/warranty consistency, and draft persistence.
- Signature-checked image/video uploads, size limits, and safe media references.
- Participant-scoped messaging and active-recipient notifications.
- Inventory integrity constraints, customizable catalog/category management,
  opening-balance history, retirement safeguards, ticket-reservation context,
  operational desktop/mobile stock controls, and automation safety.
- Role/capability enforcement across frontend routes, APIs, reporting, and audit logs.
- Least-privilege separation for the operations dashboard, tracking/coverage,
  completed-job evidence, and technician field inventory, including safe admin
  login fallbacks and capability-scoped dashboard stock alerts.
- Analytics time-range corrections, active-user filters, forecast bounds, and
  scheduled-date handling.
- Production fail-fast configuration for PostgreSQL/Aiven, Cloudinary, Redis,
  HTTPS/origins, and SMTP.
- Responsive Documents actions, mobile Technician activity cards, and accessible
  icon-only Service Ticket and Messages actions.
- Detail-dialog accuracy across roles: turned-over work remains client-complete,
  shared timelines prefer readable service/client labels, request-only calendar
  events retain scheduling notes and route to approvals, job histories preserve
  their actual final status, and proof viewers distinguish images from videos.
- Search-first progressive filtering with active-choice chips and consistent
  clearing across inventory, audit, after-sales, job/report, map, and client
  history workspaces.
- Technician checklist usability: one-screen work steps, explicit inspection
  answers/negative-finding notes, readiness progress, removable evidence,
  device-local draft recovery, mobile completion controls, and resumable
  incomplete inspection submission.
- A role-separated client support assistant with deterministic portal guidance,
  client-owned ticket/support summaries, safety/credential guardrails, and human
  support escalation links; the privileged Analytics Assistant remains admin-only.
- Client support labels use "Purchase record" for the legacy `billing` category;
  the stored value remains unchanged for compatibility and does not represent a
  billing ledger or payment workflow.
- Canonical connected Sales Records with one active record per completed ticket,
  accepted-quotation or actual-inventory source snapshots, installed equipment
  and warranty evidence, immutable confirmation, reasoned void/replacement, and
  client-owned read access; no payment or balance state exists.
- Canonical zero-padded visual identities for requests, tickets, clients,
  technicians, follow-up cases, and quotations, including normalization of
  legacy `TCK` ticket labels to `TKT` without changing primary keys.
- Isolated cross-platform E2E seeding and configurable E2E server ports.

## 7. Schema state

The table below distinguishes migrations already applied to the user's local
development SQLite database (`backend/db.sqlite3`). Production/Aiven deployment
status is called out separately where applicable:

| Migration | Purpose |
| --- | --- |
| `inventory.0005_inventory_integrity_constraints` | Inventory balance, reservation, and transaction integrity |
| `inventory.0006_inventorycategory_inventory_category_name_ci_unique` | Normalized, case-insensitive category-name and SKU identity; applied locally and pending production review |
| `inventory.0007_equipmentreturnrequest_equipmentreturnrequestitem` | Pending technician return declarations and separate warehouse review evidence; applied locally and to fresh test/E2E databases, pending production/Aiven deployment approval |
| `services.0053_servicerequest_idempotency_key` | Client-scoped request idempotency key |
| `services.0054_quotation_reference_and_warranty_terms` | Quotation number and warranty terms |
| `services.0055_unique_demand_forecast_period` | One forecast per service/period/date |
| `services.0056_aftersalescaseevent` | Append-only after-sales case activity history; applied locally and pending production deployment review |
| `services.0057_salesrecord_salesrecordline` | Connected immutable Sales/Purchase Records; applied locally after fresh isolated tests and pending production/Aiven deployment |
| `history.0005_delete_servicehistory` | Guarded removal of redundant completed-service summary model; applied locally and pending production application |
| `notifications.0009_remove_notificationlog_notification_and_more` | Guarded removal of unused notification template/delivery-log models; applied locally and pending production application |
| `users.0038_adminsettings_landing_page_projects` | Admin-managed completed-project portfolio with consent-gated public publication; applied locally and to fresh test/E2E databases, pending production/Aiven deployment approval |

Playwright applies migrations only to ignored `backend/db.e2e.sqlite3`, then
flushes and seeds that isolated database. A production/Aiven migration still
requires backup, release review, and explicit deployment authorization.

## 8. Validation ledger

Results are evidence, not promises. Rerun checks affected by later changes.

| Date | Validation | Result | Scope/notes |
| --- | --- | --- | --- |
| 2026-09-24 | Staging release Git publication | **Passed**; the complete audited snapshot was committed as `6b67c8f` and pushed to new remote branch `origin/staging-deploy` | Pre-commit `git diff --cached --check` passed across 287 paths; the credential-pattern scan found only documented placeholder database URLs, and ignored local `.env`, SQLite, media, backup, Playwright-output, and credential files were not staged. The branch includes the previously green 429-test/201-browser release candidate and valid Render Blueprint. `main` was not changed; no Render service or Aiven migration was created or run. |
| 2026-09-24 | Free Render staging Blueprint validation | **Passed** Render CLI validation with three planned actions | `render blueprints validate render.yaml -o text` reported `valid: true` for the Singapore-region free Django/Daphne web service, React static site with SPA rewrite, and private Render Key Value instance. Aiven and Cloudinary credentials remain `sync: false`; Aiven certificate verification expects `/etc/secrets/aiven-ca.pem`. Free-tier SMTP, pre-deploy, persistence, sleep, and scheduler limitations are explicit. No Render resource, Aiven schema, migration, credential, commit, or push has been created or changed yet. |
| 2026-09-24 | Canonical full local release gate after deployment-blocker remediation | **Passed** all system/dependency/migration checks, 429 backend tests with 3 expected PostgreSQL-only skips, the 2,519-module production build, and all 201 isolated Chromium scenarios in 15.8 minutes | Inventory completion fixtures now include representative post-service proof while the production proof guard remains unchanged; focused backend passed 2/2. Activity Logs uses its server-backed `serviceticket` search rather than assuming a related event appears on page 1; its focused isolated check passed 1/1 and the full order-dependent suite passed. Technician Schedule Upcoming Tasks now uses a narrow-safe grid, wraps metadata, and places the Open action full-width on mobile; its focused 390px audit and full route audit passed. No schema, migration, route, persistent database, permission, or business-workflow behavior changed. Local release confidence is green; production approval still requires the separately provisioned `quality:staging` gate and deployment-service verification. After recording the evidence, 199.69 MiB of regenerated Playwright output, disposable E2E data, and bytecode caches was removed; production build output was retained. |
| 2026-09-24 | Deployment-readiness audit across configuration, dependencies, build, smoke flows, full-suite evidence, UI/UX, and staging controls | **No-go for production today**; local build/integrity/security/smoke checks passed, but the canonical full gate and production-shaped staging gate are not green | Django system check, migration drift, and `pip check` passed; the production Vite build transformed 2,519 modules; `pip-audit` plus root/frontend npm audits reported zero known vulnerabilities; isolated Chromium smoke passed 16/16 public, authentication, logout, and role-guard scenarios. Today's full evidence remains 424/429 runnable backend tests with 3 skips and 2 stale inventory completion-fixture failures, plus 199/201 browser scenarios with one Activity Logs pagination assumption and one real 390px Technician Schedule clipped action. `quality:staging` correctly refused without `STAGING_GATE_CONFIRM=disposable-staging`; no live PostgreSQL, Redis, durable-media, row-lock, HTTPS/header/CORS, email-delivery, or deployed WebSocket evidence exists. Docker is unavailable on this workstation, so images were source-reviewed but not built. The frontend Dockerfile/Compose stack explicitly runs Vite development mode and is not a production frontend image; static-host deployment remains supported by the successful build, `VITE_API_BASE_URL`, and SPA `_redirects`. The current local `.env` is not a production environment and lacks staging/production service declarations. Newly generated smoke artifacts and bytecode caches were removed afterward (44.40 MiB) without touching source or persistent data. |
| 2026-09-24 | Safe regenerable-storage cleanup | Passed; 263.48 MiB removed across 1,300 verified workspace targets, with zero selected targets remaining | Removed only Playwright results/report, root and frontend compiled build output, the disposable E2E SQLite database, Python `__pycache__` directories, and local debug/server logs. Every resolved target was checked to remain under the repository root before deletion. Preserved `.env` files, the development database, uploaded media, local backups, source/untracked implementation work, `venv`, and both dependency trees. No application behavior, schema, migration, or persistent development data changed. |
| 2026-09-24 | Fresh canonical full gate and direct complete browser audit | Gate failed at backend tests; direct browser run completed with 199 passed and 2 failed in 17.9 minutes | `npm run quality:full` passed Django system check, `pip check`, and migration drift, then ran all 429 backend tests: 424 passed, 3 PostgreSQL-only tests skipped, and the two known inventory completion tests returned HTTP 400 because their regular-service completion requests omit the now-required completion proof. The gate therefore did not reach its build/browser stages. A direct `npx playwright test` then migrated/flushed/seeded only `backend/db.e2e.sqlite3` and ran all 201 Chromium scenarios: Activity Logs could not find a service event among the newest 10 of 38 events on page 1, and Technician Schedule detected a clipped `Open` action at 390px; 199 scenarios passed, with no server 5xx or page/console error in the route audit. Failure screenshots and Playwright error contexts were inspected. No application/test behavior, development database, or production service changed. |
| 2026-09-22 | Complete isolated Chromium E2E audit | 199 passed, 2 failed in 30.3 minutes; Analytics passed | All 201 maintained scenarios ran sequentially against the disposable E2E database. The focused Analytics journey passed, including all six workspaces, exact-date comparison, Forecasting, Assistant fallback, mobile overflow, and complete-label checks; the independent desktop/mobile Analytics route audit also passed. Failure 1 was `3.15 - Activity Logs table loads`: the page correctly returned 38 records in four pages, but the test searched only the newest 10 for a service event; the same test passed alone, confirming an order/pagination assumption. Failure 2 was the Technician Schedule mobile route audit after prior workflows changed schedule state: an Upcoming Tasks `Open` button extended past the 390px viewport. That scenario passed alone and then passed 3/3 repeated isolated runs, confirming a state-dependent full-suite layout condition. No Analytics failure, server 5xx, page exception, or development-database write occurred. |
| 2026-09-21 | Validated seasonal-trend demand forecasting | Focused backend, production build, browser journey, system check, and migration drift passed; broad suite retains 2 unrelated baseline failures | Added a monthly calendar-seasonal plus damped 12-month trend model, rolling holdout validation, stored MAE/WAPE/bias/baseline evidence, freshness and publication gates, six-month service outlook, calendar-correct 7/30-day proration, historical-location allocation, and ticket-linked item-demand translation. The affected backend suite passed 30/30 after the final proration correction, Vite built 2,519 modules, and focused isolated Chromium passed 1/1. Django reported no system issues or migration changes. Full discovery ran 429 tests: 424 passed, 3 PostgreSQL-only tests skipped, and the same two pre-existing inventory completion tests returned HTTP 400. A read-only development-data dry run published 0/9 services because the database has only 20 genuine requests over about 4.4 months; no development-database write or migration occurred. |
| 2026-09-20 | Analytics Assistant cutoff prevention | Passed 23 focused backend tests, production build, browser overflow regression, system check, and migration drift | Assistant messages now preserve whitespace, wrap ordinary and unbroken text at any point when necessary, and no longer hide bubble overflow. Gemini candidates whose finish reason is not `STOP` are rejected so a `MAX_TOKENS` response cannot be shown mid-word or mid-sentence; the endpoint returns the complete local explanation instead. The new provider-truncation regression passed, final Vite build transformed 2,519 modules, and isolated Chromium passed 1/1 with zero hidden-overflow assistant elements and no message-bubble horizontal overflow. Django reported no system issues and no migration changes. |
| 2026-09-20 | Analytics full-label presentation | Passed production build, focused browser journey, mobile overflow check, source assertion, and screenshot review | Replaced fixed-width categorical chart axes with full-width labeled proportional comparison rows across Overview and all specialized workspaces, removed deliberate Analytics truncation, allowed insight and technician names to wrap, and made all six workspace tabs wrap visibly on narrow screens. Vite built 2,519 modules and reduced the Analytics chunk from about 431 kB to 411 kB. Final isolated Chromium passed 1/1 with visible full service text, zero `.truncate` elements in Analytics, visible mobile After-Sales/Forecasting tabs, and no horizontal main-content overflow. Fresh desktop and 390px full-page screenshots were inspected. Two intermediate failures were test-selector ambiguity only: a hidden service `<option>` and the new chart aria-label matched broad locators; both were made exact before the passing rerun. |
| 2026-09-20 | Analytics Assistant forecast-contract alignment | Passed 22 focused backend tests, production build, browser journey, system check, migration drift, and scoped source review | The assistant now loads Overview plus Forecasting, uses the canonical readiness evidence in local answers and the optional Gemini prompt, excludes unlinked inventory issues from assistant demand evidence, and reports future quantities, confidence, accuracy, and staffing projections as unavailable until a model is validated. The final Vite build transformed 2,519 modules. Focused Chromium passed 1/1 after opening the chatbox and verifying the unavailable-accuracy response. An intermediate rerun exposed that a question could be submitted before the assistant's parallel loads completed; quick questions and submit are now disabled while loading, and the final rerun passed. Django reported no system issues and no migration changes. |
| 2026-09-20 | Service-location and projected-item-demand readiness | Passed focused backend suite, production build, browser journey, system check, migration drift, and scoped diff check | Forecasting now shows real service-location request density, actual ticket-linked issued-item totals, linkage/history/mapping readiness, and the intended service-demand-to-stock-demand method. It returns no future quantities because no validated model exists. Focused Analytics passed 15/15, Vite built 2,519 modules, isolated Chromium passed 1/1 across all six workspaces plus the new Forecasting panels, Django reported no system issues, migration drift reported no changes, and the scoped diff check found no whitespace errors. The Forecasting query ceiling is 12. No schema, migration, route, development-database write, or unrelated module changed. |
| 2026-09-20 | Exact custom date/range comparison | Passed focused backend suite, production build, and browser journey | Added `comparison=custom` with required `comparison_start_date` and `comparison_end_date`. A one-day current range can now be compared with any earlier exact date; multi-day custom ranges must have the same inclusive duration, end before the current range, and remain non-overlapping. Missing, malformed, reversed, overlapping, and unequal-length inputs return controlled HTTP 400 responses, while the frontend blocks locally incomplete or invalid selections. Focused Analytics passed 15/15, Vite built 2,519 modules, and isolated Chromium passed 1/1 after comparing 2026-09-10 directly with 2026-08-05. |
| 2026-09-20 | Inventory metric-fit and Forecasting evidence visuals | Passed focused backend suite, build, browser regression, overflow assertion, and query review | Inventory metrics now use three columns at the `xl` breakpoint and six only at `2xl`; metric cards have safe minimum widths, wrapping values, and nonshrinking Current badges. Forecasting now shows four evidence summaries, a labeled genuine monthly-demand line with missing-month chips, and historical demand by primary service. Historical seed descriptions remain excluded and predictions remain gated. Focused Analytics passed 15/15, Vite built 2,519 modules, and isolated Chromium passed 1/1 with an explicit six-card overflow assertion and both Forecasting visual headings. Forecasting measured five queries and returned five populated calendar months across six services in the current local database. |
| 2026-09-20 | Six-workspace Analytics implementation | Analytics checks/build/browser passed; broad suite retains 2 unrelated baseline failures | Added selected-only Overview, Technicians, Sales, Inventory, After-Sales & Maintenance, and Forecasting responses and tabs; five validated comparison modes; qualified technician rankings; currency-safe confirmed sales; inventory linkage/shortage evidence; after-sales/maintenance semantics; and model-gated forecasting. Focused Analytics tests passed 14/14; combined Analytics/permission/reporting tests passed 27/27; Django check passed; migration drift found no changes; Vite production build passed with 2,519 modules; focused isolated Chromium passed 1/1 while switching through all six tabs and checking 390px overflow. Measured local default query counts were Overview 19, Technicians 13, Sales 21, Inventory 27, After-Sales 34, and Forecasting 3. Full Django discovery ran 422 tests: 417 passed, 3 skipped, and the same two pre-existing inventory completion tests failed with HTTP 400. No model, migration, route, development-database write, or unrelated runtime module changed. |
| 2026-09-20 | Analytics equal-period comparisons and stronger categorical visuals | Passed focused backend suite, build, browser regression, query budget, responsive check, and visual review | The unified endpoint now calculates the immediately preceding equal-length range with the same filters and returns honest ticket, completion, completion-rate, duration, service, status, lead-workload, and location comparisons. KPI cards show deltas only when a valid prior baseline exists; service, status, assignment, and location charts render Current versus Previous directly. The focused Analytics/permission/security/reporting suite passed 21/21, including the new exact comparison-period/formula contract and the unchanged maximum of 20 database queries. The Vite production build passed with 2,518 modules, focused isolated Chromium passed 1/1 with explicit comparison and filter-disclosure assertions, and fresh desktop/mobile screenshots were inspected. No model, migration, route, or unrelated-module change occurred. |
| 2026-09-20 | Analytics visual-weight and filter-density refinement | Passed build, focused browser regression, responsive check, and visual review | Removed the oversized dark Analytics hero, replaced it with a compact light page header, moved the four real-data operational highlights beside the KPI layer, retained Service and Status as the only always-visible dimensions, and placed custom dates, grouping, technician, priority, city, province, and assignment behind an accessible More filters disclosure. The Vite production build passed with 2,518 modules. Focused isolated Chromium passed 1/1 after adding open/close regression assertions for the progressive filter panel; desktop and full-page 390x844 mobile screenshots were inspected and mobile retained no main-content overflow. No backend, API, formula, route, schema, migration, or unrelated-module change occurred. |
| 2026-09-20 | Current Analytics implementation reference | Passed source and structure review | Added `docs/features/ANALYTICS_CURRENT.md` as the current-state reference for the live route, unified API, permissions, UI sections, formulas, filters, response shape, source models, readiness-only forecasting, operational scope, limitations, validation evidence, and source map. Every referenced implementation/test path was checked as present, all 13 numbered sections were found, and `git diff --check` reported no whitespace errors for the new documentation. This is documentation-only and does not change application behavior. |
| 2026-09-20 | Modern glance-first Analytics presentation | Passed build, browser regression, responsive check, and visual review | Reorganized only the Admin Analytics component into a high-signal operations header, derived top-service/period/workload/location summaries, stronger KPI hierarchy, priority-coded current attention, visible chart values, separate job-count and scheduled-hour workload views, and readiness progress without changing its route, API, data formulas, or other modules. The Vite production build passed with 2,518 modules. Focused isolated Chromium passed 1/1 across desktop and 390x844 mobile sizes, the mobile main region had no horizontal overflow, and both fresh screenshots were inspected. No backend, schema, migration, or development-database change occurred. |
| 2026-09-20 | Unified live Analytics implementation | Focused suite/build/browser/check passed; broad suite has 2 unrelated pre-existing failures | The 20-test Analytics, AI-summary compatibility, permission, and reporting-capability suite passed on a fresh isolated database. Coverage includes formulas, invalid durations, Asia/Manila midnight boundaries, zero-filled periods, every supported filter, role denial, technician self-scoping, read-only snapshot GET, insufficient-forecast no-write behavior, and a maximum of 20 queries. Django system checks passed. The Vite production build passed with 2,518 modules. Focused isolated Chromium passed 1/1 across desktop and 390px mobile assertions; the final rerun emitted no Recharts size warnings. Full Django discovery ran 416 tests: 411 passed, 3 PostgreSQL-only tests skipped, and the two pre-existing `inventory.tests.AutoInventoryWorkflowTests` completion assertions still returned HTTP 400 instead of 200; Analytics produced no full-suite failures. No migration or development-database write occurred. |
| 2026-09-18 | Admin Analytics curated-data overview | Passed build, browser regression, and visual review | Limited the first screen to actual workload, completion, speed, capacity, service-demand, ticket-status, and genuine attention data. Forecast and service-value estimates remain in the deeper Demand workspace; routine scheduled jobs no longer inflate attention. The Vite production build passed with 2,518 modules. Focused isolated Chromium passed 1/1 at desktop and 390x844 mobile sizes, including actual-service evidence, the clear attention state, zero truncation classes, and no mobile summary overflow. The fresh desktop screenshot was inspected; no schema or migration changed. |
| 2026-09-18 | Admin Analytics explanatory visuals | Superseded by the later curated-data overview | The labeled-breakdown build and browser regression passed, but forecast and routine schedule data were subsequently removed from the first screen after user review. Use the newer curated-data evidence above for the current code. |
| 2026-09-18 | Admin Analytics concise responsive UI | Superseded by the later labeled-visual refinement | The original concise UI build and focused browser regression passed, but the abstract mini-chart presentation was changed later the same day after user review. Use the newer explanatory-visual evidence above for the current code. |
| 2026-09-18 | Cross-role detail-modal accuracy corrections | Passed focused backend tests, frontend build, and browser regression | Added turnover-aware client completion normalization, readable shared timeline labels, calendar request scheduling notes and request-aware navigation, technician display names, final lifecycle status in technician history, and image/video-aware proof rendering. The dashboard/calendar, completed-job history, and follow-up contract suites passed 33/33, and the focused technician job-detail display-name contract passed 1/1. The Vite production build passed with 2,518 modules. Isolated Chromium passed 4/4 for Admin Calendar, Admin Job History, Technician Job History, and Client Service History. `git diff --check` found no whitespace errors beyond existing line-ending notices. No schema or migration changed. |
| 2026-09-15 | Destination-aware sidebar notification acknowledgement | Passed build and focused browser regression | Clicking a sidebar destination now immediately removes only the unread badges routed to that workspace and persists those notifications as read; opening the role's Notifications destination uses the existing mark-all-read contract. Unrelated destination badges remain unread. The Vite production build passed with 2,518 modules, and isolated Chromium passed 1/1 while verifying two My Requests alerts disappeared from both My Requests and the aggregate Notifications badge and both IDs reached the mark-read API. No backend, schema, or migration changed. |
| 2026-09-15 | Technician Job Details operational context | Passed API, build, browser, visual, and diff checks | The assigned-job API now exposes client phone, request scope, summed requested-service duration, and checklist step totals; the modal renders schedule time, estimated duration, scope, a valid phone action, checklist progress, active equipment ahead of collapsed cancelled history, and a state-aware Complete Checklist or Finish Job action. The focused API contract passed 1/1, the Vite production build passed with 2,518 modules, and the isolated Chromium My Jobs journey passed 1/1. Its final 1440x900 modal screenshot was inspected and showed clean hierarchy, fit, and actions. `git diff --check` found no whitespace errors beyond existing line-ending notices. The first browser attempt exposed an inexact test locator that counted `View Details`; the second incorrectly expected service equipment on an inspection ticket. Both test assumptions were corrected before the passing rerun. No schema or migration changed. |
| 2026-09-14 | Technician Finish Job clarity and proof enforcement | Passed focused backend suite and frontend build | Replaced the ambiguous `Complete (Proof)` wording with `Finish Job`, simplified the completion-photo instructions, and aligned both technician completion APIs with the regular-service photo requirement while preserving the inspection exemption. Seven focused completion tests passed, followed by the complete 52-test scheduling/warranty/assignment and maintenance modules. Django system check passed, the Vite production build passed with 2,518 modules, and `git diff --check` found no whitespace errors beyond existing line-ending notices. The first focused command exposed one missing explicit helper import plus one mistyped test selector; the import and selector were corrected before the passing reruns. No schema or migration changed; a fresh browser journey was not run. |
| 2026-09-14 | Technician Jobs duplicate navigation cleanup | Passed build and diff checks | Removed the redundant standalone Navigate link from each active-job card while retaining the canonical Details/Navigate action row. The technician workspace browser assertion now requires exactly one Navigate link per rendered Details action. The Vite production build passed with 2,518 modules and `git diff --check` found no whitespace errors beyond existing line-ending notices; the browser assertion was added but not freshly executed. |
| 2026-09-14 | Dispatch Board team-presence clarity | Passed build and diff checks | The qualified technician list now retains dispatch-ready, busy, and inactive accounts, sorts Online first, uses explicit Online/Busy/Offline badges and explanations, disables assignment for Busy/Offline accounts unless preserving the selected ticket's current lead, and states that Online represents dispatch readiness rather than live device connectivity. The Vite production build passed with 2,518 modules and `git diff --check` found no whitespace errors beyond existing line-ending notices. |
| 2026-09-14 | Technician availability reconciliation and Dispatch Board qualification | Focused checks and local reconciliation passed | Django system check and migration drift passed. Four new signal/command tests passed, covering scheduled-work availability, active-work busy state, completion release, direct lead reassignment, crew add/remove, dry-run reporting, and explicit application. The reviewed local repair changed the 4 stale technician profiles from unavailable to the derived available state; an immediate dry-run verification reported `mismatched=0`, and a direct read confirmed all four availability flags are true. The Vite production build passed with 2,518 modules. A broader scheduling-suite and focused browser attempt exceeded the available command capture window before final results and are not claimed as passed. No migration was introduced. |
| 2026-09-09 | Clean complete manual E2E checklist | Passed structural review | Refined the manual artifact into 13 numbered sections with explicit testing status/pass rules, a strict 27-step master lifecycle, all public and role workspaces, cross-cutting quality/failure testing, a new 20-item operational/backend-function matrix, and final reconciliation/sign-off. Internal section references, all 63 non-wildcard frontend route patterns, sequential order IDs, table structure, and Markdown fences were checked; documentation-only change |
| 2026-09-09 | Manual test master execution order | Passed structural review | Added a strict 27-step role-by-role sequence from isolated setup through public/account testing, superadmin prerequisites, client request, admin decision/scheduling/dispatch, technician inspection and service work, final documents/turnover, inventory return, Sales/Purchase Records, support/messaging/notifications, reporting, route sweeps, cross-cutting failures, logout security, and reconciliation. Each step names its completion condition and points to the detailed section; documentation-only change |
| 2026-09-09 | Manual whole-system testing playbook | Passed structural review | Added an executable manual flow covering safe isolated E2E setup, all 8 public/account destinations, 8 direct access-control attacks, a nine-stage cross-role golden lifecycle, all 24 primary admin workspaces, all 10 technician workspaces, all 10 client workspaces, every legacy redirect, responsive/accessibility, pagination, offline/concurrency, upload/export, reconciliation, defect capture, and sign-off. All 63 non-wildcard registered route patterns were found in the playbook and its Markdown fences are balanced; this is a test procedure, not new runtime evidence |
| 2026-09-09 | Shared-shell logout confirmation modal | Passed | The Vite production build transformed 2,518 modules. Focused isolated Chromium passed 1/1 and verified the centered `Log out?` dialog, sign-in consequence message, explicit Log out/Cancel actions, dismissal without navigation, and focus restoration to the sidebar trigger. No backend or schema change was required |
| 2026-09-09 | Smart-assignment consistency and concurrency hardening | Passed | The affected auto-dispatch, administrator setting, scheduling, warranty, and assignment suite passed 63/63, including minimum-score, manual-button overlap rejection, current-best-assignee retention, and scheduled-capacity availability regressions. Django system check passed and migration drift reported no changes. The Vite production build transformed 2,518 modules. Focused isolated Chromium passed 1/1 for the connected Dispatch Board workflow. `git diff --check` reported no whitespace errors (only existing line-ending notices). No model or migration changed |
| 2026-09-08 | Purchase Record value-source clarity and enforcement | Passed | Seven focused Sales Record tests passed, covering accepted-quotation source metadata and draft edit rejection, fresh authoritative value refresh at confirmation, signed-contract fallback/read-only enforcement, missing-value confirmation rejection, and the explanation requirement for manual fallback. Django system check passed and migration drift reported no changes. The Vite production build transformed 2,518 modules. Focused isolated Chromium passed 1/1 and proved the source-backed draft shows Recorded contract value, Accepted quotation source, read-only guidance, and no manual value input; its 1440x900 screenshot was visually inspected with no clipping or overlap. No model or migration changed |
| 2026-09-08 | System pagination default standardized to 10 | Passed | All actual frontend paginator constants and state defaults now start at 10; both Job History row selectors offer 10 first, and backend global, Activity Log, admin Job History, and technician Job History defaults agree. Fourteen focused Activity Log/Job History tests passed, including 11-record default-page regressions for both custom history endpoints and the DRF activity endpoint. Django system check passed, migration drift reported no changes, and the Vite production build transformed 2,518 modules. Export batches, lookup limits, and compact dashboard previews were explicitly excluded because they are not user-facing pagination defaults |
| 2026-09-08 | Create Account and CALABARZON address refinement | Passed with live-provider caveat | Fifteen focused registration/email-verification backend tests passed, including enumeration-safe resend and cooldown coverage. Django system check and migration drift passed with no changes detected. The final Vite production build transformed 2,518 modules. Focused isolated Chromium passed 2/2 with mocked PSGC hierarchy and registration responses, proving the five-province selector, dependent enable/reset behavior, Lucena-under-Quezon handling, exact `Barangay 1, City of Lucena, Quezon` payload, persistent labels, backend-aligned password guidance, password-mismatch feedback, field-associated API errors, and resend cooldown state. The final 1440x900 screenshot was visually inspected and showed readable controls without truncation. Direct live PSGC requests failed DNS resolution from this environment, so live provider availability/CORS remains deployment evidence rather than a completed local check |
| 2026-09-08 | Complete end-to-end Markdown ERD | Passed | Generated `docs/models/ERD_CURRENT.md` from the authoritative DBML with exactly 51 numbered entity sections, 676 typed attribute rows preserving database constraints, and 102 readable relationship lines. The relationship section uses the requested `(SOURCE) ---- cardinality ---- VERB ---- cardinality ---- (TARGET)` notation and exact field mappings. Repeat generation was deterministic at SHA-256 `97A323BF0F6E154CB286FA58BF8455635BE2D3AA5285A8DC40C5270EBB602F3E`; generator byte-compilation and format/count validation passed |
| 2026-09-08 | Plain readable ERD relationship map | Passed | Generated `docs/models/ERD_CURRENT_CONNECTIONS.md` directly from the authoritative DBML. It contains all 102 relationships, numbered consecutively 1-102 and grouped into the six system areas. Every entry uses `(SOURCE) ---- cardinality ---- VERB ---- cardinality ---- (TARGET)` and includes its exact source-to-target field mapping. Repeat generation was deterministic at SHA-256 `026DC74455A22451E0F4A7C2633046A393DBDE3936214C48ED68B0DE9F60E2CD`; both generator scripts byte-compiled successfully |
| 2026-09-08 | Readable exhaustive Chen connections | Passed structurally; rendered inspection pending | Revised both maintained one-page ERD files so all 102 relationships have a stable `R001`-`R102` identifier repeated on their two direct legs and matching diamond. Every enlarged diamond also prints the complete source and target entity, field, and cardinality, making each relationship self-identifying even where routes cross. XML validation found exactly 102 diamonds, 204 direct legs, and 102 unique identifiers each occurring on the expected three objects, with no missing edge endpoints. Both generated copies have the same SHA-256 hash, `5093B068F4CBB024772615720C56E0BA5875892CAFAC6E2A37EC626578FA95BD`; generator byte-compilation and Django system check passed |
| 2026-09-08 | Current ERD alias copy | Superseded by readable-connection revision | Initially created `docs/models/ERD_CURRENT.drawio` as an exact copy at hash `B72D3045D19CBC08893B00A546576B8BFCDAB9BAB823B0623372A6271688C314`. Both maintained copies were subsequently regenerated with stable relationship identifiers and endpoint descriptions; use the newer validation above |
| 2026-09-08 | Final Chen reference-style acceptance | Passed structurally; rendered inspection pending | Revalidated the one-page DBML-derived diagram after the final routing revision. All 204 relationship legs have valid cardinality-and-field labels, with 0 invalid labels and 0 unlabeled relationships. Style counts exactly match the required reference language: 51 green entity rectangles, 676 yellow attribute ovals, and 102 orange relationship diamonds. Generator byte-compilation, Django system check, and diff check passed. No application or schema behavior changed; diagrams.net remains unavailable locally for the final human-rendered inspection |
| 2026-09-08 | Authenticated client solar-calculator regression | Passed | The Vite production build transformed 2,517 modules. Focused isolated Chromium passed 1/1, proving New calculation stays on `/client/solar-estimates`, retains the authenticated client sidebar, renders the configured calculator, and exposes Save estimate. The generated 1440x900 screenshot was visually inspected; no overflow or layout collision was observed. No backend or schema change was required |
| 2026-09-07 | Directly connected, graph-arranged classic Chen poster | Passed structurally; final export inspection pending | Implemented the complete attached A–O layout contract using `AFN_ERD.dbml` as the source. Relationship-degree analysis found `users_user` (50 endpoints) and `services_serviceticket` (26) as the hubs; their modules are centered, related modules surround them, low-connectivity modules occupy outer edges, and entities within each module are degree-ranked rather than table-ordered. The one 16,360 × 11,648 landscape draw.io canvas has 1,730 cells: 51 small green canonical entities, all 676 yellow oval attributes, 51 underlined primary keys, 102 orange relationship diamonds, and 204 direct black relationship legs with cardinalities. All 204 legs originate at canonical entities, terminate at diamonds, contain explicit orthogonal waypoints, and enable bridge arcs. Validation found 0 duplicate IDs, missing endpoints, incorrectly attached relationship edges, overlapping semantic vertices, connector passages through unrelated shapes, or out-of-page vertices. An intermediate single-lane route measured 8,903 proper crossings and was rejected; placing diamonds between their source/target module gutters reduced the final count to 251 (97.2%). SHA-256 deterministic regeneration (`B72D3045D19CBC08893B00A546576B8BFCDAB9BAB823B0623372A6271688C314`), generator byte-compilation, and Django system check passed. diagrams.net is unavailable locally, so final human inspection of rendered text/labels remains open |
| 2026-09-06 | Temporary untangled Chen relationship-index regeneration | Superseded by direct-connection revision | Rebuilt the editable ERD directly from `AFN_ERD.dbml` as one 11,560 × 7,588 draw.io page. XML validation found 1,934 cells: 51 canonical entities, all 676 attributes, 102 relationship diamonds, 204 compact relationship endpoint references, and 51 underlined primary keys. It found 0 duplicate IDs, missing edge endpoints, or overlapping semantic vertices. All 880 connector segments were horizontal and the geometric check found 0 proper line intersections. Generation was SHA-256 deterministic (`68DE8B4AA6B7A4ABEC43DEF4405440A734A451FFF7900F5C4617E64F65F6E8C7`); generator byte-compilation and Django system check passed. The user then clarified that relationships must attach directly to the canonical entity boxes, so the 2026-09-07 layout supersedes this temporary index design |
| 2026-09-06 | Dependency remediation and complete regression | Passed | Upgraded the installed and pinned Django REST Framework from 3.17.1 to 3.17.2 and refreshed the frontend lockfile to `browserslist==4.28.9` and `postcss-selector-parser==6.1.4` with their compatible browser-data dependencies. Fresh `pip-audit`, root `npm audit --audit-level=low`, and frontend `npm audit --audit-level=low` each reported 0 known vulnerabilities. A clean frontend `npm ci` succeeded after one transient Windows `EBUSY` retry. `npm run quality` then passed Django system check, `pip check`, migration drift, all 394 backend tests with 3 expected PostgreSQL-only skips, the 2,517-module production build, and 15/15 isolated browser smoke tests. The separately invoked full Playwright suite passed 197/197 in 17.5 minutes, covering public, superadmin/admin, technician, and client functional flows plus 108 desktop/mobile route-quality checks. No development or production migration was run |
| 2026-09-06 | Whole-system functional, UI, and dependency security audit | Functional/UI passed; dependency remediation required | `npm run quality` passed Django system and migration-drift checks, Python dependency integrity, all 394 discovered backend tests with 3 expected PostgreSQL-only skips, the 2,517-module frontend production build, and all 15 isolated browser smoke scenarios. The separately invoked complete Playwright suite passed all 197 scenarios in 16.7 minutes, including 108 desktop/mobile checks across all 54 canonical routes for runtime errors, HTTP 5xx, page overflow, clipped actions, visible button collisions, and unlabeled main actions. Representative admin, technician, client, and long-form mobile screenshots were visually inspected; no new mechanical spacing or action-placement defect was found, while Documents and field checklist forms remain intentionally dense. Fresh external advisory queries found 2 CVEs in `djangorestframework==3.17.1` (fixed in 3.17.2), 0 root npm findings, and 2 frontend build-tool findings: high-severity `browserslist==4.28.2` (fixed in 4.28.7) and low-severity `postcss-selector-parser==6.1.2` (fixed in 6.1.3). `npm audit --omit=dev` reported 0 vulnerabilities, confirming the npm findings are confined to development/build dependencies. A focused 144-test users/messages/inventory/progress authorization and integrity suite also passed. The first advisory attempts failed because sandbox networking was unavailable; approved network-enabled reruns produced the recorded results. No application dependency was changed during this audit |
| 2026-09-04 | Exhaustive Chen ERD restoration and spacing check | Passed structurally; final export inspection pending | At the user's direction, regenerated one editable draw.io page containing all 51 application entities, all 676 local attributes, all 102 in-scope relationship diamonds, underlined primary keys, and cardinality labels. XML parsing found 1,728 cells, zero duplicate IDs, zero missing endpoints, and zero overlapping entity/attribute/relationship vertices. Wide domain lanes and draw.io line bridges reduce ambiguity where the complete cross-domain graph intersects. Generator byte-compilation, Django system check, deterministic generation, and `git diff --check` passed. diagrams.net is not installed locally, so the exhaustive canvas still requires visual inspection/export at paper scale |
| 2026-09-04 | Clean conceptual Chen ERD geometry and contract check | Superseded by exhaustive user-required figure | The temporary 22-entity/88-attribute presentation passed zero-crossing geometry checks, but the user subsequently required every schema entity and attribute in the final Chen figure |
| 2026-09-04 | Full single-page Chen ERD reconciliation | Superseded by later exhaustive layout revision | The first exhaustive canvas established complete model coverage; the later exhaustive revision widened its domain lanes and added visible connector bridges |
| 2026-09-04 | Full physical-schema model reconciliation | Passed | Rebuilt `AFN_ERD.dbml` from live Django metadata through the retained generator; all 51 application table names and local-column sets matched exactly with zero differing tables, all 102 relationships whose targets are inside the application boundary were emitted, and the sole external `users_changelog.content_type_id` framework relationship was explicitly annotated. The official DBML-to-PostgreSQL compiler passed, repeat generation produced the same SHA-256 hash, generator byte-compilation passed, Django system check passed, and `git diff --check` passed. One first direct generator run failed because its script path omitted the backend import root and was corrected; one relationship-count command was malformed by Windows argument quoting and its corrected rerun passed |
| 2026-09-04 | Physical schema DBML compiler validation | Passed but superseded by full reconciliation | Corrected Project and column-note metadata quoting and proved the then-current source parsed, but this syntax-only result did not establish model-column accuracy; the later generator-backed validation supersedes it |
| 2026-09-04 | Professor-style editable draw.io diagram check | Passed structurally; visual export pending | XML parsing passed for the Context Diagram (41 cells), DFD (77 cells), and Chen ERD (181 cells), with zero duplicate IDs and zero missing edge endpoints; stale Firebase, SMS, Notification Template, and Service History terms were absent; `git diff --check` passed. The earlier Mermaid final sources were removed. diagrams.net is not installed locally, so final canvas inspection/export remains pending |
| 2026-09-04 | Unified Mermaid capstone diagram source check | Superseded by editable draw.io sources later the same day | The one-diagram-per-type structure remains authoritative, but Mermaid auto-layout did not match the professors' approved manually positioned format |
| 2026-09-03 | Initial split capstone diagram source reconciliation | Superseded on 2026-09-04 | The earlier 51/51 result checked table names only and did not establish column accuracy; the final generator-backed reconciliation now supersedes it, and the split Mermaid presentation was also replaced by unified draw.io figures |
| 2026-09-03 | Admin Service Calendar operations and overlap regression | Passed | Django system check and migration drift passed; all 60 affected scheduling, assignment, auto-dispatch, and focused calendar API tests passed, including new exact-time lead/crew/automatic-assignment overlap rejection; the production build transformed 2,517 modules; the isolated calendar browser journey passed 1/1 with operational summary, technician filtering, details-first modal, client/location/team content, and reschedule action; the initial combined focused backend command named one stale test class, while both new tests passed, and the corrected complete affected run superseded it; the generated desktop calendar screenshot was visually inspected; no schema migration was introduced |
| 2026-09-03 | Local Our Work schema migration | Passed with explicit user approval | Targeted `users.0038_adminsettings_landing_page_projects` applied successfully to the verified workspace database `backend/db.sqlite3`; Django migration-recorder query returned `True`, Django system check passed, and production/Aiven was not touched |
| 2026-09-03 | Landing-page Our Work publication contract | Passed | Django system check and migration-drift check passed; all 11 landing API tests passed, including public filtering of incomplete/unpermitted projects, safe field projection, and rejection of incomplete published entries; the production build transformed 2,516 modules; the isolated admin Playwright journey passed 1/1 from editor entry through publish and anonymous public rendering, with a 390x844/full-page screenshot captured; one first test-only attempt cleared localStorage but not sessionStorage and was corrected before the passing rerun |
| 2026-09-03 | Public landing-page interaction and responsive regression | Passed | Vite production build transformed 2,516 modules; focused desktop and 390x844 mobile interaction journeys passed 2/2 against the isolated E2E database, verifying the rendered primary-button style, in-page Services navigation, About-to-Home cross-page section navigation, mobile menu, calculator destination, and mobile account action; the original calculator-rich landing journey passed 1/1; route-quality checks passed desktop/mobile 2/2 with no overflow, runtime, HTTP, clipping, or action-label failures; the generated full-page mobile screenshot was visually inspected; `playwright test --list` reports 196 maintained tests at this earlier checkpoint and was later superseded by the 197-test inventory after the Our Work scenario was added; and the scoped diff check found no whitespace errors. The first combined grep command did not run because the Windows shell interpreted its alternation as a pipe; the corrected character-class selection produced the passing run |
| 2026-09-03 | Progressive-disclosure Job Progress modal regression | Passed after eliminating one React warning | The production build transformed 2,516 modules; exact desktop/mobile modal journeys passed 2/2 with live ticket reads, service-disclosure interaction, operational essentials, fixed actions, Escape/focus restoration, and no horizontal overflow; the regenerated 390x844 screenshot was visually inspected; `playwright test --list` reports 194 maintained tests in 9 files; and the scoped diff check found no whitespace errors. The first 2/2 browser pass exposed a React warning because this installed React version forwarded `defaultOpen` on `details`; disclosure state was changed to explicit `open`/`onToggle`, then the build and both journeys passed again without that warning |
| 2026-09-03 | Expanded active-job detail content and formatting | Passed | Vite production build transformed 2,516 modules and the scoped diff check found no whitespace errors; the exact desktop/mobile dashboard modal run passed 2/2 with live detail reads and assertions for Client details, canonical client ID, phone/email, Service request, Assignment, Visit details, Equipment plan, and Status timeline; the final mobile screenshot run passed 1/1 and its 390x844 image was inspected for hierarchy, wrapping, fixed actions, and horizontal fit |
| 2026-09-03 | Active technician job-details modal regression | Passed after one test-only locator correction | Vite production build transformed 2,516 modules; the connected desktop dashboard journey passed 1/1 with a live ticket-detail GET, complete section/action assertions, Escape close, and trigger-focus restoration; the final dedicated 390x844 mobile modal journey passed 1/1 with no page-level horizontal overflow and reachable persistent actions; and the captured mobile view was visually inspected. An accidentally broad `3.1` grep also passed 13 unrelated `3.10`-`3.19` admin regressions while the new mobile check alone failed because `Close` non-exactly matched both close controls; the exact locator rerun passed |
| 2026-09-03 | Type-specific inspection/service progress regression | Passed | Django system check passed; all 4 focused inspection/service/dashboard/client API contracts passed; the complete dashboard-role module passed 20/20; migration drift reported no changes; the Vite production build transformed 2,515 modules; and the connected admin dashboard journey passed 1/1 while asserting the Inspection visit badge, Inspector en route label, and accessible 35% checkpoint |
| 2026-09-03 | Canonical technician workflow-progress regression | Passed after correcting two test-only locators | Django system check and migration drift passed; all 3 focused dashboard/API progress contracts passed after the final prefetch-preserving helper adjustment; the complete 19-test dashboard-role module passed; the Vite production build transformed 2,515 modules; the connected admin dashboard Playwright journey passed 1/1 with a visible En route label and accessible 40% checkpoint; and the scoped diff check found no whitespace errors. The first backend assertions looked at the old top-level compatibility shape instead of `operations.active_technician_jobs`, and the first browser assertion expected globally unique explanatory copy rendered once per job; both tests were corrected without changing the product contract and reruns passed |
| 2026-09-02 | Local two-party return schema migration | Passed | With explicit user approval, targeted `inventory.0007_equipmentreturnrequest_equipmentreturnrequestitem` applied successfully to `backend/db.sqlite3`; `showmigrations inventory` reports all migrations `0001` through `0007` applied and the post-migration Django system check found no issues. Production/Aiven was not touched |
| 2026-09-02 | Two-party equipment-return workflow regression | Passed; pre-migration-state evidence superseded | All 24 automatic inventory workflow tests passed, including 4 focused submit/verify/reject/authorization checks; Django check and migration drift passed; fresh tests applied `inventory.0007`; the production frontend build transformed 2,515 modules. Live isolated-database technician submission passed 1/1 with POST 201 and no immediate stock release; live admin verification passed 1/1 with POST 200 and the receiving decision; admin/technician Job History desktop/mobile quality passed 4/4. The then-current local `showmigrations` result reported `inventory.0007` unapplied; that state was superseded by the authorized migration recorded above. The scoped diff check passed with line-ending warnings only. One attempted combined `--grep` invocation was split after the nested Windows command shell treated its alternation as a pipe; both separate final journeys passed |
| 2026-09-02 | Ticket-linked unused-equipment return regression | Superseded by the two-party workflow above | The earlier one-step admin-return evidence passed 3/3 backend checks, a 2,515-module build, live isolated API/database E2E 1/1, and desktop/mobile Job History quality 2/2, but its direct-stock-release contract was intentionally replaced later the same day by technician submission plus independent receiving verification |
| 2026-09-02 | Dispatch equipment-reservation editor regression | Passed | Vite production build transformed 2,514 modules; the final focused Dispatch journey passed 1/1 with deterministic inventory and verified add-item selection, stock-aware shortage feedback, invalid-quantity submission guarding, recovery to an enabled update action, and clear selection; Dispatch desktop/mobile quality passed 2/2; the fresh selected-ticket equipment-plan screenshot was visually inspected |
| 2026-09-02 | Dispatch Board guided-assignment UI regression | Passed | Final Vite production build transformed 2,514 modules; the focused connected Dispatch journey passed 1/1 and verified technician controls are disabled before ticket selection, an existing assignment can enter review with an enabled update action, and clearing returns the UI to its guarded state; desktop/mobile page quality passed 2/2; the fresh desktop screenshot was visually inspected |
| 2026-09-02 | Service Tickets versus Dispatch count-label regression | Passed | Final Vite production build transformed 2,514 modules; focused Service Tickets and Dispatch Board journeys passed 2/2 with the new scope labels and `X of Y active tickets` relationship; fresh screenshots for both pages were visually inspected |
| 2026-09-02 | Operations-attention outer-border correction | Passed | Vite production build transformed 2,514 modules after restoring the unrelated stat-card hover treatment and replacing only the attention strip's conditional amber/emerald outer border with a neutral slate border. The preceding build for the misunderstood stat-card change passed but that visual change was superseded before handoff |
| 2026-09-02 | Simple, action-oriented dashboard and ticket-queue visual regression | Passed after restoring one explicit accessible name | Final Vite production build transformed 2,514 modules; the final focused connected dashboard journey passed 1/1; final dashboard desktop/mobile quality passed 2/2; the preceding combined dashboard/Service Tickets desktop/mobile quality run passed 4/4; and fresh dashboard and Service Tickets desktop screenshots were visually inspected. The first focused connected run correctly exposed that separating the queue label and count changed the button's tested accessible name; an explicit count-bearing label was restored and the rerun passed 2/2 |
| 2026-09-02 | Dashboard destination and URL-backed ticket-view regression | Passed | Vite production build transformed 2,514 modules; focused dashboard and Service Tickets journeys passed 2/2, proving Pending Approvals focuses `#pending-approvals`, Active Tickets opens the active ticket view, and direct unassigned queue state is visible and selected; both affected pages passed all 4 desktop/mobile layout/runtime/HTTP quality checks |
| 2026-09-02 | Actionable admin/superadmin operations-dashboard regression | Passed after correcting one initialization-order mistake | Dashboard and capability backend contracts passed 25/25, including the authoritative unassigned-scheduled-visit count; the final Vite build transformed 2,514 modules; the connected dashboard journey passed 1/1 with deterministic request details, required rejection evidence, focus restoration, manual refresh, and KPI navigation; responsive/quality coverage passed 3/3 across desktop and mobile; browser smoke passed 13/13; and the complete admin workspace passed 27/27. The first focused browser attempt exposed a frontend fallback referencing the filtered schedule before initialization; that one-line error was corrected and all listed browser evidence is from subsequent successful runs. The fresh desktop dashboard screenshot was visually inspected |
| 2026-09-02 | Shared notification-state and request-deduplication regression | Passed | Vite production build transformed 2,514 modules; the focused technician dashboard passed 1/1 and verified both notification endpoints were requested no more than twice under React Strict Mode while the explicit GPS action still succeeded; the deterministic client unread-badge journey passed 1/1; browser smoke passed 13/13; the role dashboard matrix passed 12/12; and all admin, technician, and client Notifications desktop/mobile routes passed 6/6 with no runtime, layout, or HTTP failures |
| 2026-09-02 | First-screen dashboard privacy/accessibility/E2E regression | Passed | Dashboard and capability backend contracts passed 25/25 before the frontend-only correction; the production build transformed 2,513 modules; strengthened admin, technician, and client dashboard journeys passed 3/3; the technician explicit-sharing journey passed again 1/1 and proved zero geolocation calls before consent, then exactly one initial-position call, one watcher start, and a successful location POST after the explicit action; the final role functional/mobile/desktop quality matrix passed 12/12 with no overflow, runtime, or HTTP failures; fresh technician/client screenshots were visually inspected. An initial shell command containing an unescaped grep alternation failed before running tests and was superseded by the quoted dashboard runs |
| 2026-09-02 | Role-scoped Notifications sidebar and unread-badge regression | Passed after correcting a zero-data test fixture | Vite production build transformed 2,513 modules; the focused client Notifications journey passed 1/1 with a deterministic unread-count response and verified the direct sidebar route plus visible count; admin, technician, and client Notifications routes then passed all 6 desktop/mobile page-quality checks with no layout/runtime/HTTP failures. The initial focused attempt correctly found that an empty isolated notification fixture cannot render a badge and was superseded by the deterministic rerun |
| 2026-09-02 | Final complete browser regression after assertion stabilization | Passed, 193/193 | `npm run test:e2e` completed uninterrupted in 15.8 minutes on the isolated E2E database; both formerly failing scenarios passed in their original full-suite order, no HTTP 500 responses occurred, and the managed backend/frontend processes exited normally |
| 2026-09-02 | Activity Logs and technician My Jobs assertion regression | Passed, 2/2 after diagnostic failure | The initial complete browser run passed 191/193 and exposed two test-only assumptions: the Activity Logs check inspected whichever event sorted first, while My Jobs searched for obsolete generic text. The corrected focused run selected a service event explicitly and asserted the current `Active Jobs` heading and `Details` action; both passed |
| 2026-09-02 | Unified smoke quality gate | Passed | `npm run quality` passed Django system check, Python dependency integrity, migration drift, 378/378 runnable backend tests with 3 expected PostgreSQL-only skips (381 discovered), the Vite production build with 2,513 transformed modules, and 13/13 browser smoke tests |
| 2026-09-02 | Pass-on audit synchronization | Passed | Confirmed `docs/README.md` still routes new readers to this canonical handoff; added a concise latest checkpoint grounded only in existing dated evidence; the handoff secret-pattern scan returned no matches and the scoped Markdown diff check reported no whitespace errors |
| 2026-09-02 | Job History detail-dialog visual regression | Passed after correcting one test-only strict locator and one admin footer sizing issue | Admin and technician focused browser journeys passed 2/2 after selecting the new text action exactly; the admin journey passed again 1/1 after its flex-based fixed-footer correction. Fresh admin and technician screenshots were visually inspected for hierarchy, clipping, and conventional action placement. The final Vite production build transformed 2,513 modules. No backend, data contract, permission, or workflow behavior changed |
| 2026-09-01 | Completed Job History contract/UI regression | Passed after removing a stale-prefetch optimization and correcting one strict E2E locator | New focused backend contract passed 3/3 and affected capability regression passed 6/6; the broad suite initially exposed two stale relation-cache failures introduced by an attempted query optimization, the optimization was removed, both failing workflows plus history passed 5/5, and final full discovery passed all 378 runnable tests with 3 expected PostgreSQL-only skips (381 discovered). Django check and migration drift passed; final Vite build transformed 2,513 modules; final admin/technician browser journeys passed 2/2 with complete-record dialogs, Escape close, all-filtered CSV download, server-backed technician search/empty state, and no endpoint failures; the final technician route-entry rerun passed 1/1 and its corrected unclipped-header screenshot was visually inspected |
| 2026-09-01 | Technician-location audit/document synchronization | Passed | Updated the whole-system plan and client-requirement mapping to reflect explicit sharing, configurable region/retention, and daily deletion; labeled the obsolete 71-test E2E report as a historical snapshot rather than current release evidence; consistency scan found no remaining obsolete partial-tracking statement and `git diff --check` passed with line-ending warnings only |
| 2026-09-01 | Technician location configuration/privacy/retention regression | Passed after correcting one invalid test assumption about the base admin capability map | Final focused policy, capability, retention, and scheduler suites passed 13/13; Django check and migration drift passed; final production build transformed 2,513 modules; combined admin tracking and technician explicit-sharing browser checks passed 3/3, the final technician no-auto-collection regression passed again 1/1 with zero GPS-watch/route/navigation-start calls before consent, and affected desktop/mobile page-quality checks passed 4/4; final policy screenshot was visually inspected |
| 2026-09-01 | Technician Tracking connected-operations regression | Passed after correcting diagnostic test/fixture gaps | Focused backend capability suite passed 8/8, including canonical active-ticket inclusion and bounded technician-history filters; final Django check and migration drift passed; production build transformed 2,513 modules; final desktop/mobile tracking journeys passed 2/2 with real GPS submission, filters, selected-technician/job detail, recent trail, direct-line route labeling, last-known-data preservation on refresh failure, and no mobile horizontal overflow; separate desktop/mobile page-quality checks passed 2/2 and screenshots were visually inspected; final diff check had no whitespace errors |
| 2026-09-01 | Activity Log audit-workspace regression | Passed after correcting one test-only exact-text locator | Focused backend suite passed 9/9, including complete-queryset summaries, filtered streaming CSV, spreadsheet-formula neutralization, and client denial; Django check and migration drift passed; production build transformed 2,513 modules; desktop E2E verified evidence expansion, refresh, real CSV download, and filtered list/summary requests; mobile expanded evidence had no horizontal overflow; Activity Logs desktop/mobile page quality passed 2/2; fresh desktop and expanded-mobile screenshots were visually inspected |
| 2026-09-01 | Walk-in request modal workflow and responsive regression | Passed after correcting one test-only accessible-name locator | Production build transformed 2,513 modules; the focused desktop journey protected a dirty draft, performed a real save-for-review POST with HTTP 201, closed the dialog, and exposed success feedback; the dedicated 390x844 dialog check passed with no horizontal overflow and an accessible sticky action area; Service Tickets desktop/mobile page-quality checks passed 2/2; fresh desktop/mobile screenshots were visually inspected |
| 2026-09-01 | Solid profile-header visual check | Passed | Replacing the gradient with the system's solid brand blue retained a successful 2,512-module production build; the focused client profile browser check passed and its fresh desktop screenshot was visually inspected for contrast, spacing, and composition |
| 2026-09-01 | Profile visual-hierarchy regression | Passed after correcting intermediate JSX nesting | The first production build diagnostically caught mismatched closing tags while splitting the old card; tags were corrected and the final Vite build transformed 2,512 modules. All 4 role-profile behavior checks passed, including a real client PATCH; final desktop/mobile page-quality checks passed 6/6 with no overflow, clipping, collisions, unnamed actions, runtime/console errors, or HTTP 500s; a fresh client desktop screenshot check passed and was visually inspected after moving identity text off the banner and removing the redundant local description |
| 2026-09-01 | Role profile workflow and responsive regression | Passed after one test-locator correction | Final Vite build transformed 2,511 modules; admin and technician cancel restoration passed; the client journey rejected a non-image locally, restored a cancelled draft, performed a successful profile PATCH, exposed saved company/landline data, and validated mismatched passwords without a request. The first combined run passed 3/4 and exposed only a non-exact `New password` locator; the corrected client journey passed 1/1. All six admin/technician/client desktop/mobile page-quality checks passed with no overflow, clipping, collisions, unnamed actions, runtime errors, console errors, or HTTP 500 responses |
| 2026-09-01 | Coverage Heatmap compact-toolbar regression | Passed, 3 browser checks | Production build transformed 2,509 modules; the focused functional check verified the map, service-density layer toggle state, and filter disclosure; desktop/mobile page-quality checks passed; the generated desktop screenshot was visually inspected with no redundant layer card, overlap, or clipping |
| 2026-09-01 | Progressive filter usability regression | Passed, 18 browser checks | Inventory filter disclosure/chip removal and the receive-stock journey passed; desktop/mobile page-quality checks passed for Inventory, Reports, Operations Report, After-Sales Cases, Activity Logs, Coverage Heatmap, Job History, and client Service History with no overflow, clipped/colliding/unlabelled controls, runtime errors, console errors, or HTTP 500 responses; final Vite build transformed 2,509 modules |
| 2026-09-01 | Receive-stock Save Movement regression | Passed | Corrected all desktop/mobile/detail Receive entry points to submit the backend `purchase` transaction type; the isolated browser journey received 5 units with HTTP 201, refreshed inventory, and verified the movement note and quantity in history; final Vite build transformed 2,508 modules |
| 2026-09-01 | Inventory operations workspace regression | Passed | Focused inventory backend suite passed 20 tests; Django check and migration drift passed; Vite transformed 2,508 modules; custom item/opening-history/reservation-panel/stock-dialog browser journey and inventory desktop/mobile page-quality matrix passed 3/3; final diff check had no whitespace errors (line-ending warnings only) |
| 2026-08-31 | Canonical workspace-title responsive regression | Passed, 36 checks | Every affected admin, client, and technician route passed desktop/mobile page-quality validation with zero overflow, button collision/clipping, unlabelled controls, runtime errors, console errors, or HTTP 500 responses; includes the reported Activity Logs duplication |
| 2026-08-31 | Canonical workspace-title functional regressions | Passed, 3 checks | Admin Sales Records, client Purchase Records, and client Notifications retained working canonical heading assertions after their repeated content titles were removed |
| 2026-08-31 | Restrained UI-consistency production build | Passed | Final Vite 8.2.2 production build transformed 2,508 modules after removing redundant shell controls/context and correcting workspace/print-preview semantics |
| 2026-08-31 | UI-consistency responsive browser regressions | Passed, 18 checks | First run passed 12 desktop/mobile page-quality checks across Settings, Landing Page Editor, admin/client/technician profiles, and Client Support; final run passed 6 Notifications/Documents functional, responsive, collision, clipping, labeling, runtime, and desktop/mobile checks on the isolated E2E database |
| 2026-08-31 | Whole-system full-gate regression discovery | Failed diagnostically, then repaired | Backend discovery passed 369 tests with 3 expected PostgreSQL-only skips and the 2,508-module build passed; the browser run exposed HTTP 500 from a stale serializer-package lazy import in the non-empty Operations Report flow. The import was corrected and the report capability test was strengthened with real report data; its 6 focused tests passed |
| 2026-08-31 | Post-repair complete browser run | Passed 186/188; two actionable failures repaired | Operations Report returned HTTP 200 and 25/26 report checks passed. The remaining failures were a false-negative notifications assertion caused by Playwright strict-mode ambiguity and a genuinely clipped technician Schedule `Open` action on mobile; the locator was role-scoped and the action was made non-shrinking |
| 2026-08-31 | Final-code focused browser coverage | Passed | Notifications plus technician Schedule mobile passed 2/2. A later complete-suite attempt encountered `ERR_NETWORK_IO_SUSPENDED` and a browser-launch timeout on Windows; the affected document-draft and Analytics scenarios then passed 2/2 after the environment recovered. Combined with the 186-test run, every maintained browser scenario has passing final-code evidence |
| 2026-08-31 | Final whole-system static/build checks | Passed | Frontend production build transformed 2,508 modules; Django system check passed; migration drift reported no changes; `git diff --check` reported no whitespace errors (line-ending warnings only) |
| 2026-08-31 | Post-cleanup unified smoke release gate | Passed | `npm run quality` passed Django system check, Python dependency integrity, migration drift, complete 372-test discovery (369 passed and 3 expected PostgreSQL-only skips), the 2,508-module Vite production build, and all 13 public/authentication Chromium smoke tests on the isolated E2E database |
| 2026-08-31 | Services serializer-package structural refactor | Passed | All 30 serializer class ASTs exactly matched the verified pre-cleanup source; public package import exposed all 30 classes; services suite passed 193 tests and full backend discovery passed 369 with 3 expected PostgreSQL-only skips; compile, Django check, migration drift, and `git diff --check` passed |
| 2026-08-31 | Services test-package structural refactor | Passed | Services discovery passed 193 tests with 3 expected PostgreSQL-only skips; full backend discovery found 372 tests and passed 369 with the same 3 skips; Django check, migration drift, and `git diff --check` passed |
| 2026-08-29 | Django system check | Passed | No reported issues |
| 2026-08-29 | Migration drift (`makemigrations --check --dry-run`) | Passed | No unrepresented model drift at that checkpoint |
| 2026-08-29 | Full Django test discovery | Passed, 321 tests | Before the later analytics regression test was added |
| 2026-08-29 | Reporting capability regression module | Passed, 6 tests | Includes scheduled-date Analytics regression |
| 2026-08-29 | Production deployment check | Passed | Optional HSTS preload advisory remained |
| 2026-08-29 | Python dependency integrity | Passed | No broken installed requirements |
| 2026-08-29 | Root npm audit | Passed | 0 known vulnerabilities |
| 2026-08-29 | Frontend npm audit | Passed | 0 known vulnerabilities |
| 2026-08-29 | Frontend Vite production build | Passed | Vite 8.2.2 |
| 2026-08-29 | Functional Playwright groups | Passed, 75 tests | Public/auth/admin/technician/client/main/side/responsive flows |
| 2026-08-29 | Route-complete page-quality matrix | Passed, 104 tests | 52 destinations x desktop/mobile on isolated ports 8011/5181 |
| 2026-08-29 | `git diff --check` | Passed | Line-ending conversion warnings only; no whitespace errors |
| 2026-08-29 | Local development DB migration | Passed | Applied `inventory.0005` and `services.0053`-`0055` to `backend/db.sqlite3` |
| 2026-08-29 | Post-migration dashboard API smoke | Passed | Through `http://127.0.0.1:5174`: login 200, `/api/dashboard/stats/` 200, `/api/admin/analytics/` 200 |
| 2026-08-29 | Frontend Vite production build | Passed | Rerun after Admin User Management action-column layout fix |
| 2026-08-29 | Focused User Management page-quality check | Passed with teardown caveat | Desktop and mobile `admin / user management` checks passed on isolated ports 8011/5181; runner was manually interrupted after pass output while waiting in teardown |
| 2026-08-29 | Frontend Vite production build | Passed | Rerun after notification indicator color update |
| 2026-08-29 | Focused after-sales backend regression suites | Passed, 15 tests | Case lifecycle/events, capability access, and maintenance workflows |
| 2026-08-29 | Django check and migration drift | Passed | New `services.0056` is represented; no additional model drift |
| 2026-08-29 | Frontend Vite production build | Passed | Rerun after after-sales ownership, pagination, resolution, and activity UI changes |
| 2026-08-29 | Focused after-sales browser journey | Exercised with teardown caveat | Real E2E case creation and resolution PATCH succeeded and activity UI rendered; runner was manually interrupted during the recurring local teardown delay |
| 2026-08-29 | Local after-sales schema/API verification | Passed | Applied `services.0056` to `backend/db.sqlite3`; migration plan marks it applied, Django check passed, and authenticated follow-up case list returned HTTP 200 with paginated data |
| 2026-08-30 | Focused inventory and landing-promotion backend suites | Passed, 37 tests | Catalog creation, opening ledger, category/SKU identity, retirement/service-template safeguards, stock workflows, and public/admin promotion behavior |
| 2026-08-30 | Inventory schema/check/build validation | Passed | `inventory.0006` applied to local SQLite; migration drift and Django check passed; frontend Vite production build passed |
| 2026-08-30 | Custom inventory product browser journey | Passed, 1 test | On isolated ports 8011/5181: created category and consumable with custom `coil` unit, then verified opening-balance history; also exposed and verified the action-column overlap fix |
| 2026-08-30 | Inventory migration preflight regression | Passed, 9 tests | Fresh test database applied the finalized two-pass category/SKU normalization migration before catalog API tests; migration drift and Django check passed afterward |
| 2026-08-30 | Coverage Heatmap tile-provider regression | Passed | Frontend production build passed; focused Coverage Heatmap browser test passed on isolated ports 8011/5181; no CARTO URL or `clean2d` reference remains in frontend source |
| 2026-08-30 | Client support assistant validation | Passed | Frontend production build passed; focused authenticated client assistant browser journey passed on isolated ports 8011/5181 |
| 2026-08-30 | Purchase-record terminology regression | Passed | Frontend production build passed; focused authenticated assistant journey verified the no-payment-processing explanation and purchase-record action on isolated ports 8011/5181 |
| 2026-08-30 | Sales Record backend lifecycle and document-capability regression | Passed, 11 tests | Fresh test DB applied `services.0057`; 5 Sales Record lifecycle/security tests and 6 document-capability tests passed; Django check and migration drift passed |
| 2026-08-30 | Sales/Purchase Records frontend validation | Passed | Frontend production build passed; authenticated admin Sales Records and client Purchase Records browser checks passed on isolated ports 8011/5181 |
| 2026-08-30 | Local Sales Record schema migration | Passed | Applied `services.0057` to `backend/db.sqlite3`; migration plan marks it applied and Django system check passed |
| 2026-08-30 | Visual identity formatter regression | Passed | Direct Node assertions verified `REQ`, `TKT`, `CSC`, `ASC`, `QUO`, `ITX`, `RSV`, `DOC`, and `EST`, including six-digit ledger widths and raw/legacy ticket normalization |
| 2026-08-30 | Frontend production build after identity normalization | Passed | Vite 8.2.2 built 2,508 modules successfully; the first npm invocation was blocked only by local PowerShell script policy and the equivalent `npm.cmd` build passed |
| 2026-08-30 | Record identity API regression | Passed, 7 focused tests | Six document-capability/quotation tests and the correctly selected inventory append-only identity test passed; the first inventory selector named the wrong test class and was rerun correctly |
| 2026-08-30 | Final identity integration checks | Passed | Django check and migration drift passed with no schema changes; final Vite build passed with 2,508 modules |
| 2026-08-30 | Automation focused regressions | Passed, 7 tests | SLA deduplication, urgent maintenance catch-up, both formerly failing warranty expectations, command grouping, and failure propagation |
| 2026-08-30 | Full Django suite after automation hardening | Passed, 340 tests | Correct backend discovery root; isolated test database created and destroyed; expected negative-test warning logs only |
| 2026-08-30 | Automation deployment validation | Passed | Django check and migration drift passed; three PowerShell scripts parsed successfully; no obsolete `D:\Caps - Copy` automation path remains; `git diff --check` passed with line-ending warnings only |
| 2026-08-30 | Admin Analytics at-a-glance validation | Passed | Final Vite build transformed 2,508 modules; focused authenticated Analytics Playwright check passed on isolated ports/database; final 1440x900 screenshot inspected with no overlap and intentional empty-data states |
| 2026-08-30 | Admin Documents numeric-ID prefill regression | Passed with teardown caveat | Frontend production build transformed 2,508 modules; focused document draft save/reload journey passed and both installed-equipment and quotation lookups returned HTTP 200 without the prior `pickNumericId` error; Playwright was manually interrupted only after the known silent teardown delay |
| 2026-08-31 | Root-invoked complete Django discovery | Passed, 340 tests | `venv\Scripts\python.exe backend\manage.py test --noinput --verbosity 0` from repository root found and passed every current test; the prior CI-shaped command found zero before discovery was anchored to `backend` |
| 2026-08-31 | Unified smoke release gate | Passed | `npm run quality` passed Django check, migration drift, all 340 backend tests, the 2,508-module Vite production build, and 13 public/authentication browser tests; Playwright and both isolated servers exited normally |
| 2026-08-31 | Complete Playwright regression after lifecycle repair | Passed, 188 tests | `npm run test:e2e` passed all 80 functional/responsive flows and 108 desktop/mobile page-quality checks in 13.6 minutes on isolated ports/database, then shut down normally without manual interruption |
| 2026-08-31 | Phase 2 focused authorization regressions | Passed, 28 tests | Messaging/support REST and WebSocket capability parity (including explicitly scoped technicians), restricted notification recipients, and settings view/manage/public-site isolation passed on a fresh test database |
| 2026-08-31 | Dependency security gate | Passed | `pip check` reported no broken requirements; `pip-audit 2.10.1` reported no known vulnerabilities after Django 6.0.8 remediation; root and frontend npm audits both reported 0 vulnerabilities |
| 2026-08-31 | Phase 2 unified smoke release gate | Passed | `npm run quality` passed Django system check, Python dependency integrity, migration drift, all 347 backend tests, the 2,508-module Vite production build, and all 13 browser smoke tests; isolated services shut down normally |
| 2026-08-31 | Full backend regression after final WebSocket parity correction | Passed, 348 tests | Complete root-invoked Django suite passed after requiring `technician.messages.view` for assigned-technician WebSocket access; expected negative-path warning logs only |
| 2026-08-31 | Phase 3 focused workflow/authorization regressions | Passed | 51 dashboard/reporting/inventory/messaging tests passed, followed by the expanded 11-test inventory capability module; includes dashboard isolation, coverage/job-history separation, own technician inventory scope, REST support authority, and spoofed attachment rejection |
| 2026-08-31 | Phase 3 schema and frontend preflight | Passed | Django check and migration drift passed with no schema changes; Vite production build transformed 2,508 modules |
| 2026-08-31 | Initial Phase 3 unified release gate | Failed, stale test expectation | 353/354 backend tests passed; the only failure expected an after-sales-only capability set to retrieve the full legacy dashboard. The regression was corrected to require dashboard denial while preserving direct case-workspace access |
| 2026-08-31 | Final Phase 3 unified smoke release gate | Passed | `npm run quality` passed Django check, Python dependency integrity, migration drift, all 354 backend tests, the 2,508-module Vite production build, and all 13 browser smoke tests; isolated services shut down normally |
| 2026-08-31 | Phase 4 focused transition and public-site regressions | Passed, 49 tests | Manual status/reschedule/inspection exception evidence and split publish/upload/delete permissions passed on a fresh isolated database; legacy `public_site.manage` retained umbrella behavior |
| 2026-08-31 | Phase 4 unified smoke release gate | Passed | `npm run quality` passed Django check, Python dependency integrity, migration drift, all 359 backend tests, the 2,508-module Vite production build, and all 13 browser smoke tests; isolated services shut down normally |
| 2026-08-31 | Phase 5 staging-gate local preflight | Passed with expected PostgreSQL skips | 11 focused tests passed: health and staging-command safety checks passed, while the three real row-lock contracts were intentionally skipped on SQLite; both Node staging scripts passed syntax checks and the orchestrator refused to run without its confirmation marker |
| 2026-08-31 | Phase 5 local unified smoke release gate | Passed with 3 expected staging-only skips | `npm run quality` passed Django check, dependency integrity, migration drift, all 365 discovered backend tests (362 passed and 3 PostgreSQL-only concurrency contracts skipped on SQLite), the 2,508-module frontend build, and all 13 browser smoke tests |
| 2026-08-31 | Technician checklist backend/API regression | Passed, 19 tests | Full `api` app suite includes explicit site-inspection answer/recommendation requirements and required notes for negative findings; fresh isolated test database created and destroyed |
| 2026-08-31 | Technician checklist frontend build | Passed | Vite 8.2.2 transformed 2,508 modules after the completion-checklist and site-inspection UX changes |
| 2026-08-31 | Focused real technician checklist browser flows | Passed, 2 tests | Real seeded installation checklist rendered all work steps and required-photo evidence on one screen; inspection validation rendered three unanswered errors, mobile layout/sticky action passed, a valid PNG uploaded locally, and create/upload/complete APIs returned 201/200/200 |
| 2026-08-31 | Initial inspection submission browser run | Failed, superseded | The E2E server inherited workstation Cloudinary credentials and attempted an external upload; `settings_e2e.py` was pinned to local filesystem media and the same full submission passed on rerun |
| 2026-08-31 | Full backend regression after checklist usability work | Passed, 367 discovered | 364 runnable tests passed and the same 3 PostgreSQL-only concurrency contracts skipped correctly on SQLite; system check passed, no migration drift was detected, and `git diff --check` reported no whitespace errors |
| 2026-08-31 | Final mobile inspection submission rerun | Passed, 1 test | Clean 390x844 initial viewport, explicit validation errors, sticky completion controls, local PNG upload, and create/upload/complete responses 201/200/200; no unordered-pagination warning recurred and the screenshot was visually inspected |
| 2026-08-31 | Checklist draft-warning frontend build | Passed | Vite 8.2.2 transformed 2,508 modules after both technician forms received explicit non-file draft and media-reattachment notices |
| 2026-08-31 | Multi-service checklist API regression | Passed, 20 tests | Full `api` suite passed; technician job detail returned both ordered service configurations with distinct procedures, photo rules, and required equipment |
| 2026-08-31 | Multi-service checklist frontend build | Passed | Vite 8.2.2 transformed 2,508 modules after service-aware checklist composition and persisted service identity were added |
| 2026-08-31 | Multi-service technician browser flow | Passed, 1 test | Isolated E2E ticket rendered separate Solar Installation and Electrical Safety Check procedure sections, including both service-specific required-photo indicators; screenshot was visually inspected |
| 2026-08-31 | Dormant-schema focused regression | Passed, 22 tests | Fresh test database applied both cleanup migrations; active notifications and append-only progress APIs passed, retired history route returned 404, retired models were absent, and migration guards rejected simulated populated tables |
| 2026-08-31 | Dormant-schema full backend regression | Passed, 372 discovered | 369 runnable tests passed with the same 3 PostgreSQL-only skips; Django system check and migration drift passed with no unrepresented changes |
| 2026-08-31 | Local dormant-schema migration application | Passed | Confirmed all three target tables had zero rows, created ignored local backup `backend/local_backups/db-before-unused-schema-cleanup-20260831-1819.sqlite3`, applied `history.0005` and `notifications.0009`, verified retired tables absent, SQLite integrity `ok`, zero foreign-key violations, and unchanged core counts (19 users, 19 requests, 20 tickets, 220 inventory transactions, 384 notifications) |
| 2026-08-31 | Pre-structure-cleanup backup verification | Passed | Created ignored 20,545,536-byte SQLite backup and 10,258,152-byte/695-entry source ZIP; SHA-256 calculated for both, required source files were present, and archive inspection found zero `.env`, database, media, dependency, generated-output, or prior-backup entries |
| 2026-08-31 | First structure-cleanup regression | Passed, 372 discovered | Removed only dead `history` admin/serializer/URL/view modules; 369 runnable backend tests passed with 3 expected PostgreSQL-only skips, focused 22-test retirement/notification/progress suite passed, Django check and migration drift passed |

The current Playwright inventory is 198 tests in 9 files. The most recent full
uninterrupted suite evidence remains the then-current 193/193 run on 2026-09-02;
the newly added active-job mobile modal, two public landing interaction
scenarios, and one Our Work publication scenario have passed in focused runs but
have not yet been included in another
full-suite execution. Historical runs on
2026-08-30 completed assertions but hung in Windows web-server teardown. On
2026-08-31, direct Windows child-process ownership in global setup/teardown
replaced that fragile boundary and the suite exits normally.

Local E2E defaults to dedicated ports 8011/5181, refuses to reuse an existing
server, and can be overridden when another isolated run owns those ports:

```powershell
$env:E2E_BACKEND_PORT='8011'
$env:E2E_FRONTEND_PORT='5181'
npx playwright test
```

This matters because pre-existing VS Code servers may continue serving old
Python code on 8000/5174. Restart a development server after backend changes.

## 9. Known risks and pending decisions

| Priority | Item | Safest next action |
| --- | --- | --- |
| High before staging | The valid free Render staging Blueprint is published on `origin/staging-deploy` but not yet synced; its Aiven/Cloudinary/host/API placeholders and Aiven CA secret file are intentionally absent from Git | Sync the Blueprint from `staging-deploy`, enter secrets directly in Render, upload `aiven-ca.pem`, and require readiness plus the guarded staging gate before relying on the environment |
| Medium staging limitation | Render free web services sleep after idle time, free Key Value is non-persistent, common SMTP ports are blocked, paid cron jobs are omitted, and migrations must run in the single instance's startup command because free services lack pre-deploy commands | Treat this environment as disposable validation only; use logged console email and manual automation checks, then move to paid pre-deploy migrations, deliverable email, persistent Redis, and scheduled commands for production |
| High container deployment | `frontend/Dockerfile` and `docker-compose.yml` intentionally run Vite's development server with hot reload; they are suitable for local development, not as a hardened/cached production frontend. Docker is also unavailable locally, so neither image was built during the fresh audit | Prefer a managed static host using `frontend/dist` and `_redirects`, or add a multi-stage production frontend image with a real static server and SPA fallback; build/scan the chosen images in CI or staging before release |
| Low local storage | `backend/local_backups` still occupies about 139.57 MiB, including several 19.59 MiB database snapshots, a 12.22 MiB data-check export, and a 9.78 MiB source archive. These may be intentional recovery evidence and were not deleted. Installed environments also occupy about 357 MiB after bytecode cleanup but are needed for offline development | Review backup retention dates and recovery value before deleting any backup; remove `venv` or dependency trees only when accepting the cost and network requirement to reinstall them |
| Low forecasting maturity | The model lifecycle now trains, rolling-backtests, stores validation evidence, refreshes through the daily automation, and publishes only qualifying service models. The current development data still has only 20 genuine requests across about 4.4 months, so no model is publishable. Item quantities additionally depend on genuine ticket-linked issue history and configured service-item mappings. The 7/30-day view is a monthly-forecast allocation, and the location view is explicitly an allocation by recent historical density rather than a separate geographic model | Continue collecting genuine history (three years is materially stronger), keep issue transactions linked to tickets and requirement mappings current, monitor WAPE/bias after each run, and retain the descriptive trend whenever validation fails. Do not lower the publication gates merely to display a forecast |
| Low media compatibility | Structured proof records now render their declared video type, while older string-only records rely on common file extensions (`mp4`, `webm`, `ogg`, `mov`) to identify videos | Preserve the structured `type` field for new uploads; if uncommon legacy video formats exist, backfill their media type or extend the allowlist after browser-codec validation |
| Medium before deployment | Registration city/municipality and barangay choices depend on the third-party `psgc.cloud` API; the five CALABARZON provinces remain available locally and address is optional, but the lower-level selectors cannot populate during a provider or CORS outage | Verify the live hierarchy calls from the deployed browser origin; preferably vendor a dated official PSA PSGC CALABARZON snapshot or add a same-origin cached proxy before treating address capture as availability-critical |
| Low UX density | Route-wide geometry and representative screenshot inspection found consistent shell spacing and no clipped, overlapping, or unlabeled main actions, but the mobile Documents editor and inspection/checklist workflows remain long, information-dense surfaces | Conduct short task-based usability sessions with office and field users; prioritize progressive disclosure or saved server-side drafts only where observed completion time or error rate justifies the workflow change |
| High before release | Service migrations `0053` through `0056` may still be unapplied to production/Aiven data | Review backup and migration plan; obtain explicit deployment approval before applying to production |
| High before release | Sales Record migration `0057` is applied locally but not to production/Aiven | Test against staging, back up Aiven, and obtain explicit production deployment authorization before applying it there |
| High before release | Inventory migration `0006` may still be unapplied to production/Aiven data | Test against a staging copy, review its duplicate/blank identifier prechecks, back up Aiven, then apply with explicit deployment approval |
| High before production two-party returns | Inventory migration `0007` is applied locally but not to production/Aiven, so the new return-request tables do not yet exist remotely | Review and back up the intended deployment target, apply through the normal production migration step with explicit authorization, then run the technician-submit/admin-verify staging smoke journey |
| High before schema cleanup deployment | Cleanup migrations `history.0005` and `notifications.0009` are applied locally but not in production; another environment could contain rows even though local tables were empty | Back up the target, verify all three retired tables are empty, export/reconcile any unexpected rows, then obtain explicit approval and apply the guarded migrations |
| High before release | Operational commands do not run until an external scheduler is configured | Schedule `run_operational_automations --mode frequent` every 30 minutes and `--mode daily` daily; monitor both and prevent overlapping runs |
| High before release | Real production secrets/services are not verifiable from examples | Configure host secret store; run production health/deployment checks without logging values |
| High before release | Local automated checks are not an external penetration test and do not validate the deployed network perimeter, provider IAM, or secret-store configuration | Run a staging security review and authenticated penetration test, then resolve findings before production launch |
| High before release | The guarded staging gate exists but has not been executed against provisioned PostgreSQL, Redis, Cloudinary, and an HTTPS staging deployment | Provision isolated staging services and a separately scoped `test_...` PostgreSQL database, then run `npm run quality:staging` and retain the result before production approval |
| Medium | Local `REQ-0040` has historical `TKT-0035` inspection and `TKT-0037` installation records, each with downstream history/inventory data; Sales Record eligibility is currently ticket-based and could expose both completed tickets | Preserve both records; review them with the business owner, then restrict Sales Record preparation to eligible installation delivery tickets before creating records for this request |
| Medium | Existing development servers can hold stale imported Python code | Restart the intended backend after changes or use isolated E2E ports |
| Medium | Scheduler execution is verified by tests and script parsing, not by mutating live operational data | Run both modes once in staging after verifying its database/environment target, then confirm logs, notifications, assignments, reminders, and analytics |
| Low test infrastructure | The complete browser suite is intentionally sequential and most recently took 15.8 minutes because shared seeded identities prevent safe parallel execution | Keep the 13-test smoke gate on routine CI; run `npm run quality:full` for releases and only parallelize after fixtures are independently isolated |
| Low UX limitation | Checklist drafts preserve answers, notes, decisions, and completed step flags only on the current browser/device; browser security prevents restoring selected photo/video files after reload | Keep the draft label explicit; technicians must reattach evidence after reload, and a future cross-device/server draft phase would need authenticated draft media lifecycle controls |
| Low UX consistency | The shared shell, colors, status components, landmarks, and responsive boundaries are aligned, but complex feature pages still contain direct page-level Tailwind composition rather than a mandatory component library | Keep future work on the established brand tokens and shared UI components; migrate repeated patterns incrementally when a real inconsistency appears instead of forcing a risky whole-product redesign |
| Medium workflow integrity | Open tickets read the current service templates, so an administrator editing procedures after dispatch can change what a technician sees; completed submissions retain a checklist snapshot but dispatch-time templates are not yet immutable | Add a dispatch-time checklist-template snapshot and version identifier if procedures must be frozen for audit/regulatory use |
| Low progress limitation | Active checklist step completion is currently device-local until final submission, so the shared server progress intentionally represents only persisted workflow checkpoints rather than claiming live task completion | Keep the current honest status label; add authenticated server-side checklist drafts only if the business needs verified live step-level progress across devices |
| Low | HSTS preload is advisory rather than enabled | Enable only after HTTPS/subdomain policy is confirmed |
| Low | Analytics bundle is relatively large | Consider route/chart splitting after functional release work |
| Low before scale | Public OpenStreetMap tiles have a usage policy and no application-specific SLA | Keep attribution visible; review a managed or self-hosted tile provider if production traffic grows materially |
| Low public-site configuration | The public footer email address is currently a source-controlled business contact rather than part of the least-privilege landing-page publishing payload | Confirm the production mailbox before release; if non-superadmin publishers must change it, add a narrowly validated public contact field without exposing private system settings |
| Medium before production | The technical location policy, explicit collection control, capability boundary, and automated retention are implemented, but the organization has not yet named its responsible contact or approved jurisdiction-specific staff notice/consent wording | Have the responsible business/legal owner approve `TECHNICIAN_LOCATION_PRIVACY_POLICY.md`, provide it to technicians, record the contact/escalation process, and verify deployment values before activating production tracking |
| Low product limitation | Inventory now provides reliable service-stock operations and ticket reservation visibility, but it is not a procurement suite: supplier purchase orders, barcode scanning, lot/serial tracking, and demand-based reorder recommendations are not implemented | Add these only when actual operating volume requires them; preserve the append-only ledger and ticket-issue boundary when extending inventory |
| Low product boundary | Ticket equipment reconciliation is quantity-based and assumes interchangeable stock units; it does not identify which serialized physical unit came back | Keep the bounded ticket ledger for current consumables/equipment; add serial/asset-level issue and return records before using the workflow for uniquely tracked equipment |
| Low scheduling limitation | Exact-time overlap protection applies only when both appointments have concrete times and compares work beginning on the same scheduled date; `Time TBD` records cannot honestly be interval-checked and long jobs spanning midnight remain governed by the duration-based daily-capacity allocation rather than a cross-midnight interval lock | Require concrete times before dispatch where strict collision prevention is operationally necessary; extend the schedule model with explicit start/end timestamps before supporting overnight work |
| Medium dispatch data quality | Smart-assignment proximity is an explicitly labeled straight-line/travel-time estimate based on the technician profile's latest stored coordinates; candidate eligibility does not yet reject an old location timestamp or poor GPS accuracy, and routed road distance is still calculated only after assignment | Define an operational freshness/accuracy threshold and an offline fallback before enforcing it; persist or join the latest accuracy reading during ranking, then add stale/inaccurate-location regressions before using proximity as a contractual dispatch guarantee |
| Low academic-deliverable step | Final Context/DFD/Chen draw.io XML is structurally valid and the exhaustive Chen source has no overlapping semantic shapes. Its required direct 204 relationship legs use shared gutters, dedicated rails, and bridge arcs to organize unavoidable edge intersections, but the files were not rendered because diagrams.net is not installed in this workspace | Open each `.drawio` file in diagrams.net and the DBML in dbdiagram.io; verify the complete one-page export, bridge visibility, and text legibility at the intended Google Docs insertion or defense-print size |
| Low before scale | Activity Log service context still resolves related service records per visible log, so a service-heavy page can issue additional read queries | Batch-load service targets when audit volume or measured page latency justifies the optimization; CSV export already streams raw audit rows without those lookups |
| Low before scale | Completed-job detail serialization intentionally performs bounded per-page relation reads for crew/inventory correctness; the default is now 10 and an attempted generic prefetch-cache reuse proved unsafe for same-request assignment mutations and was removed | Keep the 10-record default and page-size cap; if measured latency warrants optimization, use endpoint-local immutable snapshots or explicitly clear relation caches after mutations and retain the full assignment/inventory regression suite |
| Low product boundary | Technicians can view official-document references but cannot generate office-controlled forms because generation remains behind the document-management capability | Keep the technician download labeled Work Summary; add a separate read-only finalized-document delivery contract only if the business approves technician access to official files |
| Low notification-routing limitation | Contextual sidebar badges and their click-to-read acknowledgement derive destinations from normalized notification title/message/type text because notifications do not yet store an explicit destination field | Keep notification copy aligned with the current routing rules; add a validated `target_route` or destination category to the notification contract before adding many new notification types |
| Low | The client support assistant is deterministic guidance, not a generative support agent, and reads a snapshot when opened/refreshed | Keep answers curated as workflows change; direct account-specific or unresolved concerns to the existing human support case flow |
| Low availability boundary | The public and authenticated solar calculators intentionally share published public settings and bundled defaults; during a settings-endpoint outage, either calculator can still operate with defaults that may be older than the latest published configuration | Preserve the resilient fallback, but consider displaying a settings freshness warning before estimates become contractual rather than preliminary |
| Medium product decision | Sales Records use a read-only accepted-quotation or signed-contract value when available. A reasoned manual reference value remains available for legacy completed work with neither source; it is not independently approved and is not payment evidence | Periodically review manual-value records and decide whether future exports need tax/accounting integration; keep payment collection and accounting state outside this service-management system |

## 10. Current worktree manifest

All paths below are preserved work, not cleanup candidates.

### Root, CI, and environment contracts

- `render.yaml`: free disposable Render staging Blueprint for the Django/Daphne
  backend, React static site, and Redis-compatible Key Value service; all
  provider secrets remain dashboard supplied.
- `.env.example`, `backend/.env.example`, `.gitignore`: local/production
  environment contracts, including verification-resend cooldown, and ignored
  local backup/archive boundaries.
- `backend/requirements.txt`: pinned backend dependencies, including the audited
  Django 6.0.8 security patch level.
- `README.md`, `docs/README.md`: repository and documentation entrypoints.
- `package.json`, `frontend/package.json`, `playwright.config.js`: build/test scripts, unified release/staging commands, and dedicated isolated E2E ports.
- `scripts/release-gate.mjs`: cross-platform fail-fast smoke/full release-gate
  orchestrator, including installed Python dependency integrity.
- `scripts/{staging-gate.mjs,staging-perimeter-check.mjs}`: explicit-confirmation
  staging orchestration and read-only deployed perimeter checks.
- `.github/`: time-bounded quality-gate workflow with Python/npm dependency audits.
- `AGENTS.md`: mandatory living-handoff maintenance instructions.
- `automation/`: repository-relative frequent/daily operational runners,
  Windows Task Scheduler registration, legacy analytics compatibility runners,
  logging, and deployment instructions.

### Backend platform and health

- `backend/manage.py`: repository-root-safe complete Django test discovery.
- `backend/afn_service_management/{health.py,settings.py,settings_e2e.py,tests_health.py,websocket_auth.py}`:
  production fail-fast settings, guarded disposable PostgreSQL test selection,
  critical database/Redis/media readiness, isolated E2E DB/local-only E2E media,
  authenticated WebSockets, and validated non-secret tracking-region/retention
  configuration; the global DRF pagination default is 10 records.
- `backend/afn_service_management/upload_validation.py`: shared upload validation.
- `backend/api/{views.py,test_landing_page.py}`: API behavior/validation changes,
  including canonical active-job tracking payloads, GPS freshness/accuracy,
  technician contact and workload context, generated-at metadata, and the
  consent-filtered public completed-project feed.

### Inventory

- `backend/inventory/{automation.py,consumers.py,models.py,serializers.py,tests.py,views.py}`:
  integrity, customizable catalog/category behavior, capability-scoped technician
  catalog access and own ledger/reservation visibility, transaction history,
  item-filtered ticket reservation context, `ITX`/`RSV` display identities,
  lifecycle validation, bounded ticket-return regression coverage, realtime
  behavior, and regression coverage.
- `backend/inventory/migrations/{0005_inventory_integrity_constraints.py,0006_inventorycategory_inventory_category_name_ci_unique.py,0007_equipmentreturnrequest_equipmentreturnrequestitem.py}`:
  inventory integrity, case-insensitive catalog identity, and two-party return
  requests; `0007` is applied locally and pending production/Aiven deployment.

### Messaging and notifications

- `backend/messages_app/{consumers.py,serializers.py,tests.py,views.py}`:
  capability-scoped staff/support access, participant authorization, restricted
  notification recipients, REST/WebSocket client-reply parity, signature-checked
  attachments, realtime behavior, and tests.
- `backend/notifications/{notification_utils.py,sla_notifications.py,tests.py}`:
  active-recipient handling, per-record SLA alert deduplication, and regression coverage.
- `backend/notifications/{models.py,serializers.py,admin.py,migrations/0009_remove_notificationlog_notification_and_more.py}`:
  active notification-only surface plus fail-closed retirement of unused template
  and delivery-log models; cleanup migration is applied locally, not remotely.
- `backend/history/{apps.py,models.py,tests.py,migrations/0005_delete_servicehistory.py}`
  and `backend/api/urls.py`: minimal migration shell for the retired redundant
  service-history model/API; dead admin/serializer/URL/view modules were removed,
  and completed history remains ticket-backed.

### Services, lifecycle, analytics, and documents

- `backend/services/{auto_dispatch.py,sla.py,views_follow_up.py}`:
  dispatch and lifecycle/SLA validation, including one canonical smart-ranking
  contract, minimum score enforcement, and exact-time collision rejection for
  both background and manual best-match assignment.
- `backend/services/{apps.py,signals.py,management/commands/reconcile_technician_availability.py}`
  and `backend/services/tests/test_technician_availability_reconciliation.py`:
  model-save coverage plus dry-run-first repair for derived lead/crew technician
  availability, with focused regression coverage.
- `backend/services/job_progress.py`, `backend/services/views_dashboard.py`, and
  `backend/services/tests/test_dashboard_role.py`: capability-protected role
  dashboards, complete admin attention/count evidence including unassigned
  scheduled visits, one canonical evidence-based workflow checkpoint contract
  with distinct inspection/service tracks, request-calendar scheduling-note
  preservation, and focused response-contract regression coverage.
- `backend/services/serializers/`: seven domain modules and explicit public
  re-exports preserving all 30 former serializer classes and existing import
  paths, including `QUO`/`DOC`/`EST` identities and shared ticket workflow
  progress fields used by client views.
- `backend/services/tests/`: domain-focused modules preserving the former
  monolithic suite plus focused completed-job history contract coverage and shared imports.
- `backend/services/models/{after_sales.py,analytics.py,core.py,documents.py}`:
  after-sales case events, forecast, request idempotency, and quotation/warranty fields.
- `backend/services/models/sales.py`, `backend/services/views/sales_records.py`,
  `backend/services/test_sales_records.py`, and `backend/services/migrations/0057_salesrecord_salesrecordline.py`:
  connected Sales Record schema, source snapshots, lifecycle/API authorization,
  source-controlled recorded contract values, reasoned manual fallback, migration,
  and regression coverage; migration is applied locally but not to Aiven.
- `backend/services/{maintenance.py,views_follow_up.py}`: after-sales lifecycle,
  automated handoffs, response deadlines, ownership, filtering, and summaries.
- `backend/services/views/{analytics.py,helpers.py,inspection.py,reports.py,service_requests.py,solar_estimates.py,technician.py,tickets.py}`:
  analytics, dashboard/coverage/job-history permission separation, inspection,
  reporting, centrally required transition evidence, reasoned rescheduling and
  inspection exceptions, workflow, ticket fixes, ticket-linked issued-equipment
  reconciliation, and ordered per-service checklist configuration on technician
  jobs, including field-facing client phone, request scope, estimated duration,
  checklist progress, readable lead-technician names, and final lifecycle status
  in technician history; location-history access supports administrator-scoped technician and
  recent-minute filters for field trails; assignment and reasoned rescheduling
  share duration-capacity plus lead/crew exact-time overlap validation; admin and
  technician completed-history endpoints default to 10 records per page.
- `backend/services/tests/test_staff_workspace_capability_access.py`: capability
  coverage for enriched tracking/configuration data, policy visibility, and
  administrator location-history filters.
- `backend/services/tests/test_job_history_contract.py`: completed/turnover
  lifecycle boundary, server pagination, role isolation, date validation, and
  connected history-detail regression coverage, including the 10-record default.
- `backend/services/tracking_config.py`,
  `backend/services/management/commands/purge_technician_locations.py`, and
  `backend/services/test_location_retention.py`: authenticated non-secret map/
  policy contract and enforced deletion of expired history/current coordinates.
- `backend/services/{test_document_capabilities.py,test_reporting_capabilities.py}`:
  focused capability/regression coverage.
- `backend/users/analytics_service.py`, `backend/users/analytics_workspaces.py`,
  `backend/users/forecasting_service.py`, `backend/users/views/admin_services.py`,
  `backend/services/views/analytics.py`,
  `backend/services/management/commands/generate_demand_forecasts.py`, and
  `backend/services/tests/{test_analytics_workspace.py,test_demand_forecasting.py}`: unified read-only live
  Analytics contract, six selected-only workspaces, explicit Manila date
  boundaries, comparison modes, consistent filter and metric semantics,
  qualified technician rankings, confirmed-sales/inventory/after-sales
  aggregation, validated seasonal-trend demand forecasting, rolling holdout
  evidence, guarded forecast generation, legacy snapshot no-write behavior,
  technician self-only analytics, query ceilings, and focused regression coverage.
- `backend/services/test_postgres_concurrency.py`: staging-only row-lock evidence
  for competing approvals, stock receipts, and reservations; skipped on SQLite.
- `backend/services/management/commands/run_operational_automations.py` and
  `backend/services/test_operational_automations.py`: deployable frequent/daily
  automation grouping, failure propagation, technician-location retention, and
  command-selection tests.
- `backend/services/management/commands/{generate_daily_analytics.py,generate_technician_performance.py,generate_demand_forecasts.py}`:
  scheduled analytics jobs that return non-zero failures to their scheduler;
  demand forecasts are published only after history and holdout gates pass.
- `backend/services/migrations/{0053_servicerequest_idempotency_key.py,0054_quotation_reference_and_warranty_terms.py,0055_unique_demand_forecast_period.py,0056_aftersalescaseevent.py}`:
  current service schema migrations; all are applied locally, while production
  application remains subject to deployment authorization.

### Users and security

- `backend/users/{middleware.py,permissions.py,rbac.py,serializers.py,signals.py,tests.py}`
  and `backend/users/views/{admin_activity.py,admin_settings.py,admin_users.py,auth.py,helpers.py}`:
  audit attribution, account/authentication validation, communication and system
  settings capabilities, technician field-inventory capability, granular
  public-site publishing/assets authority, restricted settings serialization,
  filtered audit summaries/streaming export, enumeration-safe verification
  resend/cooldown behavior, a 10-record Activity Log default, and tests.
- `backend/users/management/commands/verify_staging_dependencies.py` and
  `backend/users/test_staging_verification.py`: guarded live PostgreSQL, Redis,
  and durable-media probes plus local refusal-path tests.
- `backend/users/migrations/0038_adminsettings_landing_page_projects.py`: adds
  the admin-managed public project collection; it is applied locally and included
  in disposable E2E migrations, while production/Aiven remains pending approval.

### Frontend

- `frontend/src/index.css`: shared card and responsive workspace styling,
  including the existing stat-card hover feedback.
- `frontend/src/pages/LandingPage.jsx` and
  `frontend/src/components/{LandingNavbar.jsx,LandingFooter.jsx}`: editor-backed
  public content, promotions, and permission-gated completed-project cards,
  explicit CTA styling, actionable service cards, hash-aware cross-page section
  navigation, accessible mobile navigation, and connected calculator,
  registration, and email destinations.
- `frontend/src/pages/Register.jsx` and
  `frontend/src/utils/calabarzonLocations.js`: public client registration uses
  labeled dependent dropdowns for the five CALABARZON provinces, their cities/
  municipalities, and barangays. Child options clear when a parent changes;
  Lucena is intentionally grouped under Quezon to match the existing portal
  service-area convention. Selected names are joined into the existing address
  string; the page also provides persistent field labels, field-level errors,
  backend-aligned password guidance, and verification resend. No model, schema,
  or migration changed.
- `frontend/src/api/{admin.js,client.js,services.js,technician.js}`: request/API behavior,
  including activity summary/export, normalized technician location history,
  paginated completed-job contracts, a normalized single-ticket detail read,
  canonical client ticket-progress mapping including turned-over completion,
  two-party equipment-return actions,
  field-aware password errors, and a 10-record technician-history fallback.
- `frontend/src/context/{AuthContext.jsx,NotificationContext.jsx}`,
  `frontend/src/components/shared/{ProfileIdentityCard.jsx,ProfileSecuritySection.jsx}`, and
  `frontend/src/utils/profile.js`: immediate authenticated-user reconciliation,
  shared authenticated notification loading/polling/actions, branded
  identity/account summary and contact-field hierarchy, labeled password
  behavior, structured registration errors, bounded auth requests, and
  client-side profile-image constraints.
- `frontend/src/utils/roleIds.js`, `frontend/src/api/technician.js`: canonical
  zero-padded entity identities, six-digit ledger identities, and legacy/raw
  ticket-code normalization.
- `frontend/src/{App.jsx,rbac.js}` and
  `frontend/src/components/layout/{Layout.jsx,Sidebar.jsx,Topbar.jsx}` and
  `frontend/src/components/shared/ClientSupportAssistant.jsx`: responsive content
  gutter, capability-aligned routes/navigation, role-separated assistants,
  client-safe portal guidance, concise nonduplicated shell context, safe
  page-owned refresh behavior, a centered accessible logout confirmation with
  cancellation focus restoration, mobile assistant clearance, direct role-scoped
  Notifications navigation, one shared notification request lifecycle,
  synchronized total unread badges plus contextual work badges that clear and
  persist as read when their destination is opened, notification
  bell, and canonical ticket identities.
- `frontend/src/components/shared/{CalendarEventDetailsModal.jsx,EquipmentReturnDialog.jsx,InspectionReviewModal.jsx,RescheduleTicketModal.jsx}`
  and `frontend/src/pages/admin/AdminLandingPage.jsx`: required exception and
  schedule reasons, role-aware two-party ticket equipment submission/review,
  details-first calendar review with request-aware approval routing and
  image/video-aware inspection evidence, and granular public-site publish/upload controls.
- `frontend/src/pages/admin/AdminCalendar.jsx`: responsive month/agenda calendar,
  operational summaries and filters, truthful legacy conflict visibility,
  synchronized date navigation, and details-first ticket/request actions.
- `frontend/src/pages/admin/AdminDashboard.jsx`,
  `frontend/src/components/shared/{ActiveJobDetailsDialog.jsx,ActiveTechnicianJobs.jsx,ConfirmationDialog.jsx}`,
  and `frontend/src/components/ui/StatsCard.jsx`: actionable capability-safe
  operations signals, honest queue/stage labels, manual refresh and stale-data
  feedback, compact live-status/attention hierarchy, scannable interactive KPI
  affordances, visible Inspection visit/Service job track badges, accessible
  status-checkpoint progress bars, live active-ticket detail with timeline,
  full client/contact/request/assignment/visit/route/reschedule context,
  progressive service/equipment/timeline disclosure, consolidated attention
  evidence, crew/SLA/equipment context and explicit workspace actions, required
  request-rejection evidence, and keyboard-contained decision/detail dialogs.
- `frontend/src/components/shared/SearchFilterBar.jsx` and the Inventory,
  Activity Logs, After-Sales Cases, Job History, Reports, Operations Report,
  Coverage Heatmap, and client Service History pages: search-first progressive
  filters, plain-language choices, active-filter chips, consistent clearing,
  and support for compact page-specific toolbar controls such as map layers.
- `frontend/src/pages/{admin/{AdminActivityLogs.jsx,AdminDispatchBoard.jsx,AdminInventory.jsx,AdminJobHistory.jsx,AdminReports.jsx,AdminServiceTickets.jsx,AdminUserManagement.jsx},client/{ClientNotifications.jsx,ClientRequestTracking.jsx,ClientServiceHistory.jsx},follow_up/FollowUpCases.jsx,technician/TechnicianJobHistory.jsx}`:
  user-facing list pagination consistently defaults to 10 records; both Job
  History row selectors retain larger opt-in page sizes. Completed-history
  details preserve actual final status and client/technician proof viewers render
  supported video evidence with playback controls instead of broken images.
- `frontend/src/components/shared/WalkInRequestDialog.jsx` and
  `frontend/src/pages/admin/AdminServiceTickets.jsx`: responsive modal-based
  walk-in intake with explicit review/approval outcomes, grouped service and
  location data, sticky actions, guarded close/clear behavior, and URL-backed
  active/unassigned/missed-dispatch/SLA-warning/SLA-overdue queue views exposed
  through a labeled responsive selector.
- `frontend/src/components/maps/MapTileLayer.jsx` and
  `frontend/src/pages/admin/CoverageHeatmap.jsx`: shared keyless OpenStreetMap
  tiles; removed the CARTO raster dependency after its key-policy change.
- `frontend/src/pages/admin/AdminUserManagement.jsx`: user directory layout and
  access/action controls.
- `frontend/src/pages/admin/AdminAnalytics.jsx`,
  `frontend/src/pages/admin/analytics/AnalyticsWorkspacePanels.jsx`, and
  `frontend/src/api/admin.js`: one cancellable selected-workspace request,
  accessible tabs, compact domain-specific filters, the exact five Overview
  KPIs plus lower-emphasis cross-domain attention, comparison visuals,
  technician rankings/leaderboard/capacity, confirmed-sales composition,
  inventory coverage/shortage risk, after-sales and maintenance summaries,
  forecast readiness, published monthly/7-day/30-day outlooks, holdout evidence,
  service/location/item projections, readable labels, loading/empty/updating/
  error/retry states, and responsive layout.
- `docs/features/ANALYTICS_CURRENT.md`: authoritative current-state Analytics
  reference covering the live UI, unified endpoint, calculations, filters,
  permissions, data sources, forecast gate, limitations, and validation evidence;
  unlike the older blueprint, it distinguishes implemented behavior from future
  possibilities.
- `frontend/src/pages/admin/AdminTechnicianTracking.jsx`: resilient live field
  workspace with search/filters, separate GPS and job-coordinate health, selected
  technician contact/workload/history detail, route-versus-direct-line semantics,
  runtime-configured bounds/retention context, bounded empty states, and responsive
  technician/job cards.
- `frontend/src/utils/mapRegion.js` and
  `frontend/src/pages/technician/TechnicianMapNavigation.jsx`: validated runtime
  service-region normalization plus disclosed, technician-controlled location
  sharing that withholds routing, navigation-start state, and derived ETA until
  a real GPS location is explicitly supplied.
- `frontend/src/pages/admin/{AdminInventory.jsx,AdminServices.jsx}`: customizable
  inventory/category forms, full product details, safe service-template choices,
  stock summary/condition filters, compact desktop and mobile stock views,
  ticket-reservation detail, and non-overlapping guarded movement actions.
- `frontend/src/pages/admin/{AdminActivityLogs.jsx,AdminClientSupport.jsx,AdminDispatchBoard.jsx,AdminDocuments.jsx,AdminInventory.jsx,AdminLandingPage.jsx,AdminOperationsReport.jsx,AdminProfile.jsx,AdminServiceTickets.jsx,AdminServices.jsx,AdminSettings.jsx}`:
  document responsiveness and semantic print previews, numeric-ID-backed ticket
  prefill, canonical shell titles, consistent workspace spacing/landmarks,
  safe profile editing and readable permission scope, ticket accessibility,
  complete filtered audit evidence/export, explicit support/ticket/client/inventory
  identity labels, and unambiguous all-active versus ready-to-assign ticket-count
  scope across Service Tickets and Dispatch Board. Dispatch uses a responsive
  three-step select/team/review layout, guards technician actions until a valid
  ticket is selected, retains a compact current-assignments editor, and exposes
  one labeled, duplicate-safe, stock-aware equipment reservation plan. Its Find
  Best Match control is explicitly identified as a manual action that remains
  usable when unattended automatic dispatch is disabled.
- `frontend/src/pages/admin/AdminJobHistory.jsx`: server-paginated and sortable
  completed records, full-filter CSV export, connected operational detail,
  official document generation, proof preview, an accessible Escape-close
  completed-record dialog with restrained hierarchy and a persistent footer,
  plus a capability-guarded receiving verification entry point.
- `frontend/src/pages/follow_up/FollowUpCases.jsx`: paginated after-sales queue,
  case ownership, evidence-backed resolution/reopening, and activity timeline.
- `frontend/src/pages/client/{ClientDashboard.jsx,ClientNotifications.jsx,ClientProfile.jsx,ClientRequestDetail.jsx,ClientRequestTracking.jsx,ClientServiceHistory.jsx,ClientServiceRequests.jsx,ClientSolarEstimates.jsx,ClientSupport.jsx}`:
  canonical shell titles, consistent notification/profile workspace spacing,
  immediate shell unread-count updates after notification read/delete actions,
  keyboard-focusable dashboard request/ticket entry controls,
  complete client contact/company profile display, and canonical request/ticket identities across submission, tracking, history,
  detail, estimate, and support workflows; ticket details use the canonical
  backend stage label/value and no longer describe a status marker as measured
  percent complete. Solar Estimates keeps new calculations within the
  authenticated workspace and inserts successful saves into the visible list.
- `frontend/src/components/SolarCalculator.jsx`: shared public/authenticated
  calculation and save behavior with an embedded client-workspace presentation
  and a save-completion callback.
- `frontend/src/pages/shared/SalesRecords.jsx`: admin Sales Records lifecycle UI
  and client-owned Purchase Records view with canonical connected ticket labels,
  clearly sourced Recorded contract value display, read-only connected amounts,
  and a reason-required manual fallback only when no source amount exists.
- `frontend/src/pages/technician/{TechnicianDashboard.jsx,TechnicianJobHistory.jsx,TechnicianJobs.jsx,TechnicianMessages.jsx,TechnicianProfile.jsx,TechnicianSchedule.jsx}`:
  canonical shell titles, mobile activity layout, message accessibility,
  explicit session-scoped dashboard location sharing with no entry-time GPS call,
  safe profile behavior with retained performance/skills, a non-shrinking mobile
  schedule action, and recoverable/paginated completed-work history with connected
  timings, evidence, materials, equipment, field reports, office document references,
  a technician equipment-return submission entry point, and a conventional
  completed-service detail header and action footer. Active Job Details now
  presents schedule time, duration, work scope, a real call action, checklist
  progress, active equipment before collapsed cancelled history, and a
  context-sensitive checklist/Finish Job action without pretending staff chat
  is direct client messaging.
- `frontend/src/pages/technician/{TechnicianChecklist.jsx,TechnicianInspectionChecklist.jsx}`
  and `frontend/src/api/technician.js`: one-screen work procedures, explicit
  site-inspection assessment, local draft recovery, removable proof, responsive
  field controls, resumable incomplete inspection submission, and separate
  procedure/equipment sections for every requested service with service identity
  retained in submitted checklist rows.

### E2E and documentation

- `e2e/{01-public-pages.spec.js,03-admin-workspace.spec.js,04-technician-workspace.spec.js,05-client-workspace.spec.js,07-side-flows.spec.js,08-responsive-accessibility.spec.js,09-all-pages-quality.spec.js,global-setup.js,helpers.js,seed-test-data-exec.py}`:
  document persistence, responsive/accessibility, all-page matrix, isolated setup,
  real profile save/cancel/photo/security behavior, client assistant and
  role-scoped notification/sidebar-badge coverage, concrete role-dashboard
  content/action/keyboard/GPS-consent assertions, including admin refresh,
  queue totals, request-decision evidence, focus restoration, KPI routing, the
  canonical technician stage label/value, desktop/mobile active-job modal,
  calendar summary/filter/details behavior, and desktop/mobile public landing CTA,
  section-link, navigation, dependent CALABARZON registration-address behavior
  and unchanged address payload, and Our Work
  publication behavior;
  configurable dedicated ports, deterministic
  multi-service technician-checklist fixtures/assertions, geocoded active-job
  tracking fixtures, live GPS/degraded-refresh desktop coverage, responsive
  selected-technician detail, completed-history detail/CSV/search/empty-state
  journeys including ticket-linked equipment returns and visual detail-dialog
  screenshots, and direct Windows server startup/teardown without
  process-tree hangs.
- `e2e/E2E_TEST_REPORT_DETAILED.md`: retained historical 71-test snapshot with
  an explicit superseded notice pointing readers to this living validation ledger.
- `docs/SYSTEM_END_TO_END_IMPLEMENTATION_PLAN.md`: whole-system plan.
- `docs/deployment/{AIVEN_DEPLOYMENT.md,EMAIL_SMTP_SETUP.md,PRODUCTION_DEPLOYMENT_CHECKLIST.md}`:
  deployment contracts.
- `docs/deployment/STAGING_VALIDATION.md`: guarded production-shaped staging
  prerequisites, execution, safety boundaries, and evidence contract.
- `docs/security/{AUTHENTICATION_ACCOUNT_SECURITY_CONTRACT.md,INVENTORY_LEDGER_CONTRACT.md,SALES_RECORD_CONTRACT.md,ROUTE_API_CAPABILITY_MATRIX.md}`:
  current security/domain contracts.
- `docs/security/TECHNICIAN_LOCATION_PRIVACY_POLICY.md`: purpose, explicit
  collection boundary, authorized access, retention/deletion, regional settings,
  and pre-production staff-notice approval contract.
- `docs/security/RECORD_IDENTITY_CONTRACT.md`: canonical public record prefixes,
  widths, relationship labels, and persistence rules.
- `docs/quality/{IMPLEMENTATION_CHECKPOINT_PHASE_3_2026-08-29.md,IMPLEMENTATION_CHECKPOINT_PHASES_4_TO_9_2026-08-29.md,CURRENT_SYSTEM_AUDIT_AND_HANDOFF.md,MANUAL_END_TO_END_TESTING_PLAYBOOK.md}`:
  dated evidence, this living handoff, and the route-complete manual QA flow.
- `docs/academic/final_diagrams/`: the final manually laid-out draw.io Context
  Diagram plus links to one unified draw.io DFD, one unified draw.io Chen ERD,
  and the DBML schema, with captions, colors, and paper-layout instructions.
- `docs/academic/REQUIREMENT_MODELING_CAPSTONE.md`, `docs/models/README.md`,
  and `docs/README.md`: route academic readers to the final maintained diagram
  pack rather than the older compact examples.
- `docs/models/{AFN_ERD.dbml,AFN_ERD_CHEN_PRESENTATION.drawio,ERD_CURRENT.drawio,ERD_CURRENT.md,ERD_CURRENT_CONNECTIONS.md}`
  and `backend/scripts/{generate_afn_dbml.py,generate_afn_chen_drawio.py,generate_afn_relationship_map.py}`:
  reproducible physical DBML covering all 51 active application models with
  exact local columns and 102 in-scope relationships, plus the exhaustive
  DBML-derived single-page Chen presentation containing the same entities and
  attributes, with every relationship attached directly to canonical entities
  through structured gutters and rails.
- `docs/{architecture/{CONNECTED_CAPSTONE_MODELS_BLUEPRINT.md,DATABASE_DOCUMENTATION_OVERVIEW_MAY_2026.md,DATABASE_TABLE_USAGE_MAP_MAY_2026.md,DB_DIAGRAM.dbml,AFN_CORE_DATABASE_DIAGRAM_ACADEMIC.dbml},features/ANALYTICS_DASHBOARD_BLUEPRINT.md,models/{AFN_ERD.dbml,AFN_DATABASE_DIAGRAM.dbml,ERD_ACCURACY_RECHECK.md,ERD_MATERIALS_AND_CODES.md},AFN_SERVE_FULL_SYSTEM_ANALYSIS_CLIENT_REQUIREMENT_MAPPING.md}`:
  current textual/DBML schema documentation aligned with the retired models;
  older draw.io exports must be regenerated from the maintained DBML.

## 11. Authoritative document map

| Area | Document |
| --- | --- |
| Living status and handoff | This file |
| Whole-system delivery | `docs/SYSTEM_END_TO_END_IMPLEMENTATION_PLAN.md` |
| Workflow states | `docs/security/WORKFLOW_STATE_MACHINES.md` |
| Authentication/account security | `docs/security/AUTHENTICATION_ACCOUNT_SECURITY_CONTRACT.md` |
| Inventory ledger | `docs/security/INVENTORY_LEDGER_CONTRACT.md` |
| Record identities | `docs/security/RECORD_IDENTITY_CONTRACT.md` |
| Technician location privacy/retention | `docs/security/TECHNICIAN_LOCATION_PRIVACY_POLICY.md` |
| Sales/Purchase Records | `docs/security/SALES_RECORD_CONTRACT.md` |
| Route/API authorization | `docs/security/ROUTE_API_CAPABILITY_MATRIX.md` |
| Production release | `docs/deployment/PRODUCTION_DEPLOYMENT_CHECKLIST.md` |
| Production-shaped staging | `docs/deployment/STAGING_VALIDATION.md` |
| Aiven PostgreSQL | `docs/deployment/AIVEN_DEPLOYMENT.md` |
| Email/SMTP | `docs/deployment/EMAIL_SMTP_SETUP.md` |
| Analytics | `docs/features/ANALYTICS_DASHBOARD_BLUEPRINT.md` |
| Document workflow | `docs/features/DOCUMENT_AUTOFILL_AUDIT.md` and related document plans |

## 12. Fast next-agent startup

1. Read this file and `AGENTS.md`.
2. Run `git status --short --branch` only to detect drift from this manifest.
3. Read the specialist contract for the requested feature.
4. Inspect only the affected code and tests; do not repeat the full audit unless
   this file is materially stale or evidence contradicts it.
5. Make the narrow change, run proportionate validation, and update this file's
   affected sections and change ledger.
6. Leave work uncommitted unless the user explicitly authorizes a commit.

## 13. Change ledger

| Date/time (Asia/Singapore) | Change | Main paths | Validation |
| --- | --- | --- | --- |
| 2026-09-24 13:55 | Published the full audited release candidate and free staging Blueprint to a new GitHub deployment branch without changing `main` | Entire current worktree on `staging-deploy`; this file | Staged diff integrity passed across 287 paths; credential scan found placeholders only; commit `6b67c8f` created and `git push -u origin staging-deploy` succeeded. No Render resource or Aiven migration changed |
| 2026-09-24 13:38 | Prepared a no-secret free Render staging Blueprint using the user's Aiven PostgreSQL and Cloudinary services, plus Render Key Value and a React static site; documented exact free-tier limitations and the Aiven CA requirement | `render.yaml`, `docs/deployment/STAGING_VALIDATION.md`, this file | `render blueprints validate render.yaml -o text` passed with `valid: true` and three planned actions; no remote service, schema, migration, secret, commit, or push changed |
| 2026-09-24 13:12 | Cleared every known local release-gate blocker: aligned inventory completion fixtures with required proof, made Activity Logs service navigation order/pagination independent through server-backed search, and made Schedule task actions mobile-safe | `backend/inventory/tests.py`, `e2e/03-admin-workspace.spec.js`, `frontend/src/pages/technician/TechnicianSchedule.jsx`, this file | Focused backend passed 2/2; focused Activity Logs and Schedule browser checks passed; production build passed; final uninterrupted `npm run quality:full` passed system/dependency/migration checks, all 429 backend tests with 3 expected skips, 2,519-module build, and 201/201 Chromium scenarios in 15.8 minutes. Removed 199.69 MiB of regenerated test/cache artifacts afterward while retaining production builds and all persistent data |
| 2026-09-24 12:48 | Completed a production deployment-readiness audit and recorded a no-go verdict without changing application/test behavior | This file; regenerated `frontend/dist` and root `dist`; removed ignored smoke/E2E artifacts and bytecode caches | System check, migration drift, `pip check`, 2,519-module build, three dependency audits, and 16/16 browser smoke passed. Full-suite evidence remains 2 backend plus 2 browser failures; staging gate safely refused because disposable staging was not configured; Docker images could not be built locally. Removed 44.40 MiB of regenerated test/cache artifacts afterward; persistent data was untouched |
| 2026-09-24 12:02 | Removed only regenerable local storage while preserving databases, media, backups, secrets, source work, and installed environments | Removed ignored Playwright/build/E2E-database/bytecode/log artifacts; this file | Validated every resolved deletion target stayed inside the repository; removed 1,300 targets totaling 263.48 MiB and verified zero selected targets remained. No application tests were rerun because source/runtime behavior did not change |
| 2026-09-24 11:43 | Recorded a fresh end-to-end codebase audit without changing application or test behavior | This file | `npm run quality:full` passed system/dependency/migration checks, then stopped on the two known inventory fixture failures after 429 backend tests (424 pass, 3 skip, 2 fail). Direct isolated Chromium completed all 201 scenarios in 17.9 minutes with 199 pass and the same Activity Logs pagination-assumption and Technician Schedule mobile-clipping failures; failure evidence was inspected and no development database or production service was touched |
| 2026-09-22 11:50 | Recorded the complete post-forecast E2E result and classified its two non-Analytics failures without changing application or test behavior | This file | Full isolated Chromium ran 201 scenarios in 30.3 minutes: 199 passed and 2 failed. Analytics passed its focused all-workspace journey and separate desktop/mobile route audits. Activity Logs passed alone; Technician Schedule mobile passed alone and 3/3 repeated runs, demonstrating full-suite state/order dependence. Failure screenshots and Playwright error contexts were inspected; development data was untouched |
| 2026-09-21 10:42 | Implemented the Analytics trend-based demand-forecast lifecycle: genuine-history exclusion rules, per-service readiness, monthly seasonal plus damped-trend predictions, rolling holdout MAE/WAPE/bias and baseline comparison, guarded persistence, scheduled refresh/reconciliation, six-month and calendar-correct derived 7/30-day views, historical location allocation, item-demand translation, forecast-aware Assistant answers, and honest unavailable states | `backend/users/{forecasting_service.py,analytics_workspaces.py,views/admin_services.py}`, `backend/services/{models/analytics.py,serializers/analytics.py,views/analytics.py,management/commands/{generate_demand_forecasts.py,run_operational_automations.py},tests/{test_demand_forecasting.py,test_analytics_workspace.py},test_operational_automations.py}`, `frontend/src/{components/shared/SystemAssistant.jsx,pages/admin/{AdminAnalytics.jsx,analytics/AnalyticsWorkspacePanels.jsx}}`, `automation/{run_analytics.ps1,run_analytics.bat,README.md}`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Affected backend passed 30/30 after the final cross-month proration correction; Vite built 2,519 modules; focused Chromium passed 1/1; Django check and migration drift passed; full Django ran 429 with 424 pass, 3 skip, and the same 2 unrelated inventory failures. Development-data dry run published 0/9 because evidence gates are unmet. No schema migration, development-database write, route change, or unrelated business-flow change |
| 2026-09-20 21:32 | Fixed cutoff content specifically in the Analytics Assistant: removed hidden message overflow, enabled robust wrapping and preserved line breaks, rejected provider responses stopped by output limits, and fell back to the complete local explanation | `backend/users/{views/admin_services.py,tests.py}`, `frontend/src/components/shared/SystemAssistant.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused backend passed 23/23 including simulated `MAX_TOKENS`; production build passed with 2,519 modules; focused Chromium passed 1/1 with assistant overflow assertions; Django check and migration drift passed; no schema, migration, route, or unrelated module changed |
| 2026-09-20 21:22 | Removed cutoff Analytics wording and labels by replacing fixed-axis category charts with fully labeled proportional rows, removing truncation utilities, wrapping long text, and exposing every workspace tab on mobile without horizontal scrolling | `frontend/src/pages/admin/{AdminAnalytics.jsx,analytics/AnalyticsWorkspacePanels.jsx}`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Production build passed with 2,519 modules and a smaller Analytics chunk; focused Chromium passed 1/1 with full-label, no-truncation, all-tab visibility, and mobile overflow assertions; screenshots inspected; no API, backend, schema, migration, route, or unrelated module changed |
| 2026-09-20 20:47 | Synchronized the shared Analytics Assistant with the current Forecasting contract: loads canonical readiness evidence, describes location/item data as historical, excludes unlinked issue transactions, removes obsolete formula/confidence/capacity claims, aligns optional Gemini instructions and local fallback, and prevents questions during incomplete loading | `backend/users/{views/admin_services.py,tests.py}`, `frontend/src/components/shared/SystemAssistant.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused backend passed 22/22; final production build passed with 2,519 modules; focused Chromium passed 1/1 after an intermediate loading-race failure was corrected; Django check, migration drift, and scoped source review passed; no schema, migration, route, development-database write, or unrelated module changed |
| 2026-09-20 20:18 | Added a truthful service-location-to-item-demand Forecasting pipeline: historical service/location density, ticket-linked issued-item consumption, service-item requirement coverage, explicit 7/30-day readiness gates, and empty future quantities until a model is validated | `backend/users/analytics_workspaces.py`, `backend/services/tests/test_analytics_workspace.py`, `frontend/src/pages/admin/analytics/AnalyticsWorkspacePanels.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused Analytics passed 15/15; production build passed with 2,519 modules; focused Chromium passed 1/1 across all workspaces and the new panels; Django check, migration drift, and scoped diff check passed; no schema, migration, route, development-database write, or unrelated module changed |
| 2026-09-20 19:56 | Added exact custom Analytics comparisons for a specific earlier date or equal-length custom range, including controls, local validation, backend validation, period labeling, and regression coverage | `backend/users/analytics_service.py`, `backend/services/tests/test_analytics_workspace.py`, `frontend/src/{api/admin.js,pages/admin/AdminAnalytics.jsx}`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused Analytics passed 15/15; production build passed with 2,519 modules; focused Chromium passed 1/1 with an actual custom date-to-date request; no schema, migration, route, or unrelated module changed |
| 2026-09-20 19:42 | Removed Inventory summary-value overlap and added real descriptive Forecasting visuals: responsive metric sizing, genuine monthly demand with zero-filled gaps, service mix, peak month, monthly average, continuity labels, and preserved model gate | `backend/users/analytics_workspaces.py`, `backend/services/tests/test_analytics_workspace.py`, `frontend/src/pages/admin/analytics/AnalyticsWorkspacePanels.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused Analytics passed 15/15; production build passed with 2,519 modules; focused Chromium passed 1/1 including Inventory card-overflow and Forecasting visual assertions; Forecasting measured 5 queries; no schema, migration, route, or unrelated module changed |
| 2026-09-20 19:16 | Upgraded Admin Analytics into six selected-only workspaces with five comparison modes, qualified technician rankings, confirmed currency-safe sales, inventory linkage and shortage risk, after-sales/maintenance summaries, and strictly gated forecasts; added domain filters, loading boundaries, automated contracts/query ceilings, and all-tab browser coverage | `backend/users/{analytics_service.py,analytics_workspaces.py}`, `backend/services/tests/test_analytics_workspace.py`, `frontend/src/{api/admin.js,pages/admin/AdminAnalytics.jsx,pages/admin/analytics/AnalyticsWorkspacePanels.jsx}`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused Analytics 14/14 and combined permission/reporting 27/27 passed; Django check and migration drift passed; Vite build passed with 2,519 modules; focused Chromium all-workspace/mobile journey passed 1/1; full Django ran 422 with 417 pass, 3 skip, and the same 2 unrelated inventory failures; no schema, route, development-database write, or unrelated runtime module changed |
| 2026-09-20 15:34 | Added genuine equal-period comparison analytics without another frontend request: the API now returns prior-period KPI baselines/deltas and prior service, status, lead-assignment, and location counts; the UI exposes the exact comparison dates, direction-aware KPI changes, and Current-versus-Previous categorical bars instead of a non-comparative status donut | `backend/users/analytics_service.py`, `backend/services/tests/test_analytics_workspace.py`, `frontend/src/pages/admin/AdminAnalytics.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Focused Analytics/permission/security/reporting suite passed 21/21, including equal-period formulas and the <=20 query budget; Vite build passed with 2,518 modules; focused isolated Chromium passed 1/1 with comparison visibility and progressive-filter assertions; desktop/mobile screenshots inspected; no model, migration, route, or unrelated-module change |
| 2026-09-20 15:19 | Reduced Analytics visual and control overload without reducing its real-data coverage: removed the large dark hero, introduced a compact light header, placed operational highlights under the KPIs, kept Service and Status immediately available, and moved the seven secondary dimensions behind an accessible More filters disclosure | `frontend/src/pages/admin/AdminAnalytics.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/features/ANALYTICS_CURRENT.md`, this file | Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 including progressive-filter open/close assertions and desktop/mobile overflow coverage; fresh desktop and full-page mobile screenshots inspected; backend, API, metric formulas, route, schema, migrations, and unrelated modules unchanged |
| 2026-09-20 15:08 | Added an authoritative Markdown reference for the currently implemented Analytics workspace, including its route and access contract, UI hierarchy, exact KPI definitions, chart semantics, filter mapping, unified response shape, source data, honest forecast gate, Analytics-versus-Reports boundary, limitations, verification evidence, and implementation source map | `docs/features/ANALYTICS_CURRENT.md`, this file | Documentation source/structure review passed: all referenced implementation and test paths exist, all 13 numbered sections are present, and focused `git diff --check` has no whitespace errors; application code, API behavior, schema, migrations, and unrelated modules unchanged |
| 2026-09-20 14:58 | Modernized only Admin Analytics into a glance-first operations view: added live top-service, busiest-period, workload, and location summaries; strengthened KPI and attention hierarchy; added always-visible bar values and donut counts; separated technician job counts from scheduled hours so unlike units no longer share an axis; and converted forecast readiness into clear progress indicators | `frontend/src/pages/admin/AdminAnalytics.jsx`, this file | Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 at desktop and 390x844 mobile sizes with no horizontal main-content overflow; fresh desktop and full-page mobile screenshots inspected; route, API, formulas, backend, schema, migrations, and unrelated modules unchanged |
| 2026-09-20 13:45 | Rebuilt Admin Analytics around one live-data API and one cancellable frontend request; established explicit ticket-cohort formulas, Manila-time date boundaries, shared dimensions, zero-filled trends, primary-service/location/status/lead-workload charts, current attention, readiness-only forecasting, legacy assistant aliases, technician self-scoping, and read-only snapshot behavior | `backend/users/{analytics_service.py,views/admin_services.py,tests.py}`, `backend/services/{views/analytics.py,tests/test_analytics_workspace.py}`, `frontend/src/{api/admin.js,pages/admin/AdminAnalytics.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Focused Analytics/security/reporting suite passed 20/20; Django check passed; Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 at desktop/mobile widths with no final chart-size warnings; full Django suite ran 416 with 411 pass, 3 expected PostgreSQL skips, and 2 unrelated pre-existing inventory completion failures; query budget is at most 20; no migration or development-database write |
| 2026-09-18 23:25 | Curated the Admin Analytics first screen around reliable operational data: retained requests, completion, waiting work, available technicians, response/completion time, monthly completions, actual requested services, ticket status, and genuine attention items; moved forecast/value emphasis back to the deeper Demand workspace and excluded routine scheduled jobs from attention | `frontend/src/pages/admin/AdminAnalytics.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 with actual-service, empty-attention, truncation, and desktop/mobile overflow assertions; fresh desktop screenshot inspected; no schema or migration change |
| 2026-09-18 23:13 | Replaced Admin Analytics' visually clean but low-information mini charts with self-explaining breakdown cards: each completion month, forecast day, service, and operational signal now has a visible label, exact count, and proportional bar without requiring hover | `frontend/src/pages/admin/AdminAnalytics.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 with deterministic breakdown assertions plus desktop/mobile overflow coverage; fresh desktop and full-page mobile screenshots inspected; no schema or migration change |
| 2026-09-18 22:56 | Simplified Admin Analytics for faster reading: removed the duplicate insight strip, shortened metric, chart, tab, and workspace copy, allowed long values to wrap, and decluttered mini-chart axes so labels no longer clip; added desktop/mobile overflow and truncation regression coverage | `frontend/src/pages/admin/AdminAnalytics.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Final Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 with desktop and 390x844 mobile checks; fresh screenshots inspected; no schema or migration change |
| 2026-09-18 22:18 | Corrected cross-role detail accuracy: treated Turned Over / Accepted as client-complete, preferred readable timeline labels, preserved calendar request scheduling notes and routed request events to approvals, returned technician display names and history status, showed actual final status in history dialogs, and rendered video proof as video | `backend/{api/tests.py,services/{views_dashboard.py,views/technician.py,tests/{test_dashboard_role.py,test_job_history_contract.py}}}`, `frontend/src/{api/client.js,components/{shared/{CalendarEventDetailsModal.jsx,InspectionDetailsModal.jsx,TicketTimelineModal.jsx},ui/StatusBadge.jsx},pages/{admin/{AdminCalendar.jsx,AdminJobHistory.jsx},client/ClientServiceHistory.jsx,technician/TechnicianJobHistory.jsx}}`, this file | Focused backend contracts passed 33/33 plus 1/1 technician detail contract; Vite production build passed with 2,518 modules; isolated Chromium page regressions passed 4/4; diff check found only existing line-ending notices; no migration |
| 2026-09-15 23:24 | Connected notification-derived sidebar badges to their destinations: opening a badged workspace optimistically clears and persists only its mapped unread notifications, opening Notifications marks all read, failures reconcile from the server, and unrelated badges remain untouched | `frontend/src/{components/layout/Sidebar.jsx,context/NotificationContext.jsx}`, `e2e/07-side-flows.spec.js`, this file | Vite production build passed with 2,518 modules; isolated Chromium regression passed 1/1 and verified two related IDs reached the mark-read API while both contextual and aggregate badges cleared; no backend or migration change |
| 2026-09-15 22:54 | Turned Technician Job Details into an on-site decision view: added API-backed client phone, work scope, estimated duration and checklist totals; displayed schedule time and checklist progress; separated actionable equipment from cancelled history; added a valid Call Client control; and made the final action advance to Complete Checklist or Finish Job according to persisted state and lead/crew authority | `backend/services/views/technician.py`, `backend/api/tests.py`, `frontend/src/{api/technician.js,pages/technician/TechnicianJobs.jsx}`, `e2e/04-technician-workspace.spec.js`, this file | Focused API contract passed 1/1; Vite production build passed with 2,518 modules; isolated Chromium My Jobs/modal journey passed 1/1 after two recorded test-only locator corrections; final 1440x900 modal screenshot inspected; diff check passed with existing line-ending notices only; no migration |
| 2026-09-14 23:06 | Clarified the technician's last field-work step as Finish Job, reduced proof language to one clear completion-photo instruction, and enforced nonblank proof consistently on both backend completion routes for non-inspection tickets while retaining the dedicated inspection path | `backend/services/views/{helpers.py,technician.py,tickets.py}`, `backend/services/tests/{test_scheduling_warranty_and_assignment.py,test_maintenance_workflow.py}`, `frontend/src/{hooks/useTechnicianJobs.js,pages/technician/TechnicianJobs.jsx}`, this file | Seven focused completion tests and the broader 52-test scheduling/maintenance regression passed; Django check and the 2,518-module Vite production build passed; diff check found no whitespace errors beyond existing line-ending notices; no migration; browser journey not freshly run |
| 2026-09-14 22:44 | Removed the duplicate full-width Navigate action from every Technician Jobs card, retained the consistent Details/Navigate action row, and added a one-navigation-link-per-card browser regression assertion | `frontend/src/pages/technician/TechnicianJobs.jsx`, `e2e/04-technician-workspace.spec.js`, this file | Vite production build passed with 2,518 modules; `git diff --check` passed with existing line-ending notices only; browser assertion added but not freshly run |
| 2026-09-14 22:34 | Made Choose a Team show every service-qualified technician with explicit Online, Busy, or Offline dispatch state, readiness explanations, Online-first ordering, and protected Busy/Offline actions; clarified that Online is operational availability rather than device presence | `frontend/src/{components/ui/StatusBadge.jsx,pages/admin/AdminDispatchBoard.jsx}`, this file | Vite production build passed with 2,518 modules; `git diff --check` passed with existing line-ending notices only |
| 2026-09-14 22:20 | Fixed Dispatch Board technician selection at its source: ticket and crew model signals now keep derived availability synchronized for API, admin, script, and direct-model changes; a dry-run-first repair command reports historical mismatches; the board only offers technicians qualified for the selected service and explains the exact active-work busy boundary; the reviewed local reconciliation restored all four stale technician flags | `backend/services/{apps.py,signals.py,views/helpers.py,management/commands/reconcile_technician_availability.py,tests/test_technician_availability_reconciliation.py}`, `frontend/src/pages/admin/AdminDispatchBoard.jsx`, `backend/db.sqlite3`, `docs/security/WORKFLOW_STATE_MACHINES.md`, this file | Django check and migration drift passed; 4/4 focused signal/command tests passed; local application changed 4/4 mismatches, follow-up dry run reported 0 mismatches, and a direct read confirmed all four flags true; Vite production build passed with 2,518 modules; no migration |
| 2026-09-09 21:03 | Polished the requested artifact into a clean complete manual E2E Markdown checklist: normalized 13-section navigation, retained the 27-step cross-role order and every page/function matrix, and added 20 operational/backend checks covering health through recovery and the staging perimeter | `docs/quality/MANUAL_END_TO_END_TESTING_PLAYBOOK.md`, this file | Structural checks cover section references, order IDs, registered routes, Markdown fences, and the new operational matrix; documentation-only change |
| 2026-09-09 19:49 | Added the requested master testing order to the manual QA playbook: 27 sequential, role-labeled steps with explicit handoff conditions, from environment setup and account creation through the complete service lifecycle, every page/edge-case sweep, logout security, and final reconciliation | `docs/quality/MANUAL_END_TO_END_TESTING_PLAYBOOK.md`, this file | Structural review confirmed every ordered step names the actor, action, and condition required before continuing; documentation-only change |
| 2026-09-09 19:40 | Added a safe, ordered manual whole-system QA playbook: isolated environment startup, seeded roles, evidence/defect templates, public/account and authorization checks, one full service lifecycle across all roles, every primary workspace and redirect, negative/concurrency/upload/export/responsive/accessibility testing, reconciliation, and release sign-off | `docs/quality/MANUAL_END_TO_END_TESTING_PLAYBOOK.md`, `docs/README.md`, this file | Confirmed all 63 non-wildcard registered route patterns are represented and Markdown code fences are balanced; documentation-only change, so no new runtime pass is claimed |
| 2026-09-09 12:28 | Replaced the sidebar's compact inline logout confirmation with the shared centered dialog across authenticated roles; added a clear sign-in consequence, destructive Log out action, safe cancellation, request-pending protection, dialog semantics, and focus restoration to the trigger | `frontend/src/components/layout/Sidebar.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 for modal visibility/content/actions, cancellation, unchanged route, and trigger focus restoration; no backend or schema change |
| 2026-09-09 12:07 | Unified and hardened smart technician assignment: background and manual matching now share ranking, schedule-overlap, capacity, availability, location, and strict score-threshold rules; the selected candidate is rescored after locking; the current best assignee is retained; scheduled queue work no longer causes blanket busy status; proximity copy is explicitly approximate; and the Dispatch Board explains that Find Best Match is manual even when unattended dispatch is off | `backend/services/{auto_dispatch.py,test_auto_dispatch_and_sla.py,views/{helpers.py,tickets.py},tests/test_scheduling_warranty_and_assignment.py}`, `frontend/src/pages/admin/AdminDispatchBoard.jsx`, `docs/security/WORKFLOW_STATE_MACHINES.md`, this file | Affected backend passed 63/63; Django check and migration drift passed; Vite production build passed with 2,518 modules; focused isolated Chromium Dispatch Board journey passed 1/1; diff integrity check passed with existing line-ending notices only; no schema change |
| 2026-09-08 23:35 | Clarified and enforced Purchase Record value provenance: renamed the UI concept to Recorded contract value, made accepted-quotation and signed-contract totals source-controlled/read-only and refreshed at confirmation, and limited manual entry to missing-source cases with a required explanation | `backend/services/{serializers/commercial_records.py,test_sales_records.py,views/sales_records.py}`, `frontend/src/pages/shared/SalesRecords.jsx`, `e2e/03-admin-workspace.spec.js`, `docs/security/SALES_RECORD_CONTRACT.md`, this file | Seven focused backend tests, Django check, and migration drift passed; Vite production build passed with 2,518 modules; focused isolated Chromium passed 1/1 and the 1440x900 source-controlled draft screenshot was inspected; no schema change |
| 2026-09-08 23:01 | Standardized actual list pagination to 10 records across backend defaults and admin, client, technician, and after-sales pages while preserving separate dashboard-preview, lookup, and export limits | `backend/afn_service_management/settings.py`, `backend/users/{tests.py,views/admin_activity.py}`, `backend/services/{tests/test_job_history_contract.py,views/reports.py,views/technician.py}`, `frontend/src/api/technician.js`, `frontend/src/pages/{admin/{AdminActivityLogs.jsx,AdminDispatchBoard.jsx,AdminInventory.jsx,AdminJobHistory.jsx,AdminReports.jsx,AdminServiceTickets.jsx,AdminUserManagement.jsx},client/{ClientNotifications.jsx,ClientRequestTracking.jsx,ClientServiceHistory.jsx},follow_up/FollowUpCases.jsx,technician/TechnicianJobHistory.jsx}`, this file | Fourteen focused backend tests passed with new default-page assertions; Django check and migration drift passed; Vite production build passed with 2,518 modules |
| 2026-09-08 22:50 | Completed the public Create Account refinement: persistent labels, field-associated validation, Django-aligned password guidance, a 15-second request timeout, provider-neutral verification instructions, enumeration-safe/cooldown-protected resend, and dependent CALABARZON address dropdowns with unchanged combined-address storage and Lucena compatibility | `.env.example`, `backend/.env.example`, `backend/afn_service_management/settings.py`, `backend/users/{tests.py,views/auth.py}`, `frontend/src/{context/AuthContext.jsx,pages/Register.jsx,utils/calabarzonLocations.js}`, `e2e/01-public-pages.spec.js`, `docs/security/AUTHENTICATION_ACCOUNT_SECURITY_CONTRACT.md`, this file | Fifteen focused backend tests, Django check, and migration drift passed; final Vite production build passed with 2,518 modules; focused isolated Chromium passed 2/2 for address payload/reset, password mismatch, API field errors, and resend cooldown; final screenshot inspected; live PSGC provider DNS remained unavailable and is recorded as a deployment risk |
| 2026-09-08 14:26 | Expanded the Markdown deliverable into one complete end-to-end ERD containing the full entity and attribute catalog followed by the readable relationship map; retained the connection-only companion | `backend/scripts/generate_afn_relationship_map.py`, `docs/models/{ERD_CURRENT.md,ERD_CURRENT_CONNECTIONS.md,README.md}`, `docs/academic/final_diagrams/README.md`, this file | Confirmed 51 entity sections, 676 typed and constrained attribute rows, and 102 formatted relationship lines; deterministic regeneration matched SHA-256 `97A323BF0F6E154CB286FA58BF8455635BE2D3AA5285A8DC40C5270EBB602F3E`; generator byte-compilation and diff check passed |
| 2026-09-08 14:11 | Added the requested diagram-independent relationship reference, expressing every connection as source entity, source cardinality, verb, target cardinality, and target entity, with exact field mappings | `backend/scripts/generate_afn_relationship_map.py`, `docs/models/{ERD_CURRENT_CONNECTIONS.md,README.md}`, `docs/academic/final_diagrams/README.md`, `backend/scripts/generate_afn_chen_drawio.py`, this file | Confirmed exactly 102 consecutively numbered relationships; repeat generation matched SHA-256 `026DC74455A22451E0F4A7C2633046A393DBDE3936214C48ED68B0DE9F60E2CD`; generator byte-compilation passed |
| 2026-09-08 13:56 | Made every exhaustive Chen connection independently readable: added matching `R001`-`R102` identifiers to both direct legs and their diamond, printed full source/target entity-field-cardinality descriptions inside each diamond, strengthened black connection styling, and made the generator maintain both requested filenames | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,ERD_CURRENT.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | XML validation confirmed 102 relationship diamonds, 204 direct legs, 102 unique codes each appearing exactly three times, and no missing endpoints; both files match at SHA-256 `5093B068F4CBB024772615720C56E0BA5875892CAFAC6E2A37EC626578FA95BD`; generator byte-compilation and Django check passed |
| 2026-09-08 13:44 | Added the requested conveniently named current ERD file as an exact copy of the accepted exhaustive Chen presentation, without replacing the original | `docs/models/ERD_CURRENT.drawio`, this file | Source and copy SHA-256 hashes match exactly at `B72D3045D19CBC08893B00A546576B8BFCDAB9BAB823B0623372A6271688C314` |
| 2026-09-08 13:22 | Recorded final acceptance evidence for the exhaustive one-page Chen ERD after rechecking relationship labels and the exact reference-style shape counts | this file | Confirmed 204 valid cardinality-and-field labels, 0 invalid or missing relationship labels, 51 green entities, 676 yellow attributes, and 102 orange diamonds; generator byte-compilation, Django system check, and diff check passed |
| 2026-09-08 12:32 | Kept solar calculation inside the authenticated client experience: New calculation now expands the shared calculator in Solar Estimates, preserves the client shell, respects published calculator settings, and adds a successful save to the visible estimate list without a page reload; retained the public landing calculator for visitors | `frontend/src/{components/SolarCalculator.jsx,pages/client/ClientSolarEstimates.jsx}`, `e2e/05-client-workspace.spec.js`, this file | Frontend production build passed with 2,517 modules; focused isolated Chromium passed 1/1 and verified the URL remains `/client/solar-estimates`, the client navigation stays visible, and Save estimate is available; 1440x900 screenshot inspected |
| 2026-09-07 00:34 | Applied the full attached ERD poster specification: analyzed endpoint degree, centered the User and Service Ticket hubs, reordered each logical module by connectivity, moved low-connectivity modules outward, expanded the result to a true landscape canvas, and placed each diamond between its source/target module gutters to shorten relationship rails while retaining the classic supplied Chen styling | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | The deterministic one-page 16,360 × 11,648 output contains all 51 entities, 676 attributes, 51 underlined keys, 102 diamonds, 204 direct cardinality-labeled relationship legs, and 1,730 cells. Automated checks found 0 duplicate IDs, missing endpoints, wrong relationship attachments, object overlaps, connector passages through unrelated objects, or out-of-page vertices. Graph-aware placement reduced proper rail/gutter crossings from a rejected 8,903 to 251 (97.2%), all bridge-enabled; generator byte-compilation, Django check, and diff check passed; rendered inspection remains pending because diagrams.net is unavailable |
| 2026-09-07 00:15 | Restyled the directly connected exhaustive ERD to match the user's supplied classic Chen example: small green entity rectangles centered between their yellow oval attributes, orange relationship diamonds, black connectors, underlined keys, and visible cardinalities; retained the one-page DBML-derived scope and structured gutter/rail routing required by the much larger graph | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | Deterministic one-page output contains 51 entities, 676 attributes, 51 underlined keys, 102 diamonds, 204 direct relationship legs, and 1,730 cells; 0 duplicate IDs, missing endpoints, incorrectly attached relationship edges, semantic-vertex overlaps, or relationship-connector passages through unrelated shapes; byte-compilation and Django system check passed; rendered inspection remains pending because diagrams.net is unavailable |
| 2026-09-07 00:02 | Corrected the same-page Chen layout after the user clarified that the relationships must connect directly to the primary entity boxes: removed all 204 duplicate endpoint references, connected every DBML relationship leg to its canonical entity, and organized the exhaustive wiring with clear entity exits, shared domain gutters, separate source/target rails, and visible bridge arcs | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | One valid 11,560 × 13,052 page contains 51 entities, 676 attributes, 51 underlined keys, 102 diamonds, 204 directly attached routed relationship legs, and 1,730 cells; 0 duplicate IDs, missing endpoints, incorrectly attached relationship edges, overlapping semantic vertices, or connector passages through unrelated shapes; deterministic regeneration, byte-compilation, Django system check, and diff check passed; rendered inspection remains pending because diagrams.net is unavailable |
| 2026-09-06 23:39 | Rebuilt the exhaustive Chen ERD directly from the DBML reference and replaced the long cross-domain relationship web with a same-page relationship index whose 102 source–diamond–target rows cannot cross; retained all canonical entity definitions, attributes, primary-key underlining, cardinalities, editable shapes, and the professor-format color language | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | One valid 11,560 × 7,588 page contains 51 entities, 676 attributes, 102 relationship diamonds, 204 compact references, and 1,934 cells; 0 duplicate IDs, missing endpoints, semantic-vertex overlaps, non-horizontal connections, or proper line intersections; deterministic SHA-256 regeneration, byte-compilation, and corrected Django system check passed; rendered export inspection remains pending because diagrams.net is unavailable |
| 2026-09-06 23:10 | Remediated the dependency findings from the whole-system audit: upgraded Django REST Framework to 3.17.2 and refreshed the affected frontend build-chain lock entries to patched versions, then repeated the complete backend, build, smoke, all-role workflow, and route-wide UI regression | `backend/requirements.txt`, `frontend/package-lock.json`, this file | `pip-audit`, root npm audit, and full frontend npm audit each found 0 vulnerabilities; `npm ci` passed after one transient Windows `EBUSY` retry; `npm run quality` passed 394 backend tests (3 expected PostgreSQL-only skips), a 2,517-module build, and 15/15 smoke tests; complete Playwright passed 197/197 in 17.5 minutes |
| 2026-09-06 22:38 | Recorded a fresh whole-system functional, security, and UI audit without changing application dependencies or behavior; preserved the passing local release evidence, documented newly published backend and build-tool advisories, and corrected the prior stale claim that all dependency audits were clean | this file | `npm run quality` passed 394 backend tests (3 expected PostgreSQL-only skips), a 2,517-module build, and 15/15 smoke tests; focused authorization/integrity passed 144/144; complete Playwright passed 197/197 including 108 route-quality checks; representative screenshots were inspected; `pip-audit` found the two DRF 3.17.1 CVEs, root npm audit found 0, frontend npm audit found 2 build-tool issues, and frontend runtime-only npm audit found 0 |
| 2026-09-04 11:27 | Restored every physical-schema entity and attribute to the final Chen ERD at the user's direction; widened all six domain lanes, increased block spacing, retained all schema-backed relationship diamonds/cardinalities, and added visible connector bridges to distinguish unavoidable intersections in the exhaustive one-page graph | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | The valid one-page XML contains 51 entities, 676 attributes, 102 relationships, and 1,728 cells with zero duplicate IDs, missing endpoints, or overlapping entity/attribute/relationship vertices; deterministic generation, byte-compilation, Django check, and diff check passed; diagrams.net export inspection remains pending |
| 2026-09-04 11:20 | Replaced the exhaustive, connector-dense Chen canvas with a clean conceptual paper figure selected from the same authoritative schema; retained the core intake, dispatch, field, inventory/return, sales, after-sales, support, and notification entities, limited each to four defense-relevant attributes, and explicitly positioned every oval and diamond to remove tangled paths while leaving the complete 51-table contract in DBML | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | Live-model field selection passed; the one-page XML contains 22 entities, 88 attributes, 22 relationships, and 274 cells with zero duplicate IDs, missing endpoints, vertex overlaps, proper straight-connector crossings, or connector passages through unrelated vertices; deterministic generation, byte-compilation, Django check, and diff check passed; diagrams.net export inspection remains pending |
| 2026-09-04 11:05 | Replaced the partial presentation ERD with one full Chen-style draw.io diagram generated from the corrected physical schema contract; included every application model and local field, all in-scope relationships, underlined keys, cardinalities, domain lanes, and the professor-approved pink entity/green diamond/yellow oval notation, while retaining a deterministic generator for future schema changes | `backend/scripts/generate_afn_chen_drawio.py`, `docs/models/{AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/academic/final_diagrams/README.md`, this file | One valid draw.io page contains 51 entities, 676 attributes, 102 relationship diamonds, and 1,728 cells with zero duplicate IDs or missing endpoints; deterministic hash check passed; visual diagrams.net inspection remains pending |
| 2026-09-04 10:49 | Rebuilt the paper's physical database schema from live Django metadata after a deeper audit found that the prior hand-maintained version matched table names but not columns; corrected every custom primary-key name and relationship target, restored all current fields, represented nullability/uniqueness/composite indexes, annotated the one out-of-scope Django content-type relationship, and retained a deterministic generator for future model changes | `backend/scripts/generate_afn_dbml.py`, `docs/models/{AFN_ERD.dbml,README.md}`, this file | Exact model-to-DBML comparison passed for 51/51 tables with zero column differences; 102/102 in-scope relationships emitted; official DBML PostgreSQL compilation, deterministic hash check, generator byte-compilation, Django system check, and diff check passed |
| 2026-09-04 10:33 | Fixed the dbdiagram.io DBML syntax by converting Project metadata and column-note string literals to the required single-quote form, while retaining valid double-quoted TableGroup identifiers containing spaces; refreshed the schema verification note | `docs/models/AFN_ERD.dbml`, this file | Official `@dbml/cli` PostgreSQL compilation passed with exit 0 after the final edit; `git diff --check` passed |
| 2026-09-04 10:17 | Replaced the final Mermaid presentation with editable, manually positioned draw.io diagrams matching the professors' pre-oral examples; added the new central-system Context Diagram, updated the structured DFD for overlap handling, two-party returns, sales, support, and current notifications, and updated the Chen ERD with equipment returns, sales, generated documents, and customer support while removing the retired Service History concept and empty second page | `docs/academic/final_diagrams/`, `docs/models/{AFN_DETAILED_DFD_STRUCTURED.drawio,AFN_ERD_CHEN_PRESENTATION.drawio,README.md}`, `docs/README.md`, this file | All three draw.io files parsed as valid XML with 41/77/181 cells, zero duplicate IDs, zero missing edge endpoints, and zero stale terms; diff check passed; visual diagrams.net export inspection remains pending |
| 2026-09-04 09:54 | Established the professors' one-diagram-per-type structure with unified Mermaid drafts; their auto-layout presentation was superseded at 10:17 by the editable draw.io figures | `docs/academic/final_diagrams/`, `docs/README.md`, this file | The three Mermaid drafts passed structural checks before being replaced; the one-figure-per-type decision remains current |
| 2026-09-03 22:08 | Built the initial capstone diagram source set and reconciled the physical schema with the application; its split DFD/ERD presentation was superseded on 2026-09-04 by the required unified figures | `docs/academic/final_diagrams/`, `docs/academic/REQUIREMENT_MODELING_CAPSTONE.md`, `docs/models/{AFN_ERD.dbml,README.md}`, `docs/README.md`, this file | Django registry-to-DBML comparison matched 51/51 tables with zero missing/stale; the then-current eight Mermaid sources passed structural checks |
| 2026-09-03 21:35 | Upgraded the admin Service Calendar into a simple operations workspace: added focused summary counts, work/service/technician/status filters, a mobile date-led agenda, synchronized month/day navigation, details-first client/location/team/work context, explicit queue/dispatch/reschedule actions, visible legacy collision warnings, and server-side exact-time overlap rejection across manual lead/crew assignment, rescheduling, and auto-dispatch while retaining daily-duration limits | `backend/services/{auto_dispatch.py,test_auto_dispatch_and_sla.py,views_dashboard.py,views/{helpers.py,tickets.py},tests/test_scheduling_warranty_and_assignment.py}`, `frontend/src/{pages/admin/AdminCalendar.jsx,components/shared/CalendarEventDetailsModal.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Django check and migration drift passed; affected backend passed 60/60; production build transformed 2,517 modules; isolated calendar E2E passed 1/1; desktop screenshot inspected; no migration required |
| 2026-09-03 14:56 | Applied the explicitly approved Our Work schema migration to the local development SQLite database; production/Aiven was not touched | `backend/db.sqlite3`, `backend/users/migrations/0038_adminsettings_landing_page_projects.py`, this file | Targeted migration completed successfully; migration-recorder verification returned `True`; Django system check and scoped diff check passed |
| 2026-09-03 14:40 | Added an admin-managed Our Work portfolio: project records require complete service/location/date/description/photo details and confirmed client permission before publication; the public API filters and strips control fields, the landing page renders responsive project cards, asset deletion respects project references, and the admin editor supports private drafts, image upload, permission confirmation, and publish controls | `backend/{api/views.py,users/{models.py,serializers.py,signals.py,views/admin_settings.py,migrations/0038_adminsettings_landing_page_projects.py}}`, `frontend/src/{utils/landingSettings.js,pages/LandingPage.jsx,pages/admin/AdminLandingPage.jsx}`, `e2e/{01-public-pages.spec.js,03-admin-workspace.spec.js}`, this file | Django check, migration drift, and 11 focused API tests passed; production build transformed 2,516 modules; live isolated admin-to-anonymous publication journey passed 1/1; screenshot inspected; current inventory confirmed as 197 tests in 9 files |
| 2026-09-03 12:56 | Repaired and clarified the real public landing page: replaced undefined button-theme classes with complete controls, made all service cards actionable, added an accessible phone/tablet menu, fixed cross-page section destinations and hash scrolling, retained admin-managed copy/promotions/calculator settings, and improved responsive hierarchy without changing backend or schema contracts | `frontend/src/{pages/LandingPage.jsx,components/{LandingNavbar.jsx,LandingFooter.jsx}}`, `e2e/01-public-pages.spec.js`, this file | Production build passed with 2,516 modules; focused desktop/mobile interactions passed 2/2, the original landing journey passed 1/1, and desktop/mobile page quality passed 2/2 on isolated services; 390x844 full-page screenshot inspected; current inventory confirmed as 196 tests in 9 files; scoped diff check passed |
| 2026-09-03 12:29 | Simplified the detailed Job Progress modal through progressive disclosure: retained operational essentials up front, collapsed service/equipment support detail, limited the initially open timeline to three events with full expansion, removed empty optional rows, consolidated hold/reschedule/SLA warnings, and preserved complete underlying evidence | `frontend/src/components/shared/ActiveJobDetailsDialog.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Final production build passed with 2,516 modules; final desktop/mobile modal journeys passed 2/2 without the initial React disclosure warning; 390x844 screenshot inspected; current test inventory confirmed as 194 in 9 files; scoped diff check passed |
| 2026-09-03 11:56 | Expanded and reformatted the active-job modal into seven clear operational sections; added canonical client identity and contact/account details, service descriptions/items and notes, admin/technician assignment evidence, route/reschedule context, truthful empty values, and mail/phone actions without adding a new data endpoint | `frontend/src/components/shared/ActiveJobDetailsDialog.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Production build passed with 2,516 modules; exact desktop/mobile modal journeys passed 2/2; final mobile screenshot run passed 1/1 and the 390x844 result was visually inspected; scoped diff check passed |
| 2026-09-03 11:49 | Replaced direct progress-card dispatch navigation with a responsive, accessible live job-details modal covering progress, people, service, schedule/start, location, priority/SLA, equipment reservations, and status history; retained explicit ticket-queue/dispatch actions plus loading retry, Escape/backdrop close, and focus restoration | `frontend/src/{api/services.js,components/shared/{ActiveJobDetailsDialog.jsx,ActiveTechnicianJobs.jsx},pages/admin/AdminDashboard.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Production build passed with 2,516 modules; desktop modal journey passed 1/1; final dedicated mobile modal journey passed 1/1 and its captured view was inspected; the broad diagnostic run passed 13/14 with only a corrected ambiguous Close locator before the focused passing rerun |
| 2026-09-03 11:34 | Split canonical technician progress into Inspection visit and Service job tracks with type-specific labels/percentages; inspection work now moves through ready/en-route/on-site/underway/submitted-for-review while approved records automatically use the service path, and both track identity fields flow through admin/client APIs and UI | `backend/services/{job_progress.py,views_dashboard.py,serializers/tickets.py,tests/test_dashboard_role.py}`, `frontend/src/{api/client.js,components/shared/ActiveTechnicianJobs.jsx,pages/client/ClientRequestDetail.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Django check passed; focused contracts passed 4/4; dashboard-role module passed 20/20; migration drift passed; production build passed with 2,515 modules; connected admin dashboard journey passed 1/1 |
| 2026-09-03 11:22 | Replaced elapsed-time technician percentages with one persisted-status workflow contract across the admin dashboard and client ticket API; included Navigating/Arrived jobs in active counts and queues, preserved the last achieved checkpoint on hold, added clear stage/paused labels and accessible progress semantics, and reserved 100% for completed/accepted work | `backend/services/{job_progress.py,views_dashboard.py,views/helpers.py,serializers/tickets.py,tests/test_dashboard_role.py}`, `frontend/src/{api/client.js,components/shared/ActiveTechnicianJobs.jsx,pages/client/ClientRequestDetail.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Django check and migration drift passed; focused progress tests passed 3/3; dashboard-role tests passed 19/19; production build passed with 2,515 modules; final connected dashboard journey passed 1/1. Two initial test-only shape/strictness failures were corrected and superseded by the passing reruns |
| 2026-09-02 23:19 | Applied the explicitly authorized two-party equipment-return schema migration to the local development SQLite database; production/Aiven was not touched | `backend/db.sqlite3`, `inventory.0007` contract, this file | Targeted migration completed successfully; `showmigrations inventory` marks `0007` applied; Django system check passed |
| 2026-09-02 22:56 | Replaced direct admin equipment returns with a two-party honesty control: the assigned technician submits bounded quantities plus declared condition/evidence notes, pending quantities block duplicate claims without changing stock, receiving staff independently record observed condition and verify/reject, only sealed/usable verification restores stock, damaged/incomplete returns cannot enter availability, and both actor records remain immutable | `backend/inventory/{models.py,tests.py,migrations/0007_equipmentreturnrequest_equipmentreturnrequestitem.py}`, `backend/services/views/tickets.py`, `frontend/src/{api/admin.js,components/shared/EquipmentReturnDialog.jsx,pages/{admin/AdminJobHistory.jsx,technician/TechnicianJobHistory.jsx}}`, `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js,seed-test-data-exec.py}`, this file | Auto inventory suite passed 24/24; focused new workflow checks passed 4/4; Django check and migration drift passed; build passed with 2,515 modules; live technician submit and admin verify journeys passed 1/1 each; both Job History pages passed desktop/mobile quality 4/4; scoped diff passed. The then-current unapplied local `0007` state was superseded by the explicitly authorized 23:19 migration; production remains untouched |
| 2026-09-02 22:21 | Added accountable unused-equipment returns for completed tickets: a dispatch-capability API computes outstanding issued quantities, rejects missing notes and over-returns under row locks, records append-only ticket/technician/operator-linked return movements, and exposes the bounded workflow through a focused Job History dialog; the isolated E2E seed now includes a real issued-equipment fixture so browser coverage crosses the live API and database | `backend/services/views/tickets.py`, `backend/inventory/tests.py`, `frontend/src/{api/admin.js,components/shared/EquipmentReturnDialog.jsx,pages/admin/AdminJobHistory.jsx}`, `e2e/{03-admin-workspace.spec.js,seed-test-data-exec.py}`, this file | Focused backend passed 3/3; Django check passed; production build passed with 2,515 modules; final full-stack Job History return journey passed 1/1 with live GET 200/POST 201; desktop/mobile page quality passed 2/2; scoped diff check passed. Initial invalid-settings and PowerShell-policy command invocations were corrected and superseded; the earlier mocked browser pass was replaced by the live isolated-database run |
| 2026-09-02 21:03 | Consolidated Dispatch Board equipment handling into one stock-aware Equipment Reservation Plan: removed repeated representations, distinguished current pending versus completed reservations, prevented duplicate selections, labeled the controls, disclosed stock and partial-shortage outcomes, guarded invalid quantities, and added deterministic E2E coverage plus a selected-plan screenshot | `frontend/src/pages/admin/AdminDispatchBoard.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Production build passed with 2,514 modules; final focused equipment interaction passed 1/1; Dispatch desktop/mobile quality passed 2/2; equipment-plan screenshot inspected |
| 2026-09-02 20:27 | Reworked the Dispatch Board UI into a simple guarded assignment flow: reduced the crowded six-card summary to four active-work metrics, added visible select/team/review steps, reordered the panels to match, disabled premature or unavailable-technician actions, added clear-selection and board-refresh controls, clarified current-assignment editing, and corrected zero-record pagination text | `frontend/src/pages/admin/AdminDispatchBoard.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Final production build passed with 2,514 modules; focused assignment interaction passed 1/1; desktop/mobile Dispatch quality passed 2/2; fresh screenshot inspected |
| 2026-09-02 16:54 | Removed ambiguity between the Service Tickets and Dispatch Board totals: renamed the former to All Active Tickets with assigned/unassigned scope, renamed the latter to Ready to Assign, added active-subset context and consistent empty-state language, and covered the distinction in E2E | `frontend/src/pages/admin/{AdminServiceTickets.jsx,AdminDispatchBoard.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Final production build passed with 2,514 modules; focused connected pages passed 2/2; fresh screenshots inspected |
| 2026-09-02 16:43 | Corrected the requested border target after visual clarification: restored the unrelated stat-card hover treatment and removed the amber/emerald outer border only from the dashboard operations-attention strip, which now uses a neutral slate border | `frontend/src/pages/admin/AdminDashboard.jsx`, `frontend/src/{index.css,components/ui/StatsCard.jsx}`, this file | Final production build passed with 2,514 modules |
| 2026-09-02 16:40 | Removed the glowing gradient edge and brand-colored hover shadow from shared stat cards; retained a subtle neutral lift and the accessible keyboard focus ring | `frontend/src/{index.css,components/ui/StatsCard.jsx}`, this file | Production build passed with 2,514 modules |
| 2026-09-02 16:29 | Refined the admin/superadmin first-screen UI into a simple, productive command center: added a compact live snapshot and refresh control, clearer clickable KPI affordances, one low-height operations-attention strip with truthful direct actions, visually distinct approval/schedule headers, and a responsive labeled Service Tickets queue selector | `frontend/src/pages/admin/{AdminDashboard.jsx,AdminServiceTickets.jsx}`, `frontend/src/components/ui/StatsCard.jsx`, this file | Final production build passed with 2,514 modules; final focused dashboard journey passed 1/1; final dashboard desktop/mobile quality passed 2/2; preceding combined affected-page quality passed 4/4; fresh dashboard and Service Tickets screenshots inspected. The initial focused run exposed and led to correction of a queue-button accessible-name regression |
| 2026-09-02 16:02 | Corrected the dashboard links after user review: Pending Approvals now focuses the real in-dashboard review queue instead of opening a ticket-only page; active, unassigned, missed-dispatch, SLA-warning, and SLA-overdue work now use distinct URL-backed Service Tickets views; signals without a truthful destination are noninteractive | `frontend/src/pages/admin/{AdminDashboard.jsx,AdminServiceTickets.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Production build passed with 2,514 modules; focused connected journeys passed 2/2; dashboard and Service Tickets desktop/mobile quality passed 4/4 |
| 2026-09-02 15:50 | Upgraded the admin/superadmin landing dashboard into an actionable operations workspace: added capability-safe KPI/alert/queue navigation, nonzero inventory/maintenance/after-sales/unassigned-visit attention signals, subset totals, manual refresh and last-known-data feedback, honest technician/stage labels, accessible dialogs/focus restoration, and mandatory client-visible rejection reasons | `backend/services/{views_dashboard.py,tests/test_dashboard_role.py}`, `frontend/src/{pages/admin/AdminDashboard.jsx,components/shared/{ActiveTechnicianJobs.jsx,ConfirmationDialog.jsx},components/ui/StatsCard.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Dashboard/capability backend passed 25/25; final production build passed with 2,514 modules; corrected dashboard journey passed 1/1, desktop/mobile dashboard quality passed 3/3, smoke passed 13/13, and the full admin workspace passed 27/27; fresh desktop screenshot inspected. The first browser attempt identified and led to correction of one schedule-fallback initialization-order error |
| 2026-09-02 15:06 | Centralized authenticated notification state so Sidebar, Topbar, and the technician dashboard share one guarded fetch/poll lifecycle and read/delete actions; retained immediate event-driven refresh, contextual badges, last-known state on transient failures, and strengthened E2E coverage against per-consumer request duplication | `frontend/src/{App.jsx,context/NotificationContext.jsx,components/layout/{Sidebar.jsx,Topbar.jsx},pages/technician/TechnicianDashboard.jsx}`, `e2e/04-technician-workspace.spec.js`, this file | Production build passed with 2,514 modules; focused technician request-count and GPS journey passed 1/1, client badge passed 1/1, browser smoke passed 13/13, dashboard matrix passed 12/12, and Notifications desktop/mobile routes passed 6/6 |
| 2026-09-02 13:43 | Hardened the first authenticated screen for every role: removed technician dashboard entry-time GPS watching and the duplicate warning banner, added one explicit session-scoped sharing control, replaced mouse-only client dashboard rows with focusable service controls, and strengthened all role dashboard E2E checks around real stats/actions plus consent behavior | `frontend/src/pages/{technician/TechnicianDashboard.jsx,client/ClientDashboard.jsx}`, `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js,05-client-workspace.spec.js}`, this file | Dashboard/capability backend checks passed 25/25; production build passed with 2,513 modules; focused role dashboards passed 3/3, explicit technician GPS passed 1/1 with zero pre-consent and expected post-consent calls, final dashboard matrix passed 12/12, and updated technician/client screenshots were visually inspected |
| 2026-09-02 12:47 | Added a direct Notifications sidebar entry for admin, technician, and client roles; attached the authoritative total unread count with the existing `99+` cap; preserved contextual work badges; and synchronized sidebar/top-bar counts immediately after notification read/delete actions | `frontend/src/components/layout/{Sidebar.jsx,Topbar.jsx}`, `frontend/src/pages/client/ClientNotifications.jsx`, `e2e/07-side-flows.spec.js`, this file | Production build passed with 2,513 modules; the first focused browser run exposed only the intentionally empty notification fixture, the deterministic badge rerun passed 1/1, and all role-scoped Notifications desktop/mobile routes passed 6/6 |
| 2026-09-02 12:34 | Stabilized two test-only full-suite assertions: Activity Logs now finds a real service event before checking its related-workspace link, and technician My Jobs now asserts the current accessible job heading/actions instead of obsolete generic copy; no product runtime behavior changed | `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js}`, this file | Initial full browser run passed 191/193 and identified both assumptions; corrected focused checks passed 2/2; final uninterrupted full browser suite passed 193/193 in 15.8 minutes; the preceding unified quality gate passed all checks, 378 runnable backend tests plus 3 expected skips, 2,513-module build, and 13/13 smoke tests |
| 2026-09-02 10:18 | Prepared the canonical living audit for direct agent handoff by adding a concise latest checkpoint covering the newest completed work, current affected and broad evidence, undeployed release gates, and safe next-agent boundary; no runtime code or behavior changed | this file | Documentation entrypoint reference confirmed; handoff secret-pattern scan returned no matches; scoped Markdown diff check passed |
| 2026-09-02 09:47 | Refined admin and technician Job History View details dialogs without redesigning them: strengthened completed-record identity and section hierarchy, replaced decorative treatment with neutral record surfaces, added conventional persistent action footers, and retained all connected data, permissions, downloads, and workflows | `frontend/src/pages/{admin/AdminJobHistory.jsx,technician/TechnicianJobHistory.jsx}`, `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js}`, this file | Focused admin/technician journeys passed 2/2 after one exact-locator correction; admin passed again 1/1 after the fixed-footer sizing correction; both screenshots were visually inspected and the final production build transformed 2,513 modules |
| 2026-09-01 23:26 | Hardened admin and technician Job History into connected completed-work records: aligned lifecycle inclusion, added server pagination/sort/search/date contracts, recoverable loading, actual work timing, crew/material/equipment/report/warranty/maintenance/after-sales/document/timeline detail, all-filtered CSV, accurate technician summary labeling, dialog semantics/Escape handling, and clean route-entry scroll position | `backend/services/views/{reports.py,technician.py}`, `backend/services/tests/test_job_history_contract.py`, `frontend/src/api/{services.js,technician.js}`, `frontend/src/pages/{admin/AdminJobHistory.jsx,technician/TechnicianJobHistory.jsx}`, `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js}`, this file | Focused backend passed 3/3 and related capability checks 6/6; one attempted stale-prefetch optimization caused two full-suite regressions, was removed, and the affected workflows passed 5/5; final full backend passed 378/378 runnable tests with 3 expected skips, Django check/migration drift passed, Vite built 2,513 modules, final combined Job History journeys passed 2/2, and the final route-entry rerun passed 1/1 with its corrected screenshot visually inspected |
| 2026-09-01 22:49 | Synchronized current audit Markdown after the tracking privacy/retention phase: completed the plan item, replaced the outdated partial-GPS description with the explicit-sharing/runtime-policy contract, and labeled the old 71-test report as superseded historical evidence | `docs/{SYSTEM_END_TO_END_IMPLEMENTATION_PLAN.md,AFN_SERVE_FULL_SYSTEM_ANALYSIS_CLIENT_REQUIREMENT_MAPPING.md}`, `e2e/E2E_TEST_REPORT_DETAILED.md`, this file | Documentation consistency scan found no remaining obsolete partial-tracking statement; final `git diff --check` passed with line-ending warnings only |
| 2026-09-01 22:46 | Implemented the technician-location operational baseline: backend-validated configurable service bounds and trail/retention settings, authenticated policy metadata, explicit technician-controlled sharing, no pre-sharing routing/fake ETA, daily deletion of expired history and current coordinates, environment/deployment contracts, and a staff-facing privacy/retention policy | `.env.example`, `backend/.env.example`, `backend/{afn_service_management/settings.py,api/views.py,services/{tracking_config.py,views/inspection.py,management/commands/{purge_technician_locations.py,run_operational_automations.py},test_location_retention.py,test_operational_automations.py,tests/test_staff_workspace_capability_access.py}}`, `frontend/src/{api/services.js,utils/mapRegion.js,pages/{admin/AdminTechnicianTracking.jsx,technician/TechnicianMapNavigation.jsx}}`, `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js}`, location/deployment docs, this file | Final focused backend passed 13/13 after correcting an invalid test-only base-admin assumption; Django check/migration drift and final 2,513-module build passed; connected tracking/explicit-sharing browser checks passed 3/3, final no-auto-collection check passed again 1/1, affected page quality passed 4/4, and the final technician policy screenshot was visually inspected |
| 2026-09-01 22:11 | Upgraded Technician Tracking from a marker overview into a connected field-operations workspace: canonical active lifecycle jobs, GPS freshness/accuracy, active workload and contact context, search and operational filters, honest route fallback labeling, recent technician trails, resilient refresh with last-known data, actionable empty states, and responsive detail/job views | `backend/api/views.py`, `backend/services/{views/inspection.py,tests/test_staff_workspace_capability_access.py}`, `frontend/src/{api/services.js,pages/admin/AdminTechnicianTracking.jsx}`, `e2e/{seed-test-data-exec.py,03-admin-workspace.spec.js}`, this file | Focused backend passed 8/8; final Django check/migration drift/build/diff checks passed; final tracking desktop/mobile journeys passed 2/2 and separate page-quality checks passed 2/2; screenshots visually inspected |
| 2026-09-01 17:43 | Upgraded Activity Logs into an investigation-ready audit workspace: replaced page-local category counts with full filtered server summaries, added exact technical evidence, stable event references, and related-workspace navigation, added visible refresh state, and provided capability-protected streamed filtered CSV export with spreadsheet-formula neutralization | `backend/users/{views/admin_activity.py,tests.py}`, `frontend/src/{api/admin.js,pages/admin/AdminActivityLogs.jsx}`, `e2e/03-admin-workspace.spec.js`, this file | Focused backend passed 9/9; Django check/migration drift/build/diff checks passed; desktop workflow verified evidence, navigation, refresh, download, and filtering; mobile evidence plus page quality passed 4/4 final browser checks; screenshots visually inspected |
| 2026-09-01 17:12 | Replaced the long inline Create Walk-in Request panel with a focused responsive dialog, grouped the workflow into client/service, schedule, and location sections, clarified save-for-review versus immediate approval, added sticky actions and discard/clear protection, and retained the existing API behavior | `frontend/src/components/shared/WalkInRequestDialog.jsx`, `frontend/src/pages/admin/AdminServiceTickets.jsx`, `e2e/03-admin-workspace.spec.js`, this file | 2,513-module production build passed; real save-for-review flow returned HTTP 201; draft guard passed; dedicated mobile dialog regression passed; Service Tickets desktop/mobile page quality passed 2/2; screenshots visually inspected |
| 2026-09-01 16:47 | Removed the profile identity gradient and replaced it with the existing solid brand blue for a more conventional, less template-generated appearance | `frontend/src/components/shared/ProfileIdentityCard.jsx`, this file | Final 2,512-module production build passed; focused client profile browser check passed; fresh desktop screenshot visually inspected |
| 2026-09-01 16:36 | Enriched all three profiles without changing their workflows: added a shared restrained brand-gradient identity card, larger avatar, role/status badges, canonical account ID and lifecycle facts, clearer contact/security section hierarchy, bordered information tiles, and removed the repeated local description; retained the role-specific client and technician content | `frontend/src/components/shared/{ProfileIdentityCard.jsx,ProfileSecuritySection.jsx}`, `frontend/src/pages/{admin/AdminProfile.jsx,client/ClientProfile.jsx,technician/TechnicianProfile.jsx}`, this file | An intermediate build caught and led to correction of JSX nesting; final production build passed with 2,512 modules; profile behavior passed 4/4, final desktop/mobile page quality passed 6/6, and the final client desktop screenshot check passed and was visually inspected |
| 2026-09-01 13:13 | Hardened all role profiles without redesigning them: cancel restores server-backed fields/photo, saves update AuthContext without reload, pending email and image constraints are explicit, client company/landline/full name remain visible, password inputs are labeled and shared with show/hide and clearer errors, readable admin permission wording replaces raw scope codes, and the duplicate profile logout card is gone | `frontend/src/{api/client.js,context/AuthContext.jsx,components/shared/ProfileSecuritySection.jsx,utils/profile.js,pages/admin/AdminProfile.jsx,pages/client/ClientProfile.jsx,pages/technician/TechnicianProfile.jsx}`, `e2e/{03-admin-workspace.spec.js,04-technician-workspace.spec.js,05-client-workspace.spec.js}`, this file | Final 2,511-module build passed; role profile behavior reached 3/4 before an ambiguous test locator was made exact, then the affected client save/cancel/photo/security journey passed; all six desktop/mobile profile page-quality checks passed |
| 2026-09-01 12:42 | Removed Coverage Heatmap's redundant standalone Map layers card by placing its two independent layer toggles beside Filters in the shared compact toolbar; retained clear selected/unselected state and accessible pressed semantics | `frontend/src/components/shared/SearchFilterBar.jsx`, `frontend/src/pages/admin/CoverageHeatmap.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Production build passed with 2,509 modules; focused functional plus desktop/mobile browser checks passed 3/3; generated desktop screenshot visually inspected |
| 2026-09-01 12:32 | Standardized optional filters around a lower-confusion search-first interaction: one Filters disclosure, descriptive field/option labels, visible removable active-filter chips, and Clear all; migrated eight filter-heavy operational/reporting pages while keeping required form choices and immediate controls visible | `frontend/src/components/shared/SearchFilterBar.jsx`, `frontend/src/pages/admin/{AdminActivityLogs.jsx,AdminInventory.jsx,AdminJobHistory.jsx,AdminOperationsReport.jsx,AdminReports.jsx,CoverageHeatmap.jsx}`, `frontend/src/pages/follow_up/FollowUpCases.jsx`, `frontend/src/pages/client/ClientServiceHistory.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Production build passed with 2,509 modules; 18/18 focused browser checks passed across functional Inventory filtering/receiving and all migrated desktop/mobile pages |
| 2026-09-01 12:10 | Fixed Save Movement from every Receive entry point by replacing the invalid frontend-only `in` code with the inventory ledger's valid `purchase` transaction type; expanded the browser regression to perform and verify an actual receipt instead of stopping after dialog-open | `frontend/src/pages/admin/AdminInventory.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Isolated receive journey returned HTTP 201, closed the dialog, refreshed stock, and showed Qty 5 plus its supplier note in movement history; final 2,508-module production build passed |
| 2026-09-01 12:01 | Refocused Admin Inventory as a service-stock operations workspace: added actionable physical/reserved/available summaries and condition filters, reduced the desktop table to operational columns, added mobile item cards, removed duplicate low-stock/movement UI, activated guarded receive/correction actions, and exposed per-item ticket reservation context through a scoped API filter | `backend/inventory/{serializers.py,views.py,tests.py}`, `frontend/src/pages/admin/AdminInventory.jsx`, `e2e/03-admin-workspace.spec.js`, this file | Focused backend suite passed 20/20; Django check/migration drift passed; 2,508-module build passed; custom item/history/reservation/dialog journey plus desktop/mobile page-quality passed 3/3; diff check passed with line-ending warnings only |
| 2026-08-31 23:54 | Made the top bar the single canonical page title throughout authenticated workspaces, removed repeated lower titles and a hidden obsolete after-sales hero, added missing Operations Report/Landing Page Editor shell metadata, and retained dynamic record plus real document/report headings | `frontend/src/components/layout/Topbar.jsx`, affected admin/client/technician/follow-up/shared pages, `e2e/{03-admin-workspace.spec.js,05-client-workspace.spec.js,07-side-flows.spec.js}`, this file | Final 2,508-module build passed; all 36 affected desktop/mobile page-quality checks passed; 3 updated Sales/Purchase Records and Notifications functional checks passed; diff check passed with line-ending warnings only |
| 2026-08-31 23:15 | Applied a restrained UI-consistency cleanup without redesigning workflows: removed the duplicate tablet breadcrumb and global hard-refresh control/dead action logic, reduced nested workspace padding, corrected nested main/document landmarks, shortened the clipped after-sales label with full hover titles, and protected client mobile content from the assistant launcher | `frontend/src/components/layout/{Layout.jsx,Sidebar.jsx,Topbar.jsx}`, `frontend/src/pages/admin/{AdminClientSupport.jsx,AdminDocuments.jsx,AdminLandingPage.jsx,AdminProfile.jsx,AdminSettings.jsx}`, `frontend/src/pages/client/{ClientNotifications.jsx,ClientProfile.jsx}`, `frontend/src/pages/technician/TechnicianProfile.jsx`, this file | Final 2,508-module production build passed; 12 affected admin/client/technician desktop/mobile page-quality checks and 6 final Documents/Notifications functional/responsive checks passed on isolated E2E services |
| 2026-08-31 22:25 | Completed a whole-system functional connectivity audit and repaired the issues it exposed: restored non-empty Operations Report serialization after the serializer-package split, added data-backed report regression coverage, removed mobile Schedule action clipping, and made the notifications assertion unambiguous | `backend/services/{serializers/tickets.py,test_reporting_capabilities.py}`, `frontend/src/pages/technician/TechnicianSchedule.jsx`, `e2e/07-side-flows.spec.js`, this file | Backend/build portions of both full gates passed with 369 tests plus 3 expected PostgreSQL skips and 2,508 modules; second browser run passed 186/188 before both remaining issues were fixed; focused report tests passed 6/6, fixed browser flows passed 2/2, environment-interrupted document/Analytics flows passed 2/2 on recovery, and final check/migration-drift/build/diff checks passed |
| 2026-08-31 21:33 | Recorded fresh full-stack smoke evidence after the test and serializer package cleanup; no runtime code or schema changed | this file | `npm run quality` passed check/dependencies/migration drift, complete 372-test discovery with 3 expected skips, the 2,508-module frontend build, and 13/13 isolated Chromium smoke tests |
| 2026-08-31 21:20 | Replaced the 73 KB services serializer monolith with seven domain-focused modules behind the same explicit public import surface | removed `backend/services/serializers.py`, added `backend/services/serializers/`, this file | All 30 class ASTs exactly matched the verified pre-cleanup source; public import exposed 30 classes; services suite passed 193 tests and full backend discovery passed 369 with 3 expected skips; compile/check/migration drift/diff checks passed |
| 2026-08-31 21:13 | Replaced the 176 KB services test monolith with a discoverable domain-focused package while preserving all 15 classes and 118 test methods | removed `backend/services/tests.py`, added `backend/services/tests/`, this file | Services suite passed 193 tests with 3 expected PostgreSQL skips; full backend discovery found 372 tests and passed 369 with the same 3 skips; Django check, migration drift, and `git diff --check` passed |
| 2026-08-31 21:02 | Created verified pre-cleanup database/source backups with secrets and generated content excluded, added ZIP backup ignore coverage, and removed four dead history runtime modules while preserving its production migration shell | `.gitignore`, `backend/history/{admin.py,serializers.py,urls.py,views.py}`, ignored `backend/local_backups/{db-before-file-structure-cleanup-20260831-1827.sqlite3,source-before-file-structure-cleanup-20260831-1827.zip}`, this file | Both backups SHA-256 verified; archive had 695 entries and zero exclusion violations; focused 22 tests and full 372-test backend discovery passed with 3 expected PostgreSQL skips; check/migration drift passed |
| 2026-08-31 18:20 | Applied the explicitly authorized dormant-schema cleanup to local SQLite after an ignored safety backup; removed the three empty physical tables without touching production/Aiven | `backend/db.sqlite3`, ignored `backend/local_backups/db-before-unused-schema-cleanup-20260831-1819.sqlite3`, this file | Both migrations applied; retired tables absent; integrity `ok`; zero FK violations; core record counts unchanged; Django check passed |
| 2026-08-31 18:15 | Retired three confirmed zero-row, disconnected schema models and the redundant history API while retaining active Notification, ServiceStatusHistory, and secured TicketProgress contracts; added fail-closed deletion migrations and aligned current DBML/schema documentation | `backend/{history,notifications,api/urls.py}`, current architecture/model/analytics documentation, this file | Focused 22-test suite and full 372-test backend discovery passed with 3 expected PostgreSQL skips; Django check and migration drift passed; migrations were not applied during this code-validation step and the later local application is recorded above |
| 2026-08-31 17:36 | Made completion checklists service-specific for single- and multi-service tickets: the technician API exposes every ordered service template without per-ticket list queries, the UI renders a labeled section and required equipment for each service, and submissions snapshot service identity per step | `backend/{services/views/technician.py,api/tests.py}`, `frontend/src/{api/technician.js,pages/technician/TechnicianChecklist.jsx}`, `e2e/{seed-test-data-exec.py,04-technician-workspace.spec.js}`, this file | Full 20-test API suite passed after the final query optimization; 2,508-module production build passed; focused isolated two-service browser flow passed and screenshot was inspected |
| 2026-08-31 12:38 | Removed the remaining checklist draft ambiguity by telling technicians exactly which non-file fields persist locally and that selected photos/videos must be reattached after refresh | `frontend/src/pages/technician/{TechnicianChecklist.jsx,TechnicianInspectionChecklist.jsx}`, this file | Frontend production build passed with 2,508 modules |
| 2026-08-31 12:36 | Reworked technician completion and site-inspection checklists for field usability: collapsed procedures into one touch-friendly work screen, added device-local non-file draft recovery/removable proof, required explicit inspection answers and negative-finding notes in UI and API, added readiness/mobile controls, made incomplete inspection retries resumable, and made E2E uploads fully local | `frontend/src/pages/technician/{TechnicianChecklist.jsx,TechnicianInspectionChecklist.jsx}`, `frontend/src/api/technician.js`, `backend/{services/{serializers.py,views/inspection.py},api/tests.py,afn_service_management/settings_e2e.py}`, `e2e/{04-technician-workspace.spec.js,seed-test-data-exec.py}`, this file | Full 367-test backend discovery passed with 3 expected PostgreSQL skips; system/migration/diff checks and 2,508-module frontend build passed; focused real browser flows passed, including clean mobile validation, local PNG upload, and create/upload/complete; initial external-upload failure is retained in the validation ledger and superseded |
| 2026-08-31 12:10 | Prepared Phase 5 production-shaped staging validation: fail-closed environment/database targeting, live PostgreSQL/Redis/Cloudinary probes, real PostgreSQL approval/inventory race tests, stricter storage readiness, and read-only HTTPS/header/CORS/anonymous-access perimeter checks | `backend/afn_service_management/{settings.py,health.py,tests_health.py}`, `backend/services/test_postgres_concurrency.py`, `backend/users/{management/commands/verify_staging_dependencies.py,test_staging_verification.py}`, `scripts/{staging-gate.mjs,staging-perimeter-check.mjs}`, environment/package/deployment contracts, this file | 11 focused preflight tests and Node syntax/refusal checks passed; `npm run quality` passed all local gates with 362/362 runnable backend tests, 3 correctly skipped PostgreSQL-only tests, 2,508-module build, and 13/13 browser smoke tests; production-shaped execution remains pending provisioned staging services |
| 2026-08-31 11:40 | Completed Phase 4 audit evidence and public-site least privilege: every central ticket transition now requires normalized evidence, manual status/reschedule/inspection-exception flows collect operator reasons, and public-site publishing, asset upload, and asset deletion have independent capabilities with legacy-manage compatibility | `backend/{services/{views/helpers.py,views/tickets.py,tests.py},users/{rbac.py,tests.py,views/admin_settings.py}}`, `frontend/src/{rbac.js,components/shared/{InspectionReviewModal.jsx,RescheduleTicketModal.jsx},pages/admin/AdminLandingPage.jsx}`, workflow/auth/route contracts, this file | Django check and 49 focused tests passed; `npm run quality` passed dependency/migration checks, all 359 backend tests, the 2,508-module build, and 13/13 browser smoke tests |
| 2026-08-31 11:25 | Completed Phase 3 least-privilege workflow/data boundaries: dedicated operations-dashboard access, split coverage vs completed-job evidence, capability-scoped technician field inventory and dashboard stock alerts, capability-aware admin login fallback, REST/WebSocket parity for direct client ticket replies, and support-image spoof rejection coverage | `backend/{services/{views_dashboard.py,views/reports.py,tests.py,test_reporting_capabilities.py},inventory/tests.py,messages_app/{tests.py,views.py},users/{permissions.py,rbac.py}}`, `frontend/src/{App.jsx,rbac.js}`, security contracts, this file | 51 focused cross-domain tests and expanded 11-test inventory module passed; first full gate exposed one stale dashboard expectation; after correction `npm run quality` passed check/dependencies/migration drift, 354 backend tests, 2,508-module build, and 13/13 browser smoke tests |
| 2026-08-31 04:08 | Completed the Phase 2 security gate: added frontend/backend parity for staff messaging, support view/manage, support notification privacy, ticket WebSocket authority (including technician message capability), and system settings view/manage; added CI dependency auditing; patched Django 6.0.6 to 6.0.8 after four disclosed advisories | `backend/{requirements.txt,messages_app/{consumers.py,tests.py,views.py},users/{permissions.py,rbac.py,tests.py,views/admin_settings.py}}`, `frontend/src/{App.jsx,rbac.js,components/layout/Sidebar.jsx}`, `scripts/release-gate.mjs`, `.github/workflows/quality.yml`, security/deployment contracts, this file | 28 focused authorization tests and final 348-test backend suite passed; `pip check`, `pip-audit`, and both npm audits passed; `npm run quality` passed checks, migration drift, its then-current 347 backend tests, 2,508-module build, and 13/13 browser smoke tests; final `git diff --check` passed with line-ending warnings only |
| 2026-08-31 01:15 | Completed the production-readiness Phase 1 release gate: made root/CI Django discovery exhaustive, replaced hanging Windows Playwright web-server teardown with direct managed children, prohibited unsafe reuse of development servers, assigned dedicated E2E defaults, added smoke/full one-command release gates, and time-bounded CI jobs | `backend/manage.py`, `playwright.config.js`, `e2e/{global-setup.js,helpers.js}`, `scripts/release-gate.mjs`, `package.json`, `.github/workflows/quality.yml`, this file | `npm run quality` passed checks, migration drift, 340 backend tests, 2,508-module build, and 13 browser smoke tests; subsequent complete Playwright run passed 188/188 in 13.6 minutes and exited normally; syntax checks passed |
| 2026-08-30 23:55 | Restored Admin Documents installed-equipment and quotation prefill by importing the existing shared `pickNumericId` helper; updated the current browser inventory and recorded the backend-discovery and Playwright-teardown findings exposed by the full-system test run | `frontend/src/pages/admin/AdminDocuments.jsx`, this file | Frontend build passed with 2,508 modules; focused document draft journey passed, both dependent lookups returned HTTP 200, and no `pickNumericId` error recurred; focused runner was interrupted only after its known teardown delay |
| 2026-08-30 17:51 | Reworked the Admin Analytics first screen into a responsive at-a-glance performance pulse with a completion gauge, stacked request outcomes, compact KPIs, correct time-series/category chart types, priority-sorted signals, and clean empty states; retained all deeper analytics workspaces | `frontend/src/pages/admin/AdminAnalytics.jsx`, admin Analytics E2E assertion, this file | Final 2,508-module Vite build passed; focused authenticated Analytics Playwright check passed on isolated 8011/5181 test services; 1440x900 screenshot visually inspected with no overlap |
| 2026-08-30 17:33 | Hardened system automations: deduplicated repeating SLA notifications, prioritized three-day maintenance catch-up, corrected stale warranty regressions, added grouped frequent/daily operational command, made analytics failures observable, and replaced workstation-specific scheduler paths/scripts | notification utility/SLA tests, maintenance/tests, operational and analytics management commands, `automation/`, deployment/after-sales docs, this file | 7 focused tests and full 340-test Django suite passed; Django check/migration drift passed; PowerShell parsing and path scan passed; `git diff --check` passed with line-ending warnings only |
| 2026-08-30 17:02 | Completed the record-identity phase without changing database IDs: distinct `CSC` customer support and `ASC` after-sales cases, generated/unique-on-API `QUO`, computed `ITX`/`RSV`/`DOC`/`EST` codes, prominent UI labels, six-digit ledger widths, and a canonical identity contract | inventory/services serializers and tests, solar estimate view, shared ID formatter, support/inventory/solar/technician/document frontend paths, identity/inventory contracts, docs index, this file | 7 focused backend tests passed across successful reruns; Django check and migration drift passed; direct identity assertions and final 2,508-module Vite build passed |
| 2026-08-30 16:39 | Standardized initial entity identities without changing database IDs: requests `REQ-####`, tickets `TKT-####`, clients `CL-####`, technicians `TECH-####`, and quotations `QUO-####`; normalized raw/legacy ticket labels and replaced remaining hand-built ticket/request displays | shared role ID utility, technician API normalization, assistant/inspection components, admin Documents/Operations/Tickets pages, client request/history/support pages, Sales Records, this file | Direct formatter assertions passed including `TCK-35` -> `TKT-0035`; frontend Vite production build passed (2,508 modules) |
| 2026-08-30 15:47 | Applied the explicitly authorized Sales Record migration to the local development SQLite database; production/Aiven was not touched | `backend/db.sqlite3`, Sales Record contract, this file | `services.0057` applied successfully; `showmigrations` marks it applied; Django system check passed |
| 2026-08-30 13:19 | Implemented connected non-billing Sales Records: completed-ticket preparation, accepted-quotation/inventory/equipment/warranty snapshots, draft value review, immutable confirmation, reasoned void/replacement, document-capability enforcement, client-owned Purchase Records, navigation, assistant routing, audit signals, schema/tests/contract | sales model/migration/serializer/API/admin/test, frontend API/routes/layout/shared page, E2E route suites, Sales Record contract, this file | 11 focused backend tests passed on fresh migrated DB; Django check and migration drift passed; frontend production build passed; focused admin and client browser checks passed on isolated ports 8011/5181; migration not applied to local/Aiven |
| 2026-08-30 12:24 | Removed the client-facing implication of an in-system billing workflow: relabeled support as purchase-record help and made the assistant state that the portal records delivered products/services but does not process payments; retained the internal `billing` category value for migration-free compatibility | client assistant, client/admin support pages, client E2E, this file | Frontend production build passed; focused authenticated assistant E2E passed on isolated ports 8011/5181 |
| 2026-08-30 12:07 | Added a client-only support assistant with common-question guidance, client-scoped ticket/support summaries, sensitive-data refusal, safety guidance, and direct workflow links while keeping admin analytics isolated | `frontend/src/components/shared/ClientSupportAssistant.jsx`, `frontend/src/components/layout/Layout.jsx`, `e2e/05-client-workspace.spec.js`, this file | Frontend production build passed; focused authenticated client assistant E2E passed on isolated ports 8011/5181; final `git diff --check` passed with line-ending warnings only |
| 2026-08-30 11:35 | Replaced Coverage Heatmap's newly key-gated CARTO raster layer with the shared OpenStreetMap provider and hid the redundant one-option layer switcher | `frontend/src/components/maps/MapTileLayer.jsx`, `frontend/src/pages/admin/CoverageHeatmap.jsx`, this file | Frontend build passed; focused isolated Coverage Heatmap E2E passed; source scan found no remaining CARTO tile references |
| 2026-08-30 10:53 | Implemented customizable inventory catalog and integrity phases: category management, product types/free-form units/full details, normalized unique SKUs, opening-balance history, derived stock state, retirement safeguards, service-template filtering, and table action overlap fix; verified the existing landing promotion flow | inventory models/serializer/views/tests/migration/contract, `frontend/src/pages/admin/{AdminInventory.jsx,AdminServices.jsx}`, admin E2E spec, this file | 37 focused backend tests passed; finalized migration preflight rerun passed 9 tests; migration drift/Django check passed; local `inventory.0006` applied; frontend build passed; isolated custom-product E2E passed |
| 2026-08-29 23:54 | Applied the approved after-sales event migration to local development SQLite and verified the follow-up queue | `backend/db.sqlite3`, this file | `services.0056` applied; Django check passed; authenticated follow-up list returned HTTP 200 with paginated data |
| 2026-08-29 23:47 | Implemented the after-sales ticketing hardening phase: lifecycle rules, resolution/reopen evidence, case ownership, server pagination, activity events, corrected warranty response deadlines, turned-over eligibility, and functional coverage | after-sales models/migration/serializer/API/maintenance/tests, `frontend/src/api/services.js`, `frontend/src/pages/follow_up/FollowUpCases.jsx`, E2E seed/spec, after-sales/deployment docs, this file | Django check and migration drift passed; 15 focused backend tests passed; frontend build passed; browser flow reached successful create/resolve/activity UI with teardown caveat |
| 2026-08-29 21:28 | Changed unread notification indicators from brand/amber tones to red for stronger visibility | `frontend/src/components/layout/Topbar.jsx`, `frontend/src/components/layout/Sidebar.jsx`, this file | Frontend build passed |
| 2026-08-29 21:17 | Fixed Admin User Management row action overlap by widening the table/action column and stacking action buttons | `frontend/src/pages/admin/AdminUserManagement.jsx`, this file | Frontend build passed; focused desktop/mobile User Management page-quality checks printed `ok` before teardown interrupt |
| 2026-08-29 20:53 | Applied pending inventory/services migrations to local development SQLite and verified dashboard APIs | `backend/db.sqlite3`, this file | `migrate` passed; `showmigrations services inventory --plan` shows all applied; `manage.py check` passed; `5174` login/dashboard/analytics smoke passed |
| 2026-08-29 19:35 | Added CI enforcement requiring the living handoff in every repository change | `.github/workflows/quality.yml`, this file | Required job/path references present and `git diff --check` passed; structure/shell reviewed; no local YAML parser available |
| 2026-08-29 19:30 | Added canonical living audit/handoff and mandatory maintenance policy | `AGENTS.md`, this file, `docs/README.md` | All 12 required references exist; secret-pattern scan clean; `git diff --check` passed |
| 2026-08-29 19:00 | Added route-complete page audit; fixed ticket/message action names, technician mobile activity layout, Analytics scheduled-date 500, and isolated E2E ports | frontend pages, Analytics view/test, Playwright config/helpers/spec | 104/104 page checks, 6/6 focused backend tests, frontend build passed |
| 2026-08-29 earlier | Completed phases 3-9 across security, workflow, inventory, documents, deployment, and E2E | See worktree manifest and dated checkpoints | 321-test backend checkpoint, 75 browser journeys, audits/build/checks passed |
